.claude/
.serena/

# Build artifacts
build/

# Claude authentication token (NEVER commit this!)
.claude-docker-token

# Vibrator workspace pin — may contain a plaintext claude-mem API key
# after auto-bootstrap. Per-workspace, never shared via VCS.
.vb.env

# website build artifacts
web/

.DS_Store
core
