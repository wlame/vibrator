{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"Vibrator","text":"<p>Run AI coding agents in disposable, per-workspace Docker sandboxes \u2014 configured by a single <code>.vb</code> file.</p> <p>Vibrator (<code>vibrate</code>, alias <code>vb</code>) is a single Go binary that builds a tailored Docker image for each of your projects and drops you straight into an AI coding agent \u2014 Claude Code, Codex, OpenCode, or Pi \u2014 running inside it. The agent gets a full toolchain, your host credentials, and your project mounted at the same absolute path, while staying isolated from the rest of your machine.</p> <pre><code>cd ~/my-project\nvibrate          # wizard fills the gaps, image builds, agent drops you in\n</code></pre> <p>The first run writes a <code>.vb</code> file capturing your choices, so every subsequent <code>vibrate</code> in that workspace reuses the container and jumps in instantly.</p>"},{"location":"#why-use-it","title":"Why use it?","text":"<ul> <li> <p> Isolation per workspace</p> <p>Each project gets its own image and container, keyed by a content fingerprint. Two projects never share state, and the agent can't touch anything outside the mounted workspace.</p> </li> <li> <p> Declarative configuration</p> <p>A small <code>.vb</code> TOML file pins the harness, profile, features, extensions, and LLM provider. Commit-free, gitignored, reproducible.</p> </li> <li> <p> Batteries included</p> <p>Profiles bundle Python, Go, Node, Playwright, a security audit toolkit, and more \u2014 resolved with dependency-aware feature layering.</p> </li> <li> <p> Curated extensions</p> <p>A per-harness extensions catalogue of MCP servers, skills, agents, and bundles like ECC \u2014 installed at build time.</p> </li> <li> <p> Host integrations</p> <p>Serena and claude-mem integrations wire host-side services into the container with automatic transport fallback \u2014 no manual Docker networking.</p> </li> <li> <p> Credentials, not secrets</p> <p>Your OAuth tokens, API keys, GPG agent, and AWS credentials are forwarded into the container \u2014 never baked into the image.</p> </li> </ul>"},{"location":"#how-it-works-in-30-seconds","title":"How it works in 30 seconds","text":"<pre><code>flowchart LR\n    A[vibrate] --&gt; B{.vb exists?}\n    B -- no --&gt; C[Wizard]\n    C --&gt; D[Save .vb]\n    B -- yes --&gt; E[Resolve spec]\n    D --&gt; E\n    E --&gt; F{Container?}\n    F -- running --&gt; G[docker exec]\n    F -- stopped --&gt; H[docker start + exec]\n    F -- none, image exists --&gt; I[docker run]\n    F -- none, no image --&gt; J[docker build] --&gt; I\n    G --&gt; K([Agent])\n    H --&gt; K\n    I --&gt; K</code></pre> <ol> <li>Resolve \u2014 <code>vibrate</code> reads the workspace <code>.vb</code>, overlays any CLI flags, and runs    the wizard for anything still unset.</li> <li>Build \u2014 it generates a deterministic Dockerfile for the    resolved spec and builds the image (only when one doesn't already exist).</li> <li>Launch \u2014 it runs or re-enters the container, mounting your workspace and    forwarding credentials, then execs the harness's own CLI.</li> </ol> <p>See What happens on build and What happens on start for the full step-by-step.</p>"},{"location":"#get-started","title":"Get started","text":"<ul> <li> <p> Install</p> <p>Build the binary, put it on <code>$PATH</code> with a <code>vb</code> alias, and register shell completion.</p> </li> <li> <p> Quick start</p> <p>Your first <code>vibrate</code> run, the wizard, and what the <code>.vb</code> file captures.</p> </li> <li> <p> Core concepts</p> <p>Workspace, variant, harness, profile, feature, extension, integration \u2014 the vocabulary.</p> </li> <li> <p> FAQ</p> <p>Quick answers to common questions.</p> </li> </ul> <p>Status</p> <p>Vibrator is a Go program. Docker integration is done by shelling out to the <code>docker</code> CLI (no SDK dependency), so any Docker-compatible runtime works \u2014 Docker Desktop, OrbStack, Colima, Rancher Desktop, Podman, or native. Released under the MIT license.</p>"},{"location":"#related-pages","title":"Related pages","text":"<ul> <li>About Vibrator \u2014 what it is, why it exists, and how it compares.</li> <li>Core concepts \u2014 the vocabulary behind the tool.</li> <li>FAQ \u2014 quick answers to common questions.</li> <li>Troubleshooting \u2014 concrete fixes for common failures.</li> </ul>"},{"location":"about/","title":"About Vibrator","text":""},{"location":"about/#what-vibrator-is","title":"What Vibrator is","text":"<p>Vibrator is a single-binary CLI that runs AI coding agents \u2014 Claude Code, Codex, OpenCode, and Pi \u2014 inside an isolated Docker container, one per workspace, with declarative profile and extension configuration captured in a <code>.vb</code> file.</p> <p>You install it with <code>just install</code>, invoke it as <code>vibrate</code> (alias <code>vb</code>), and run it from any project directory:</p> <pre><code>cd ~/my-project\nvibrate          # wizard fills the gaps, image builds once, agent drops you in\n</code></pre> <p>The first run launches a wizard, writes a <code>.vb</code> TOML pin, builds a tailored image once, and execs the agent's own CLI inside the container. Every subsequent <code>vibrate</code> in that workspace reuses the container and re-enters instantly.</p>"},{"location":"about/#why-it-exists","title":"Why it exists","text":"<p>AI coding agents want a clean, complete, reproducible toolchain \u2014 and they want to run commands, install packages, and write files freely. Running them directly on your host forces an uncomfortable trade-off:</p> <ul> <li>Host pollution. The agent installs language runtimes, CLIs, and global packages onto   your machine, mixing its needs with yours.</li> <li>No reproducibility. \"Works on my machine\" creeps back in. Two projects, or two   people, get subtly different environments depending on what's already installed.</li> <li>No isolation. An agent with shell access to your host can touch anything you can \u2014   not just the project you pointed it at.</li> </ul> <p>The usual fix is to hand-roll a Dockerfile per project, but that means owning and maintaining build files, credential forwarding, Docker networking, and runtime detection yourself \u2014 for every repo. Vibrator turns that recurring chore into one declarative <code>.vb</code> file and a single command.</p> <p>The core idea is per-workspace isolation</p> <p>Each project directory gets its own image and container, keyed by a content fingerprint. Two projects never share state, and the agent can't reach anything outside the mounted workspace.</p>"},{"location":"about/#design-philosophy","title":"Design philosophy","text":"<p>Vibrator's design follows a few deliberate principles. The Architecture reference lists every decision; the themes are:</p> <p>Isolation per workspace. A container is cheap and disposable; your host is not. Every workspace gets its own image and container so the agent has a full, writable toolchain that is sealed off from the rest of your machine. The workspace is mounted at the same absolute path it has on the host, so git, editors, debuggers, and stack traces all see identical paths inside and out.</p> <p>Declarative configuration. The <code>.vb</code> file is TOML precisely because it's meant to be read and hand-edited. It pins the harness, profile, features, extensions, and (for provider-agnostic harnesses) the LLM provider. Configuration is data, not a script.</p> <p>Determinism. A given resolved spec produces a byte-identical Dockerfile, and variant identity is a SHA-256 fingerprint over <code>harness;shell;features;extensions;user</code> (profile is deliberately excluded \u2014 it's just a label for a feature bundle). Identical resolved specs reuse one image, so builds are cacheable, inspectable, and reproducible.</p> <p>Batteries included, but opt-in. Profiles bundle whole toolchains \u2014 the default <code>full</code> profile carries Python, Go, Node, Playwright, the GitHub CLI, a Postgres client, and an audit toolkit \u2014 while extensions (MCP servers, skills, subagents, and bundles like ECC) and host integrations (Serena, claude-mem) stay opt-in. You start with a sensible bundle and add only what a given project needs.</p> <p>Runtime-agnostic, by shelling out. Vibrator talks to Docker by invoking the <code>docker</code> CLI rather than linking a Docker SDK. That keeps it free of import churn and lets it work with any Docker-compatible runtime \u2014 Docker Desktop, OrbStack, Colima, Rancher Desktop, Podman, or native Linux, all auto-detected.</p> <p>A small, typed surface. Harnesses are a built-in Go interface, not a plugin system, so adding one is a reviewed PR with a compile-time guarantee rather than runtime plugin loading. The extensions catalogue, by contrast, is Markdown with YAML frontmatter \u2014 so the catalogue is browsable without any tooling and \"add a plugin\" is a docs change.</p>"},{"location":"about/#how-it-compares","title":"How it compares","text":"<p>Vibrator occupies a specific niche. It's worth being clear about where it helps and where a different tool fits better.</p> Vibrator Agent on the host Hand-rolled Dockerfile Devcontainers Per-workspace isolation \u2705 Automatic \u274c None \u26a0\ufe0f If you build it \u2705 Yes Reproducible toolchain \u2705 Deterministic spec \u274c Host-dependent \u26a0\ufe0f You maintain it \u2705 Yes Zero per-repo Docker files \u2705 One <code>.vb</code> \u2014 \u274c Per-repo Dockerfile \u26a0\ufe0f <code>.devcontainer/</code> per repo Built for AI agents \u2705 Harness-aware \u26a0\ufe0f Manual \u274c DIY \u274c Editor-oriented Credential forwarding \u2705 Built-in \u2014 \u274c DIY \u26a0\ufe0f Partial Runtime auto-detection \u2705 Built-in \u2014 \u274c DIY \u26a0\ufe0f Editor-tied <p>vs. running agents directly on the host. The host approach has the least overhead and the most risk: no build step, but no isolation, no reproducibility, and the agent shares your machine's full surface. Vibrator trades a one-time per-spec build for a sealed, repeatable environment.</p> <p>vs. a hand-rolled Dockerfile per project. This gives you isolation, but you own every moving part \u2014 the build file, credential mounts, Docker networking, runtime quirks \u2014 and you repeat that work for each repo. Vibrator generates the Dockerfile from a declarative spec and handles the plumbing. If you need a bespoke, long-lived image with logic Vibrator doesn't model, a hand-rolled Dockerfile remains the more flexible choice.</p> <p>vs. devcontainers. Devcontainers solve a related problem \u2014 reproducible dev environments \u2014 but are oriented around an editor/IDE session and a per-repo <code>.devcontainer/</code> config. Vibrator is CLI-first and agent-aware: it knows about harnesses, forwards host credentials, wires up host integrations, and auto-detects the Docker runtime, none of which a devcontainer does out of the box. If your goal is an IDE remote-development session rather than running a coding agent, devcontainers are the better fit.</p> <p>Docker-in-Docker is powerful and dangerous</p> <p>The agent can run <code>docker</code> itself via <code>--dind</code>, but that grants host-root-equivalent access. Only enable it for trusted workspaces.</p>"},{"location":"about/#project-status","title":"Project status","text":"<p>Vibrator began as a bash implementation; the current Go rewrite (Go \u2265 1.26, a single static binary) is feature-complete. It's released under the MIT license from github.com/wlame/vibrator.</p> <p>Releases are tag-driven: pushing a version tag runs goreleaser, which builds cross-platform binaries with checksums and auto-generates the changelog.</p> <p>If you're coming from the old bash version, <code>vibrate migrate-pin</code> converts a legacy <code>.vb.env</code> file into the current TOML <code>.vb</code> format.</p> <p>New here?</p> <p>Start with the Getting started guide, then read Core concepts for the vocabulary.</p>"},{"location":"about/#related-pages","title":"Related pages","text":"<ul> <li>Getting started \u2014 install, first run, and the mental model.</li> <li>Core concepts \u2014 workspace, variant, harness, profile,   feature, extension, integration.</li> <li>Architecture \u2014 the internal module map and every design   decision.</li> <li>Troubleshooting \u2014 concrete fixes for the failures you're likely to hit.</li> </ul>"},{"location":"faq/","title":"FAQ","text":"<p>Quick answers to common questions. Each links to the fuller explanation.</p>"},{"location":"faq/#getting-started","title":"Getting started","text":"What is Vibrator, in one sentence? <p>A single Go binary (<code>vibrate</code>) that builds a tailored Docker image per project and runs an AI coding agent \u2014 one of four harnesses (Claude Code, Codex, OpenCode, or Pi) \u2014 inside it, configured by a <code>.vb</code> file. See the overview.</p> What's the difference between <code>vibrate</code> and <code>vb</code>? <p>Nothing \u2014 <code>vb</code> is a symlink to <code>vibrate</code>, installed alongside it. Use either.</p> Do I need Docker installed? <p>To run a workspace, yes \u2014 any Docker-compatible runtime (Docker Desktop, OrbStack, Colima, Rancher Desktop, Podman, native). To merely build the binary, no. See Installation.</p> How do I start? <p><code>cd</code> into a project and run <code>vibrate</code>. The wizard asks a few questions, the image builds, and the agent drops you in. See the Quick start.</p>"},{"location":"faq/#daily-use","title":"Daily use","text":"Why was the first run slow and the next one instant? <p>The first run does a <code>docker build</code> for your variant. Once an image exists, <code>vibrate</code> reuses the container (or starts it) and just <code>exec</code>s in. See Lifecycle.</p> How do I change my setup after the first run? <p><code>vibrate reconfigure</code> re-runs the wizard and rebuilds, preserving your credentials and <code>[env]</code>. Or edit <code>.vb</code> by hand.</p> How do I get a shell instead of the agent? <p><code>vibrate shell</code>. Same build/run logic, it just execs your shell at the end.</p> How do I update the agent without rebuilding everything? <p><code>vibrate update</code> upgrades just the harness CLI in place. For a full rebuild from the Dockerfile, use <code>vibrate --rebuild</code>.</p> How do I skip the wizard? <p>Pass everything as flags and add <code>--no-wizard</code>: <code>vibrate --no-wizard --harness=claude-code --profile=full</code>. See the launch flags.</p> How do I try a config once without saving it? <p>Add <code>--no-save</code> \u2014 the resolved config isn't written to <code>.vb</code>.</p>"},{"location":"faq/#configuration","title":"Configuration","text":"Where does Vibrator look for <code>.vb</code>? <p>It walks up from your current directory to the git root (or filesystem root); the first <code>.vb</code> wins, and its directory becomes the workspace root. See The <code>.vb</code> file.</p> Is <code>.vb</code> safe to commit? <p>No \u2014 it can hold plaintext credentials (minted keys, pasted API keys). It's written <code>0600</code> and auto-added to <code>.gitignore</code>. Treat it like a dotenv file.</p> What's a profile vs. a feature? <p>A profile is a named bundle of features; features are the build-time layers (Python, Node, Playwright, \u2026). Tune a profile with <code>--with</code>/<code>--no</code>. See Profiles &amp; features.</p> I removed a feature with <code>--no</code> but it's still there. Why? <p>Something else needs it. <code>--no=node --with=playwright</code> keeps <code>node</code> because Playwright depends on it. Drop the dependent too. See the resolution subtlety.</p> Why do <code>--profile=full</code> and the default produce the same image? <p>Profile is just a label for a feature bundle; it's deliberately excluded from the fingerprint. Identical resolved features \u2192 identical image.</p>"},{"location":"faq/#authentication","title":"Authentication","text":"The banner says <code>auth: not configured</code>. What now? <p>Export the relevant key on the host before running (e.g. <code>export ANTHROPIC_API_KEY=...</code>), or for Claude Code run <code>vibrate --login</code> for the browser OAuth flow. See Authentication.</p> What does <code>vibrate --login</code> do? <p>Runs <code>claude auth login</code> in the container, opens the OAuth URL in your host browser, and writes the auth back to your host so future runs are pre-authenticated. See <code>--login</code>.</p> Can I sign git commits inside the container? <p>Yes \u2014 if you've configured a gpg-agent <code>extra-socket</code>, it's auto-forwarded so <code>git commit -S</code> uses your host key without it leaving the host. See GPG forwarding.</p> Are my AWS credentials available? <p>If <code>~/.aws</code> exists on the host, it's mounted read-only. See AWS credentials.</p>"},{"location":"faq/#models-extensions","title":"Models &amp; extensions","text":"Can I use a local model (Ollama / LM Studio)? <p>Yes, for Codex/OpenCode/Pi. Pick it in the wizard; Vibrator ensures the provider is running and pulls the model before launch. See LLM providers.</p> How do I add an MCP server / skill / plugin? <p>Select an extension by ID: <code>--extensions=context7,github-mcp</code>. Browse with <code>vibrate extensions list &lt;harness&gt;</code>.</p> What is ECC? <p>Everything Claude Code \u2014 an opt-in bundle of agents, skills, rules, and hooks, shipped as <code>ecc-*</code> extensions. Try <code>--extensions=ecc-developer</code>.</p>"},{"location":"faq/#integrations","title":"Integrations","text":"What are integrations vs. extensions? <p>Extensions are installed into the image. Integrations connect the container to a host-side service (Serena, claude-mem) with automatic transport fallback.</p> What's the difference between <code>auto</code>, <code>host</code>, <code>local</code>, and <code>off</code>? <p>They're hosting modes for an integration: probe-then-fall-back, host-only, container-only, or disabled.</p>"},{"location":"faq/#troubleshooting","title":"Troubleshooting","text":"<code>vibrate</code> can't find my Docker socket. <p>Run <code>vibrate runtime detect</code>. Override with <code>--docker-socket=...</code> or <code>VIBRATOR_DOCKER_SOCKET</code>. See Runtime detection.</p> I get <code>node: not found</code> (or python) errors from hooks. <p>A hook in your <code>~/.claude/settings.json</code> shells out to a tool that isn't installed in the image \u2014 common with the <code>minimal</code> profile. <code>vibrate</code> detects this at launch and offers to install the tool (adds <code>--with=node</code>, rebuilds) or disable those hooks, and the container also auto-skips any hook whose tool isn't on <code>PATH</code>. See Missing-tool hooks.</p> How do I see what the container setup is doing? <p>Set <code>VIBRATOR_VERBOSE=1</code> to print <code>[vibrator] ...</code> diagnostics from the entrypoint and <code>claude-exec</code>. Silence the banner with <code>VIBRATOR_NO_BANNER=1</code>.</p> How do I clean up old images and containers? <p><code>vibrate variants list</code> to see them, <code>vibrate variants prune</code> to remove stopped containers and unused images.</p> The container can't reach a service on my host. <p>Use <code>host.docker.internal</code> (Vibrator adds the host-gateway mapping automatically). For host integration servers, check they bind <code>0.0.0.0</code> and your hosting mode.</p> Can the agent run <code>docker</code> itself? <p>Yes, with <code>--dind</code> \u2014 but it grants host-root-equivalent access, so only use it for trusted workspaces.</p>"},{"location":"faq/#still-stuck","title":"Still stuck?","text":"<ul> <li>Work through Troubleshooting \u2014 concrete fixes grouped by symptom.</li> <li>Read the Lifecycle pages \u2014 most \"why did it do that?\" answers live   there.</li> <li>Open an issue on GitHub.</li> </ul>"},{"location":"faq/#related-pages","title":"Related pages","text":"<ul> <li>Troubleshooting \u2014 symptom-by-symptom fixes.</li> <li>Core concepts \u2014 the vocabulary the answers assume.</li> <li>Quick start \u2014 the first-run walkthrough.</li> </ul>"},{"location":"troubleshooting/","title":"Troubleshooting","text":"<p>Concrete fixes for the failures you're most likely to hit, grouped by symptom. Each entry covers what you see, why it happens, and the fix.</p> <p>Turn on diagnostics first</p> <p>Set <code>VIBRATOR_VERBOSE=1</code> to print <code>[vibrator] ...</code> diagnostics from the entrypoint and the <code>claude-exec</code> wrapper. Most \"why did it do that?\" questions answer themselves once verbose output is on. Silence the launch banner with <code>VIBRATOR_NO_BANNER=1</code>.</p>"},{"location":"troubleshooting/#no-docker-runtime-found","title":"No Docker runtime found","text":"<p>What you see. <code>vibrate</code> exits before building anything, complaining it can't find a Docker socket. <code>vibrate runtime detect</code> exits non-zero and reports no reachable socket.</p> <p>Why it happens. Vibrator talks to Docker by shelling out to the <code>docker</code> CLI, so it needs a reachable Unix socket. If no runtime is running \u2014 and none of the override mechanisms point at a live socket \u2014 detection fails.</p> <p>Fix. Start a runtime, or tell Vibrator where the socket is:</p> <pre><code># 1. Start a runtime (Docker Desktop, OrbStack, Colima, Rancher Desktop, \u2026) and recheck:\nvibrate runtime detect\n\n# 2. Or point Vibrator at a specific socket, one-off:\nvibrate runtime detect --docker-socket=/custom/path/docker.sock\n\n# 3. Or persist it for the session:\nexport VIBRATOR_DOCKER_SOCKET=/custom/path/docker.sock\n</code></pre> <p><code>$DOCKER_HOST</code> (when it's a <code>unix://</code> socket) is also honored. See Runtime detection for the full resolution order.</p>"},{"location":"troubleshooting/#docker-in-docker-fails-on-colima-or-rancher-desktop","title":"Docker-in-Docker fails on Colima or Rancher Desktop","text":"<p>What you see. Everything works until you add <code>--dind</code>; then <code>docker</code> commands inside the container can't reach the daemon, specifically on Colima or Rancher Desktop.</p> <p>Why it happens. These are VM-based runtimes. The macOS-side socket is a proxy that only listens on the host and is unreachable from inside the VM (and therefore from inside a container). Vibrator works around this by mounting the daemon's own <code>/var/run/docker.sock</code> \u2014 which the daemon, running inside the VM, resolves to its real socket \u2014 with a sudo wrapper from the <code>docker-cli</code> feature handling group access.</p> <p>Fix. If docker-in-docker still fails on these runtimes, this proxy/socket mismatch is almost always the cause. Confirm the runtime and socket Vibrator is using, then retry:</p> <pre><code>vibrate runtime detect            # confirm runtime = colima / rancher-desktop\nvibrate runtime detect --colima-profile=work   # if you use a non-default Colima profile\n</code></pre> <p><code>--dind</code> grants host-root-equivalent access</p> <p>Mounting the Docker socket into a container lets the agent control the host daemon \u2014 effectively root on the host. Only use <code>--dind</code> for trusted workspaces.</p>"},{"location":"troubleshooting/#serena-host-server-unreachable","title":"Serena host server unreachable","text":"<p>What you see. With <code>[integrations] serena = \"host\"</code> in <code>.vb</code>, a pre-launch check warns that the Serena server is unreachable. In <code>auto</code> mode you instead notice a brief delay each session (a <code>uvx</code> cold-start).</p> <p>Why it happens. In <code>host</code> mode Vibrator expects a persistent host server bound on <code>0.0.0.0:8765</code>; if it isn't bound there, the pre-launch check warns and \u2014 because there's no fallback in <code>host</code> mode \u2014 Serena is simply absent. In <code>auto</code> mode there's no host server to reach, so <code>claude-exec</code> spawns Serena locally via <code>uvx</code>, paying a cold-start on each new session.</p> <p>Fix. Start the host server and make sure it binds the right interface:</p> <pre><code>vibrate integrations serena       # start the host server (process or docker runtime)\n</code></pre> <p>If you run it yourself, bind <code>0.0.0.0</code> (not <code>127.0.0.1</code>) on port <code>8765</code>:</p> <pre><code>uvx --from git+https://github.com/oraios/serena \\\n    serena start-mcp-server --transport=streamable-http \\\n    --host=0.0.0.0 --port=8765\n</code></pre> <p>Override the port with <code>SERENA_PORT</code> on the host before starting both the server and Vibrator. A failing check never blocks the launch \u2014 see Serena.</p>"},{"location":"troubleshooting/#claude-mem-token-rejected","title":"claude-mem token rejected","text":"<p>What you see. On launch, the claude-mem entrypoint auth-probe fails. A <code>401</code>/<code>403</code> means the token was rejected; a <code>400</code>/<code>422</code> means the server is fine but the token is bad. A connection refused means the server isn't running (and is skipped silently).</p> <p>Why it happens. Only a bearer token crosses into the container \u2014 the Postgres DSN stays host-only. If that token is stale or wrong, the probe rejects it. If the server isn't up at all, there's nothing to probe.</p> <p>Fix. Re-mint the token:</p> <pre><code>vibrate prereqs bootstrap claude-mem-server-beta --force\n</code></pre> <p>For connection refused, start the claude-mem server first (it's skipped silently when down, so the agent still launches). If your DSN uses <code>localhost</code>, note it's auto-rewritten to <code>host.docker.internal</code> for you. See claude-mem.</p>"},{"location":"troubleshooting/#a-hook-references-a-tool-that-isnt-installed","title":"A hook references a tool that isn't installed","text":"<p>What you see. A hook in your <code>~/.claude/settings.json</code> shells out to a tool \u2014 <code>node</code>, <code>python</code>, etc. \u2014 that isn't in the resolved feature set. You may see <code>node: not found</code> (or similar) from a hook.</p> <p>Why it happens. A hook assumes a tool that the chosen profile/features don't include \u2014 common with the <code>minimal</code> profile. Vibrator has a two-layer defense: at launch it prompts to add the missing feature (and rebuild); at runtime the entrypoint guard runs <code>command -v</code> and silences any hook whose tool isn't on <code>PATH</code>.</p> <p>Fix. Take the launch-time prompt to add the feature, or add it yourself and rebuild:</p> <pre><code>vibrate --with=node --rebuild\n</code></pre> <p>To keep a hook intentionally tool-less, acknowledge it under <code>[hooks] acknowledged_missing</code> in <code>.vb</code>. See Missing-tool hooks.</p>"},{"location":"troubleshooting/#-no-wizard-fails-on-a-required-field","title":"<code>--no-wizard</code> fails on a required field","text":"<p>What you see. With <code>--no-wizard</code>, Vibrator errors out that a required field (commonly the harness) is unset, instead of prompting.</p> <p>Why it happens. <code>--no-wizard</code> makes missing required fields a hard error rather than a prompt. The harness \u2014 and any other required field \u2014 must come from flags or an existing <code>.vb</code>.</p> <p>Fix. Supply the required flags, or drop <code>--no-wizard</code> to let the wizard fill the gaps:</p> <pre><code>vibrate --no-wizard --harness=claude-code --profile=full --shell=zsh\n</code></pre>"},{"location":"troubleshooting/#stale-image-or-container-after-a-config-change","title":"Stale image or container after a config change","text":"<p>What you see. You changed <code>.vb</code> (or passed new flags) but the container still behaves as before \u2014 old tools, old extensions.</p> <p>Why it happens. Vibrator reuses an existing container when the resolved spec matches. Some changes don't force a fresh build on their own, so the old image/container lingers.</p> <p>Fix. Force a rebuild, or re-run the wizard and rebuild:</p> <pre><code>vibrate --rebuild        # force a fresh build from the Dockerfile\nvibrate reconfigure      # re-run the wizard, then rebuild (preserves credentials + [env])\n</code></pre> <p>To clean up afterwards, remove stopped containers and unused images:</p> <pre><code>vibrate variants prune\n</code></pre> <p>Profile changes alone won't change the image</p> <p>Profile is excluded from the variant fingerprint \u2014 only the resolved feature set is hashed. <code>--profile=full</code> and the implicit default build the same image. If you expected a new image from a profile swap that resolves to the same features, that's why.</p>"},{"location":"troubleshooting/#first-build-is-very-slow-claude-code","title":"First build is very slow (claude-code)","text":"<p>What you see. The first <code>vibrate</code> run for a Claude Code workspace takes a long time to build.</p> <p>Why it happens. The default profile is <code>full</code> (~2 GB) \u2014 everything except aider. That's a lot to install on the first build.</p> <p>Fix. Pick a smaller profile for a faster first build:</p> <pre><code>vibrate --profile=minimal      # ~150 MB, base toolkit only\nvibrate --profile=backend      # ~600 MB, python/go/gh/postgres-client/ralphex\n</code></pre> <p>Subsequent runs reuse the image and are instant. See Profiles.</p>"},{"location":"troubleshooting/#diagnostic-commands","title":"Diagnostic commands","text":"<p>When you don't have a specific error, these commands tell you what Vibrator sees:</p> Command Purpose <code>vibrate runtime detect</code> Show the detected Docker runtime, socket path, and how it was found. <code>vibrate hostprobe</code> Scan the host for installed harness plugins/extensions Vibrator can reuse. <code>vibrate prereqs status</code> Report the state of host-side prerequisites (e.g. claude-mem) for this workspace. <code>vibrate variants list</code> List the images and containers Vibrator has built, with their fingerprints."},{"location":"troubleshooting/#still-stuck","title":"Still stuck?","text":"<p>Open an issue and include the output of <code>vibrate runtime detect</code>, the relevant <code>VIBRATOR_VERBOSE=1</code> output, your <code>.vb</code> (redact any credentials), and the steps to reproduce.</p>"},{"location":"troubleshooting/#related-pages","title":"Related pages","text":"<ul> <li>FAQ \u2014 quick answers to common questions.</li> <li>Runtime detection \u2014 how the Docker socket is resolved.</li> <li>Integrations \u2014 hosting modes, readiness checks, and transport   fallback for Serena and claude-mem.</li> <li>Docker-in-Docker \u2014 <code>--dind</code>, socket mounting, and its risks.</li> </ul>"},{"location":"getting-started/","title":"Getting started","text":"<p>New to Vibrator? Start here.</p> <ul> <li> <p> Installation</p> <p>Requirements, the <code>just install</code> flow, manual installation, and shell completion.</p> </li> <li> <p> Quick start</p> <p>Your first run, the setup wizard, skipping the wizard with flags, and re-runs.</p> </li> <li> <p> Core concepts</p> <p>The mental model: workspaces, variants, harnesses, profiles, features, extensions, and integrations.</p> </li> </ul>"},{"location":"getting-started/#the-shortest-possible-path","title":"The shortest possible path","text":"<pre><code>just install            # build + install the binary, alias, and completion\ncd ~/my-project\nvibrate                 # answer the wizard once; the agent drops you in\n</code></pre> <p>From then on, <code>vibrate</code> in that directory reuses the container and re-enters instantly. Everything else in this documentation explains how to shape what gets built and how the container behaves at runtime.</p>"},{"location":"getting-started/#related-pages","title":"Related pages","text":"<ul> <li>About Vibrator \u2014 what it is and why it exists.</li> <li>Core concepts \u2014 workspaces, variants, harnesses, and the rest.</li> <li>The <code>.vb</code> file \u2014 the per-workspace pin.</li> </ul>"},{"location":"getting-started/concepts/","title":"Core concepts","text":"<p>Vibrator has a small vocabulary. Once these seven terms click, the rest of the tool is just detail.</p> <pre><code>flowchart TD\n    WS[Workspace&lt;br/&gt;&lt;i&gt;a project directory&lt;/i&gt;] --&gt; PIN[.vb pin&lt;br/&gt;&lt;i&gt;saved choices&lt;/i&gt;]\n    PIN --&gt; H[Harness]\n    PIN --&gt; P[Profile]\n    PIN --&gt; E[Extensions]\n    P --&gt; F[Features]\n    H --&gt; F\n    E --&gt; F\n    H &amp; P &amp; F &amp; E --&gt; V[Variant&lt;br/&gt;&lt;i&gt;fingerprint&lt;/i&gt;]\n    V --&gt; IMG[Image]\n    IMG --&gt; CT[Container]\n    CT --&gt; INT[Integrations]</code></pre>"},{"location":"getting-started/concepts/#workspace","title":"Workspace","text":"<p>A workspace is a project directory. Vibrator finds it by walking up from your current directory to the git root (or, if you're not in a repo, the filesystem root) looking for a <code>.vb</code> file. The workspace is mounted into the container at the same absolute path it has on your host \u2014 so <code>pwd</code>, stack traces, and error messages all match your host muscle memory.</p>"},{"location":"getting-started/concepts/#harness","title":"Harness","text":"<p>A harness is the AI coding agent that runs inside the container. Vibrator ships four built-ins:</p> ID Agent <code>claude-code</code> Claude Code (Anthropic) <code>codex</code> OpenAI Codex <code>opencode</code> OpenCode (SST) <code>pi</code> Pi (<code>pi-coding-agent</code>) <p>Each harness declares how to install itself, which auth env vars it expects, which base features it requires, and the command <code>vibrate</code> execs to launch it. Bare <code>vibrate</code> launches the harness's own CLI; <code>vibrate shell</code> launches your shell instead.</p>"},{"location":"getting-started/concepts/#profile","title":"Profile","text":"<p>A profile is a named bundle of features \u2014 a starting point you tune with <code>--with</code>/<code>--no</code>. There are four, ordered smallest to largest:</p> Profile Roughly <code>minimal</code> Base toolkit only (jq, rg, fd, vim, git, \u2026) <code>backend</code> + Python, Go, GitHub CLI, Postgres client <code>frontend</code> + Node, Bun, Playwright/Chromium <code>full</code> (default) Backend + frontend + audit toolkit + Codex CLI <p>See the Profiles reference for the exact feature lists.</p>"},{"location":"getting-started/concepts/#feature","title":"Feature","text":"<p>A feature is a build-time capability layer \u2014 a language toolchain (<code>python</code>, <code>go</code>, <code>node</code>), a browser runtime (<code>playwright</code>), a CLI (<code>gh</code>, <code>postgres-client</code>), or a tool bundle (<code>audit-toolkit</code>). Features carry their own Dockerfile fragments and declare dependencies on each other. The resolver:</p> <ol> <li>Starts with the profile's features.</li> <li>Adds features required by the harness and any selected extensions.</li> <li>Applies your <code>--with</code> (add) and <code>--no</code> (remove) deltas.</li> <li>Auto-enables transitive dependencies (selecting <code>playwright</code> pulls in <code>node</code>).</li> </ol> <p>See the Features reference for the full catalogue.</p>"},{"location":"getting-started/concepts/#extension","title":"Extension","text":"<p>An extension is something installed on top of the harness \u2014 an MCP server, a skill, a subagent, or a whole bundle like ECC. The catalogue is a set of Markdown files with YAML frontmatter, one per item, scoped per harness under <code>extensions/&lt;harness&gt;/</code>. You pick them by ID (<code>--extensions=context7,ecc-developer</code> or in the wizard) and they're installed at image-build time. See Extensions.</p>"},{"location":"getting-started/concepts/#variant","title":"Variant","text":"<p>A variant is a unique <code>(harness, shell, features, extensions, user)</code> combination, identified by an 8-character SHA-256 fingerprint. The fingerprint is what makes isolation work:</p> <ul> <li>It determines the image tag   (<code>vb-&lt;harness&gt;-&lt;profile&gt;-&lt;user&gt;-&lt;fp8&gt;:latest</code>).</li> <li>It distinguishes container names, so   Claude Code \"backend\" and Codex \"backend\" in the same project get two containers.</li> </ul> <p>Profile is deliberately not part of the fingerprint \u2014 it's just a label for a feature bundle, so <code>--profile=full</code> and the implicit default resolve identically. See Naming &amp; labels.</p>"},{"location":"getting-started/concepts/#integration","title":"Integration","text":"<p>An integration connects the container to a host-side service \u2014 for example Serena (a code-aware MCP server) or claude-mem (persistent agent memory). Integrations wire themselves into the container via a build-time manifest and an on-every-entry probe that picks the right transport (HTTP to the host server, or a container-local fallback). You control the policy per workspace with a hosting mode: <code>auto</code>, <code>host</code>, <code>local</code>, or <code>off</code>.</p>"},{"location":"getting-started/concepts/#how-they-fit-together","title":"How they fit together","text":"<p>When you run <code>vibrate</code>:</p> <ol> <li>The workspace is located and its <code>.vb</code> pin loaded (or the    wizard fills it in).</li> <li>The pin's harness + profile + extensions resolve into a final feature    set.</li> <li>That combination is the variant, hashed into a fingerprint that names the    image and container.</li> <li>The image is built if missing; the container is run or re-entered.</li> <li>On entry, integrations and credentials are wired in.</li> </ol> <p>The next two sections go deep on steps 3\u20135: What happens on build and What happens on start.</p>"},{"location":"getting-started/concepts/#related-pages","title":"Related pages","text":"<ul> <li>Naming &amp; labels \u2014 how the variant fingerprint names things.</li> <li>Profiles &amp; features \u2014 how profiles resolve into features.</li> <li>Architecture \u2014 the internal module map behind these terms.</li> </ul>"},{"location":"getting-started/installation/","title":"Installation","text":"<p>Vibrator is a single Go binary. You install it by building from source with the project's <code>just</code> task runner, which also places a short <code>vb</code> alias on your <code>$PATH</code> and registers shell completion for your shell.</p>"},{"location":"getting-started/installation/#requirements","title":"Requirements","text":"Tool Why Notes Go \u2265 1.26 Compiles the binary https://go.dev/dl/ <code>just</code> Task runner used for install/build See installing <code>just</code> Docker Runs the containers at runtime Any Docker-compatible runtime \u2014 see Runtime detection <p>Docker isn't needed to build the binary</p> <p>You only need Docker to actually run a workspace (and to run the integration test suite). Building and installing <code>vibrate</code> itself needs only Go and <code>just</code>.</p>"},{"location":"getting-started/installation/#install-with-just","title":"Install with <code>just</code>","text":"<p>From the repository root:</p> <pre><code># Install: builds the binary, places it on $PATH with a short `vb` alias,\n# and registers shell completion for $SHELL (bash, zsh, or fish).\njust install                       # \u2192 /usr/local/bin (uses sudo if needed)\n\n# Or install user-locally, no sudo:\njust install ~/.local/bin\n</code></pre> <p>This recipe:</p> <ol> <li>Builds <code>./build/vibrate</code>.</li> <li>Copies it to the destination directory (default <code>/usr/local/bin</code>).</li> <li>Creates a <code>vb</code> symlink alongside it \u2014 <code>vb</code> and <code>vibrate</code> are interchangeable.</li> <li>Generates and installs shell completion for your current <code>$SHELL</code>.</li> </ol> <p>To remove everything again, use the symmetric recipe with the same argument:</p> <pre><code>just uninstall                     # removes the binary, alias, and completion\njust uninstall ~/.local/bin        # if you installed user-locally\n</code></pre>"},{"location":"getting-started/installation/#installing-just","title":"Installing <code>just</code>","text":"<p><code>just</code> is a small Rust-based command runner \u2014 think Make, but simpler. Install it once:</p> macOS / LinuxDebian / Ubuntu / Fedora / ArchWindows <pre><code>brew install just                       # Homebrew\ncargo install just                      # any Rust toolchain\ncurl -fsSL https://just.systems/install.sh | bash -s -- --to ~/.local/bin\n</code></pre> <pre><code>sudo apt install just                   # Debian 13+ / Ubuntu 24.10+\nsudo dnf install just                   # Fedora\nsudo pacman -S just                     # Arch\n</code></pre> <pre><code>winget install --id Casey.Just --exact  # winget\nscoop install just                      # Scoop\n</code></pre> <p>Verify with <code>just --version</code>.</p>"},{"location":"getting-started/installation/#verify-the-install","title":"Verify the install","text":"<pre><code>vibrate --version        # prints the embedded version (or \"dev\")\nvibrate --help           # lists every subcommand\nvb runtime detect        # confirms vibrate can find your Docker socket\n</code></pre> <p>If <code>vibrate runtime detect</code> prints a runtime and socket path, you're ready to go. If it can't find a socket, see Runtime detection.</p>"},{"location":"getting-started/installation/#building-manually","title":"Building manually","text":"<p>If you'd rather not use <code>just</code>, you can build and install by hand:</p> <pre><code># Build with an embedded version string.\nVERSION=0.2.0 just build        # \u2192 ./build/vibrate\n# or with plain Go:\ngo build -o build/vibrate ./cmd/vibrate\n\n# Put it on your PATH yourself.\ncp build/vibrate /usr/local/bin/vibrate\nln -sf /usr/local/bin/vibrate /usr/local/bin/vb\n</code></pre> <p>Shell completion can be generated on demand \u2014 <code>vibrate</code> is built with Cobra, which provides a <code>completion</code> command:</p> <pre><code>vibrate completion zsh  &gt; \"${fpath[1]}/_vibrate\"\nvibrate completion bash &gt; /etc/bash_completion.d/vibrate\nvibrate completion fish &gt; ~/.config/fish/completions/vibrate.fish\n</code></pre>"},{"location":"getting-started/installation/#next-steps","title":"Next steps","text":"<ul> <li>Quick start \u2014 your first <code>vibrate</code> run.</li> <li>Core concepts \u2014 the mental model.</li> </ul>"},{"location":"getting-started/installation/#related-pages","title":"Related pages","text":"<ul> <li>Quick start \u2014 the first-run wizard walkthrough.</li> <li>Runtime detection \u2014 how <code>vibrate</code> finds your Docker socket.</li> <li><code>vibrate runtime detect</code> \u2014 verify the install can reach Docker.</li> </ul>"},{"location":"getting-started/quickstart/","title":"Quick start","text":"<p>This page walks through your first <code>vibrate</code> run end to end: the wizard, what gets written to <code>.vb</code>, how to skip the wizard entirely, and how re-runs behave.</p>"},{"location":"getting-started/quickstart/#1-run-vibrate-in-a-project","title":"1. Run <code>vibrate</code> in a project","text":"<pre><code>cd ~/my-project\nvibrate          # or `vb` \u2014 same binary\n</code></pre> <p>With no <code>.vb</code> file present, <code>vibrate</code> launches the setup wizard.</p>"},{"location":"getting-started/quickstart/#2-answer-the-wizard","title":"2. Answer the wizard","text":"<p>The wizard asks only for what it needs, in order. Steps you've already answered via flags or an existing <code>.vb</code> are skipped automatically:</p> Step Choices Default Harness <code>claude-code</code>, <code>codex</code>, <code>opencode</code>, <code>pi</code> \u2014 (required) Profile <code>minimal</code>, <code>backend</code>, <code>frontend</code>, <code>full</code> <code>full</code> Shell <code>bash</code>, <code>zsh</code>, <code>fish</code> <code>zsh</code> LLM provider shown only for provider-agnostic harnesses (Codex, OpenCode, Pi) \u2014 Extensions multi-select from the harness's catalogue per-entry defaults Serena hosting <code>auto</code>, <code>host</code>, <code>local</code>, <code>off</code> (Claude Code) <code>auto</code> <p>When you finish, the wizard prints a summary and the equivalent command \u2014 the exact flags that reproduce your choices without the wizard next time. Copy it into a script if you like.</p>"},{"location":"getting-started/quickstart/#3-the-image-builds-and-the-agent-starts","title":"3. The image builds and the agent starts","text":"<p><code>vibrate</code> then:</p> <ol> <li>Writes your choices to a <code>.vb</code> file and adds it to    <code>.gitignore</code> (if one exists).</li> <li>Generates a Dockerfile for your spec and builds the image.    This is the slow step \u2014 once per spec.</li> <li>Creates the container, mounts your workspace, forwards your    credentials, and execs the harness's CLI.</li> </ol> <p>You land inside the container, in your project directory, with a banner showing the harness, auth status, profile, and tools available.</p> <pre><code>+--------------------------------------------------------------+\n|  Vibrator \u2014 AI coding agent sandbox                          |\n+--------------------------------------------------------------+\n\nclaude:       1.x.x\nauth:         OAuth token\nprofile:      full\ntools:        python,go,node,playwright,gh,postgres-client,...\n\nworkspace:    /Users/you/my-project\n</code></pre>"},{"location":"getting-started/quickstart/#4-subsequent-runs-are-instant","title":"4. Subsequent runs are instant","text":"<p>Run <code>vibrate</code> again in the same workspace and it skips the wizard and the build \u2014 it reuses the existing container (or starts it if stopped) and re-enters:</p> <pre><code>vibrate          # reuse container, jump straight in\n</code></pre> <p>When you exit the agent, the container stays around for next time. It's recreated only when your spec changes or you ask for a rebuild.</p>"},{"location":"getting-started/quickstart/#skip-the-wizard-entirely","title":"Skip the wizard entirely","text":"<p>Pass everything as flags. <code>--no-wizard</code> makes missing required fields a hard error instead of prompting:</p> <pre><code>vibrate --no-wizard \\\n        --harness=claude-code \\\n        --profile=full \\\n        --shell=zsh\n</code></pre> <p>Add and remove individual features on top of the profile, and select extensions:</p> <pre><code>vibrate --harness=claude-code \\\n        --profile=backend \\\n        --with=node,playwright \\\n        --no=ralphex \\\n        --extensions=context7,ecc-developer\n</code></pre> <p>To try a combination once without writing it to <code>.vb</code>, add <code>--no-save</code>.</p>"},{"location":"getting-started/quickstart/#get-a-shell-instead-of-the-agent","title":"Get a shell instead of the agent","text":"<p>Everything about <code>vibrate run</code> applies to <code>vibrate shell</code> \u2014 except the final step launches your shell instead of the harness CLI. Handy for debugging, inspecting config, or running one-off commands:</p> <pre><code>vibrate shell\n</code></pre>"},{"location":"getting-started/quickstart/#common-follow-ups","title":"Common follow-ups","text":"<ul> <li>Change your setup later \u2192 <code>vibrate reconfigure</code>   re-runs the wizard and rebuilds, preserving your credentials.</li> <li>Update the agent CLI \u2192 <code>vibrate update</code> upgrades   the harness in place without a full rebuild.</li> <li>Authenticate \u2192 if the banner says <code>auth: not configured</code>, see   Authentication (including <code>vibrate --login</code>).</li> <li>List what you've built \u2192 <code>vibrate variants list</code>.</li> </ul>"},{"location":"getting-started/quickstart/#next-steps","title":"Next steps","text":"<ul> <li>Core concepts \u2014 understand workspaces, variants, and the build pipeline.</li> <li>The <code>.vb</code> file \u2014 what gets saved and how to hand-edit it.</li> </ul>"},{"location":"getting-started/quickstart/#related-pages","title":"Related pages","text":"<ul> <li>The wizard \u2014 every step of the setup form.</li> <li>Profiles &amp; features \u2014 shaping what gets built.</li> <li>Troubleshooting \u2014 fixes if the first run misbehaves.</li> </ul>"},{"location":"guides/","title":"Guides","text":"<p>Task-oriented guides to using Vibrator, each from a different angle. If you're looking for exhaustive tables, see the Reference section instead.</p> <ul> <li> <p> The .vb file</p> <p>The per-workspace pin: what it captures, how it's found, and how to hand-edit it.</p> </li> <li> <p> Harnesses</p> <p>Claude Code, Codex, OpenCode, Pi \u2014 what each installs, expects, and supports.</p> </li> <li> <p> Profiles &amp; features</p> <p>How profiles bundle features and how <code>--with</code>/<code>--no</code> resolution works.</p> </li> <li> <p> Extensions</p> <p>The curated per-harness catalogue: MCP servers, skills, subagents, bundles.</p> </li> <li> <p> The ECC bundle</p> <p>Everything Claude Code \u2014 the opt-in <code>ecc-*</code> family and how it's installed.</p> </li> <li> <p> LLM providers</p> <p>Cloud and local model providers for provider-agnostic harnesses.</p> </li> <li> <p> Integrations</p> <p>Host-side services, hosting modes, and automatic transport switching.</p> </li> <li> <p> Authentication</p> <p>Auth env vars, <code>--login</code>, OAuth, GPG agent, and AWS credential forwarding.</p> </li> <li> <p> Docker-in-Docker</p> <p>Let the container drive the host Docker daemon with <code>--dind</code>.</p> </li> <li> <p> Migrating from bash</p> <p>Convert a legacy <code>.vb.env</code> into the current TOML <code>.vb</code>.</p> </li> </ul>"},{"location":"guides/#related-pages","title":"Related pages","text":"<ul> <li>Reference \u2014 the exhaustive tables behind these guides.</li> <li>CLI reference \u2014 every <code>vibrate</code> subcommand.</li> <li>Core concepts \u2014 the vocabulary the guides assume.</li> </ul>"},{"location":"guides/authentication/","title":"Authentication","text":"<p>The container needs your credentials, but Vibrator never bakes them into the image \u2014 they're forwarded at run time from your host environment, files, or sockets. This guide covers every way credentials reach the container.</p>"},{"location":"guides/authentication/#harness-auth-env-vars","title":"Harness auth env vars","text":"<p>Each harness declares the env vars it uses for auth. At <code>docker run</code>, Vibrator forwards those that are set on your host:</p> Harness Forwarded if set Claude Code <code>CLAUDE_CODE_OAUTH_TOKEN</code>, <code>ANTHROPIC_API_KEY</code> Codex <code>OPENAI_API_KEY</code> OpenCode <code>ANTHROPIC_API_KEY</code>, <code>OPENAI_API_KEY</code>, <code>GEMINI_API_KEY</code>, <code>GROQ_API_KEY</code>, <code>OPENROUTER_API_KEY</code>, <code>DEEPSEEK_API_KEY</code> Pi <code>ANTHROPIC_API_KEY</code>, <code>OPENAI_API_KEY</code>, <code>GEMINI_API_KEY</code>, <code>GROQ_API_KEY</code>, <code>OPENROUTER_API_KEY</code>, <code>XAI_API_KEY</code>, <code>DEEPSEEK_API_KEY</code>, <code>HF_TOKEN</code> <p>So the simplest path is to export the key in your shell before running <code>vibrate</code>:</p> <pre><code>export ANTHROPIC_API_KEY=sk-...\nvibrate\n</code></pre> <p>The welcome banner shows which credential it detected (<code>auth: Anthropic API key</code>), or <code>not configured</code> if none is set.</p>"},{"location":"guides/authentication/#claude-code-oauth","title":"Claude Code OAuth","text":"<p>Claude Code can authenticate via a browser OAuth flow instead of an API key. Two paths:</p>"},{"location":"guides/authentication/#vibrate-login","title":"<code>vibrate --login</code>","text":"<pre><code>vibrate --login\n</code></pre> <p>This:</p> <ol> <li>Starts the container detached so the entrypoint    finishes its setup.</li> <li>Execs <code>claude auth login</code>, intercepts the printed OAuth URL, and opens it in your host    browser automatically.</li> <li>Writes the resulting auth state back to your host <code>~/.claude.json</code>.</li> <li>Execs the harness.</li> </ol> <p>Because the auth is saved to the host, subsequent launches without <code>--login</code> are pre-authenticated \u2014 the entrypoint merges host config into the container on every start. <code>--login</code> always runs the flow when passed, so use it to re-authenticate or switch accounts.</p>"},{"location":"guides/authentication/#host-config-merge","title":"Host config merge","text":"<p>If you've already authenticated Claude Code on your host (<code>~/.claude.json</code> has <code>oauthAccount</code>), Vibrator mounts that config read-only and the entrypoint merges the OAuth/onboarding state into the container \u2014 so you're authenticated without <code>--login</code> or an API key.</p>"},{"location":"guides/authentication/#token-file-fallback","title":"Token file fallback","text":"<p>For Claude Code, if <code>CLAUDE_CODE_OAUTH_TOKEN</code> isn't exported, Vibrator falls back to reading <code>~/.claude-docker-token</code> on your host (whitespace-trimmed). Handy for keeping a long-lived token in a file instead of your shell rc.</p>"},{"location":"guides/authentication/#extension-and-llm-credentials","title":"Extension and LLM credentials","text":"<ul> <li>Extensions that declare <code>auth.env</code> get that host env var   forwarded \u2014 e.g. an extension needing   <code>OPENAI_API_KEY</code> picks it up from your host automatically.</li> <li>LLM providers for Codex/OpenCode/Pi resolve their key from   <code>[llm.auth]</code> \u2014 either a host env var name (preferred)   or a pasted literal.</li> </ul>"},{"location":"guides/authentication/#env-forwarding","title":"<code>[env]</code> forwarding","text":"<p>For anything else, forward arbitrary variables via <code>.vb</code>:</p> <pre><code>[env]\nGITHUB_TOKEN = \"$GITHUB_TOKEN\"   # $NAME \u2192 resolved from host at run time\nREGION       = \"us-east-1\"       # literal value\n</code></pre>"},{"location":"guides/authentication/#gpg-agent-forwarding","title":"GPG agent forwarding","text":"<p>If you've configured an <code>extra-socket</code> in <code>~/.gnupg/gpg-agent.conf</code>, Vibrator auto-mounts it at <code>/gpg-agent-extra</code> and the entrypoint symlinks it where gpg expects. Then <code>git commit -S</code> and <code>gpg --sign</code> inside the container use your host private key \u2014 the key itself never crosses the boundary. No flag needed; it's automatic when the extra-socket exists.</p>"},{"location":"guides/authentication/#aws-credentials","title":"AWS credentials","text":"<p>If <code>~/.aws</code> exists on your host, it's mounted read-only into the container so AWS CLI / SDK calls work. Read-only means a buggy container can't rotate or wipe your credentials. Automatic \u2014 no flag.</p>"},{"location":"guides/authentication/#precedence","title":"Precedence","text":"<p>When env var names collide, later sources win: harness auth \u2192 LLM-derived \u2192 extension <code>auth.env</code> \u2192 <code>[env]</code> overrides. So your explicit <code>[env]</code> value always takes precedence, and the harness's own auth beats an extension's hint.</p>"},{"location":"guides/authentication/#related-pages","title":"Related pages","text":"<ul> <li>What happens on start \u2014 the full   forwarding order.</li> <li>LLM providers \u2014 provider credentials for Codex/OpenCode/Pi.</li> <li>Environment variables \u2014 every forwarded var.</li> </ul>"},{"location":"guides/configuration/","title":"The <code>.vb</code> file","text":"<p><code>.vb</code> is a per-workspace pin file in TOML. It records the choices you made so future <code>vibrate</code> runs in that workspace skip the wizard and resolve to the same image. It's auto-managed \u2014 the wizard writes it \u2014 but it's plain text you can read and edit by hand.</p> <p>For the exhaustive field-by-field schema, see the <code>.vb</code> reference. This page is the practical guide.</p>"},{"location":"guides/configuration/#what-it-captures","title":"What it captures","text":"<pre><code># vibrator workspace pin (`.vb`) \u2014 auto-managed by `vibrate`.\nharness = \"claude-code\"\nprofile = \"full\"\nshell   = \"zsh\"\nwith    = [\"docker-cli\"]\nno      = [\"aider\"]\nextensions = [\"context7\", \"ecc-developer\"]\n\n[integrations]\nserena = \"auto\"\n</code></pre> Section Holds <code>harness</code> / <code>profile</code> / <code>shell</code> the core selection (harness, profile) <code>with</code> / <code>no</code> feature deltas on the profile <code>extensions</code> selected extension IDs <code>[llm]</code> LLM provider choice (provider-agnostic harnesses only) <code>[prereqs.*]</code> minted credentials (e.g. claude-mem keys) <code>[env]</code> host\u2192container env forwarding <code>[integrations]</code> per-integration hosting modes"},{"location":"guides/configuration/#how-its-found","title":"How it's found","text":"<p><code>vibrate</code> walks up from your current directory looking for <code>.vb</code>, stopping at:</p> <ul> <li>the git root, if you're inside a repo, or</li> <li>the filesystem root otherwise.</li> </ul> <p>The first <code>.vb</code> found wins, and its directory becomes the workspace root. So a <code>.vb</code> at your repo root applies to every subdirectory of the repo.</p>"},{"location":"guides/configuration/#how-values-are-resolved","title":"How values are resolved","text":"<p>For each field the precedence is CLI flag \u2192 <code>.vb</code> value \u2192 built-in default:</p> <pre><code># .vb says profile = \"full\", but this run uses backend:\nvibrate --profile=backend\n</code></pre> <p>Flags are a per-invocation overlay; they don't rewrite <code>.vb</code> unless the wizard runs and saves. To try a combination once without persisting it, add <code>--no-save</code>.</p>"},{"location":"guides/configuration/#security-and-gitignore","title":"Security and gitignore","text":"<p><code>.vb</code> is written with mode <code>0600</code> because it may contain plaintext credentials (minted prereq keys, or a pasted LLM API key). When you first save a pin, Vibrator idempotently appends <code>.vb</code> to your <code>.gitignore</code> (only if one already exists \u2014 it won't create one).</p> <p>Keep <code>.vb</code> out of version control</p> <p>Treat <code>.vb</code> like a dotenv file. If your repo has no <code>.gitignore</code>, add one with a <code>.vb</code> line yourself before committing.</p>"},{"location":"guides/configuration/#editing-by-hand","title":"Editing by hand","text":"<p>You can edit <code>.vb</code> directly \u2014 it's just TOML. Common edits:</p> <pre><code># Forward a host env var into the container.\n[env]\nOPENAI_API_KEY = \"$OPENAI_API_KEY\"   # $NAME is resolved from the host at run time\nMY_FLAG        = \"literal-value\"     # literals pass through as-is\n\n# Pin an integration to host-only mode.\n[integrations]\nserena = \"host\"\n</code></pre> <p>After editing, the next <code>vibrate</code> picks the changes up. Note that build-affecting fields (<code>harness</code>, <code>profile</code>, <code>shell</code>, <code>with</code>, <code>no</code>, <code>extensions</code>) change the variant fingerprint, so they map to a different image/container \u2014 <code>vibrate</code> will build the new one.</p>"},{"location":"guides/configuration/#changing-setup-safely","title":"Changing setup safely","text":"<p>To re-pick harness/profile/extensions through the wizard while preserving your <code>[prereqs.*]</code> and <code>[env]</code>, use <code>vibrate reconfigure</code> rather than editing by hand.</p>"},{"location":"guides/configuration/#related-pages","title":"Related pages","text":"<ul> <li><code>.vb</code> schema reference \u2014 every field and its type.</li> <li>Migrating from bash \u2014 converting a legacy <code>.vb.env</code>.</li> <li>The wizard \u2014 what writes <code>.vb</code>.</li> </ul>"},{"location":"guides/docker-in-docker/","title":"Docker-in-Docker","text":"<p>Sometimes the agent needs to run <code>docker</code> itself \u2014 build images, run test containers, inspect the daemon. The <code>--dind</code> flag wires the container up to drive your host Docker daemon.</p> <pre><code>vibrate --dind\n</code></pre> <p>This grants host-root-equivalent access</p> <p>Mounting the host's Docker socket lets anything in the container run any <code>docker</code> command \u2014 including <code>docker run --privileged</code>. That's effectively root on your host. <code>--dind</code> is opt-in for exactly this reason. Only use it for workspaces you trust.</p>"},{"location":"guides/docker-in-docker/#what-dind-does","title":"What <code>--dind</code> does","text":"<ol> <li>Auto-installs the <code>docker-cli</code> feature \u2014 the container gets a <code>docker</code> client binary    (not a daemon). You can still override with <code>--no=docker-cli</code> if you supply your own.</li> <li>Mounts the host Docker socket at <code>/var/run/docker.sock</code> (the path every <code>docker</code> CLI    looks for by default, so no <code>DOCKER_HOST</code> needed inside).</li> <li>Adds the container user to the socket's group (<code>--group-add</code>) so <code>docker</code> works    without <code>sudo</code> \u2014 where the runtime allows it.</li> </ol> <p>The socket is discovered the same way as runtime detection: <code>$DOCKER_HOST</code> first, then the well-known per-runtime paths.</p>"},{"location":"guides/docker-in-docker/#vm-based-runtimes","title":"VM-based runtimes","text":"<p>On Colima and Rancher Desktop the macOS-side socket is a host-only proxy that containers (running inside the Linux VM) can't reach. For these, Vibrator mounts the daemon's own <code>/var/run/docker.sock</code> instead, and the <code>docker-cli</code> feature's sudo wrapper handles group access \u2014 <code>docker</code> in the container is a tiny shim that runs <code>sudo /usr/bin/docker</code>, and the container user has passwordless sudo inside the sandbox.</p>"},{"location":"guides/docker-in-docker/#verifying","title":"Verifying","text":"<pre><code>vibrate --dind shell\n# inside the container:\ndocker ps          # lists the host's containers\ndocker version     # client + server (the host daemon)\n</code></pre>"},{"location":"guides/docker-in-docker/#persisting-it","title":"Persisting it","text":"<p>To make a workspace always use DinD, the cleanest path is the auto-installed feature plus the flag each run. You can also pin the client binary by adding the feature explicitly:</p> <pre><code>vibrate --with=docker-cli   # bake the client in\n# then run with --dind when you actually need socket access\n</code></pre>"},{"location":"guides/docker-in-docker/#related-pages","title":"Related pages","text":"<ul> <li>Runtime detection \u2014 how the socket is found.</li> <li><code>docker-cli</code> feature \u2014 the client binary + sudo wrapper.</li> <li><code>vibrate</code> flags \u2014 <code>--dind</code>.</li> </ul>"},{"location":"guides/ecc/","title":"The ECC bundle","text":"<p>ECC \u2014 \"Everything Claude Code\" (MIT) \u2014 is a cross-harness bundle of subagents, skills, rules, and hooks. Vibrator ships it as a family of opt-in extensions, one per ECC profile, each named <code>ecc-*</code>.</p> <p>When selected, an <code>ecc-*</code> extension's build-time install snippet shallow-fetches a pinned ECC commit and runs ECC's own manifest-driven installer into the harness-native config directory (<code>~/.claude</code>, <code>~/.codex</code>, or <code>~/.opencode</code>).</p>"},{"location":"guides/ecc/#profiles","title":"Profiles","text":"<p>ECC profiles trade capability against agent-context cost. None is on by default \u2014 you opt in consciously.</p> Profile What it is claude-code codex opencode <code>ecc-minimal</code> lightest, no hook runtime \u2713 \u2014 \u2713 <code>ecc-core</code> lean baseline \u2713 \u2713 \u2713 <code>ecc-developer</code> default engineering preset (recommended) \u2713 \u2713 \u2713 <code>ecc-security</code> core + security module \u2713 \u2713 \u2713 <code>ecc-research</code> core + research/content \u2713 \u2713 \u2713 <code>ecc-full</code> everything (heaviest context) \u2713 \u2713 \u2713 <pre><code>vibrate --harness=claude-code --extensions=ecc-developer\nvibrate extensions show ecc-developer        # full docs for any profile\n</code></pre> <p>Harness coverage</p> <ul> <li>Codex has no <code>ecc-minimal</code> \u2014 with the hook runtime skipped, minimal and core   resolve to the same install on Codex, so only <code>ecc-core</code> is offered.</li> <li>Pi is not supported: ECC ships no <code>pi</code> adapter, so there are no <code>ecc-*</code> entries   for the Pi harness.</li> </ul>"},{"location":"guides/ecc/#how-its-installed","title":"How it's installed","text":"<p>The install snippet (visible via <code>vibrate extensions show &lt;id&gt;</code>) does, roughly:</p> <pre><code>ECC_REF=&lt;pinned-commit&gt;                    # same SHA across all ecc-* entries\ngit init -q\ngit remote add origin https://github.com/affaan-m/ECC.git\ngit fetch -q --depth 1 origin \"$ECC_REF\"   # shallow-fetch exactly the pinned commit\ngit checkout -q FETCH_HEAD\nnpm install --no-audit --no-fund --omit=dev\nnode scripts/install-apply.js --target &lt;harness&gt; --profile &lt;profile&gt;\n</code></pre> <p>Because ECC's installer needs Node, every <code>ecc-*</code> entry declares <code>deps.features: [node]</code>, which auto-enables the <code>node</code> feature.</p>"},{"location":"guides/ecc/#pinning","title":"Pinning","text":"<p>Every <code>ecc-*</code> entry pins the same ECC commit for reproducibility and supply-chain hygiene. Bumping it is a deliberate, single find-and-replace of <code>ECC_REF=</code> across all <code>extensions/*/ecc-*.md</code> files \u2014 a consistency test fails the build if the pins ever drift apart.</p>"},{"location":"guides/ecc/#context-cost","title":"Context cost","text":"<p>ECC profiles install a lot of agents, skills, and rules (<code>ecc-developer</code> is ~63 agents + ~249 skills; <code>ecc-full</code> is heavier still). That capability comes at the cost of agent context budget, so pick the smallest profile that covers your workflow. The wizard shows an \"about\" blurb for the focused entry to help you choose.</p>"},{"location":"guides/ecc/#related-pages","title":"Related pages","text":"<ul> <li>Extensions \u2014 the catalogue mechanics ECC plugs into.</li> <li><code>vibrate extensions show</code> \u2014   read any <code>ecc-*</code> entry's full docs and install snippet.</li> </ul>"},{"location":"guides/extensions/","title":"Extensions","text":"<p>An extension is something installed on top of the harness at build time \u2014 an MCP server, a slash-command skill, a subagent, a runtime tool, or a whole bundle like ECC. The catalogue is curated and data-driven: one Markdown file per item, with YAML frontmatter, organized per harness under <code>extensions/&lt;harness&gt;/</code>. Adding or updating an extension is a docs-style change, not Go code.</p>"},{"location":"guides/extensions/#browsing-the-catalogue","title":"Browsing the catalogue","text":"<pre><code>vibrate extensions list                 # counts per harness\nvibrate extensions list claude-code     # entries for one harness\nvibrate extensions show context7        # full docs for one entry\n</code></pre> <p>See <code>vibrate extensions</code> for the command details.</p>"},{"location":"guides/extensions/#selecting-extensions","title":"Selecting extensions","text":"<p>By ID, comma-separated, scoped to your chosen harness:</p> <pre><code>vibrate --harness=claude-code --extensions=context7,ecc-developer\n</code></pre> <p>Or tick them in the wizard \u2014 which pre-checks entries that are marked default or that you already use on the host (detected via <code>hostprobe</code>). Selections persist in <code>.vb</code> as <code>extensions</code>.</p> <p>Extensions are per-harness</p> <p>An extension ID is resolved as <code>&lt;harness&gt;/&lt;id&gt;</code>. Selecting an ID that doesn't exist for your harness is an error. Run <code>vibrate extensions list &lt;harness&gt;</code> to see what's available.</p>"},{"location":"guides/extensions/#what-an-entry-looks-like","title":"What an entry looks like","text":"<p>Each entry is Markdown with frontmatter. A simple MCP-server example:</p> <pre><code>---\nname: context7\ndescription: Up-to-date library docs MCP \u2014 fetches API references on demand\nkind: mcp\ndefault: true\nsize_mb: 1\ninstall: |\n  claude mcp add context7 \\\n    --scope user \\\n    --transport http \\\n    https://mcp.context7.com/mcp\nsource: https://github.com/upstash/context7\n---\n\n# context7\n...prose describing the extension...\n</code></pre>"},{"location":"guides/extensions/#frontmatter-fields","title":"Frontmatter fields","text":"Field Meaning <code>name</code> display label (required) <code>kind</code> <code>plugin</code>, <code>skill</code>, <code>mcp</code>, <code>subagent</code>, or <code>tool</code> (required) <code>source</code> upstream URL (required) <code>description</code> short summary shown in the wizard <code>default</code> pre-check in the wizard <code>size_mb</code> approximate image-size impact <code>category</code> one of the catalogue categories <code>deps.features</code> features the install needs (folded into resolution) <code>deps.extensions</code> other extension IDs this one needs <code>prereq</code> a host-side prerequisite ID to verify <code>auth.env</code> env var carrying a credential, forwarded from the host <code>install</code> the shell snippet run at build time <code>host_aliases</code> alternate IDs for hostprobe matching <code>runtime_needs</code> declares network/host-service needs (e.g. <code>outbound_net</code>, <code>self_hosted</code>)"},{"location":"guides/extensions/#kinds","title":"Kinds","text":"Kind What it is <code>plugin</code> a complete pluggable package (command + skill + MCP) <code>skill</code> a slash-command-triggered behavior <code>mcp</code> a Model Context Protocol server <code>subagent</code> a dispatchable specialized agent <code>tool</code> a CLI/runtime tool"},{"location":"guides/extensions/#categories","title":"Categories","text":"<p><code>code-intelligence</code>, <code>memory</code>, <code>documentation</code>, <code>web-browser</code>, <code>version-control</code>, <code>project-management</code>, <code>communication</code>, <code>cloud-infrastructure</code>, <code>databases</code>, <code>design-ui</code>, <code>testing</code>, <code>security</code>, <code>ai-integration</code>, <code>dev-tools</code>, <code>observability</code>, <code>harness-specific</code>, <code>niche</code>.</p>"},{"location":"guides/extensions/#how-extensions-affect-the-build-and-run","title":"How extensions affect the build and run","text":"<ul> <li>Build: each selected entry's <code>install</code> snippet runs in   Stage 4. Its <code>deps.features</code> are merged into   feature resolution, so a node-dependent   MCP pulls in the <code>node</code> feature automatically.</li> <li>Run: if an entry declares <code>auth.env</code>, that host env var is   forwarded into the container so the   extension can authenticate.</li> <li>Fingerprint: selected extensions are part of the   variant fingerprint \u2014 changing them builds a new   image.</li> </ul>"},{"location":"guides/extensions/#catalogue-highlights","title":"Catalogue highlights","text":"<p>The catalogue spans all four harnesses and includes, among many others:</p> <ul> <li>MCP servers \u2014 <code>context7</code>, <code>github-mcp</code>, <code>linear-mcp</code>, <code>notion-mcp</code>, <code>playwright-mcp</code>,   <code>postgres-mcp</code>, <code>sentry-mcp</code>, <code>kubernetes-mcp</code>, <code>terraform-mcp</code>, <code>filesystem-mcp</code>,   <code>sqlite-mcp</code>, <code>fetch</code>, <code>sequential-thinking</code>, <code>chrome-devtools</code>.</li> <li>Skills / agents \u2014 <code>docs-writer-skill</code>, <code>code-review</code>, <code>code-reviewer-agent</code>,   <code>pm-agent</code>, <code>superpowers</code>, <code>superclaude</code>.</li> <li>Integration plugins \u2014 <code>serena</code>, <code>claude-mem</code>, <code>codex-plugin-cc</code>, <code>cc-thingz</code>.</li> <li>The ECC bundle \u2014 the <code>ecc-*</code> family.</li> </ul> <p>Run <code>vibrate extensions list &lt;harness&gt;</code> for the authoritative, current list.</p>"},{"location":"guides/extensions/#related-pages","title":"Related pages","text":"<ul> <li>The ECC bundle \u2014 the big opt-in bundle.</li> <li><code>vibrate extensions</code> \u2014 command reference.</li> <li>Profiles &amp; features \u2014 how <code>deps.features</code> feed resolution.</li> </ul>"},{"location":"guides/harnesses/","title":"Harnesses","text":"<p>A harness is the AI coding agent that runs inside the container. Vibrator ships four built-ins. You pick one with <code>--harness=&lt;id&gt;</code> or in the wizard; it's stored as <code>harness</code> in <code>.vb</code>.</p> <p>Each harness declares five things that drive the rest of the tool:</p> <ul> <li>Install \u2014 the Dockerfile fragment that puts its binary in the image   (Stage 3).</li> <li>Auth env vars \u2014 host environment variables forwarded for authentication.</li> <li>Host config dir \u2014 where its settings live on your host (mounted for persistence).</li> <li>Required features \u2014 base features it needs.</li> <li>LLM provider support \u2014 whether the LLM step applies.</li> </ul>"},{"location":"guides/harnesses/#at-a-glance","title":"At a glance","text":"Harness ID Launches Required features LLM provider step Host config Claude Code <code>claude-code</code> <code>claude</code> \u2014 \u2717 <code>~/.claude</code> OpenAI Codex <code>codex</code> <code>codex</code> <code>node</code> \u2713 <code>~/.codex</code> OpenCode <code>opencode</code> <code>opencode</code> \u2014 \u2713 <code>~/.config/opencode</code> Pi <code>pi</code> <code>pi</code> <code>node</code> \u2713 <code>~/.pi</code> <p><code>vibrate</code> (bare) launches the harness's CLI; <code>vibrate shell</code> launches your shell instead. Update the agent in place with <code>vibrate update</code>.</p>"},{"location":"guides/harnesses/#claude-code","title":"Claude Code","text":"<p>Anthropic's Claude Code. Installed via the official <code>claude.ai/install.sh</code> script and symlinked into <code>/usr/local/bin/claude</code>.</p> <ul> <li>Auth env vars: <code>CLAUDE_CODE_OAUTH_TOKEN</code> (preferred), <code>ANTHROPIC_API_KEY</code> (fallback).   See Authentication, including <code>vibrate --login</code> for the OAuth flow.</li> <li>Host config: <code>~/.claude</code> \u2014 extensively mounted so onboarding state, rules, settings,   hooks, and session history carry over (see What happens on start).</li> <li>LLM provider step: not shown \u2014 Claude Code is Anthropic-only, so the auth env vars   suffice.</li> <li>Update: <code>claude update</code>.</li> </ul> <p>Claude Code is also the harness with the richest integration support (Serena, claude-mem) and the ECC bundle.</p>"},{"location":"guides/harnesses/#codex","title":"Codex","text":"<p>OpenAI Codex. Installed with <code>npm install -g @openai/codex</code>, so it requires the <code>node</code> feature (auto-enabled).</p> <ul> <li>Auth env vars: <code>OPENAI_API_KEY</code>.</li> <li>Host config: <code>~/.codex</code>.</li> <li>LLM provider step: shown. Codex maps your provider into an OpenAI-compatible shape   (<code>OPENAI_API_KEY</code> + <code>OPENAI_BASE_URL</code>), so you can point it at OpenAI, a local   Ollama/LM Studio server, or any OpenAI-compatible endpoint.</li> <li>Update: <code>npm install -g @openai/codex@latest</code>.</li> </ul>"},{"location":"guides/harnesses/#opencode","title":"OpenCode","text":"<p>SST's OpenCode. Installed as a self-contained prebuilt binary downloaded from GitHub Releases (architecture-aware, pinned to <code>0.5.0</code>), so it needs no base feature.</p> <ul> <li>Auth env vars: <code>ANTHROPIC_API_KEY</code>, <code>OPENAI_API_KEY</code>, <code>GEMINI_API_KEY</code>,   <code>GROQ_API_KEY</code>, <code>OPENROUTER_API_KEY</code>, <code>DEEPSEEK_API_KEY</code>.</li> <li>Host config: <code>~/.config/opencode</code>.</li> <li>LLM provider step: shown. OpenCode is multi-provider; pick the one you want and the   matching key is forwarded.</li> <li>Update: <code>opencode upgrade</code>.</li> </ul>"},{"location":"guides/harnesses/#pi","title":"Pi","text":"<p><code>@mariozechner/pi-coding-agent</code>. Installed with <code>npm install -g</code>, so it requires the <code>node</code> feature.</p> <ul> <li>Auth env vars: <code>ANTHROPIC_API_KEY</code>, <code>OPENAI_API_KEY</code>, <code>GEMINI_API_KEY</code>,   <code>GROQ_API_KEY</code>, <code>OPENROUTER_API_KEY</code>, <code>XAI_API_KEY</code>, <code>DEEPSEEK_API_KEY</code>, <code>HF_TOKEN</code>.</li> <li>Host config: <code>~/.pi</code>.</li> <li>LLM provider step: shown (OpenAI-compatible mapping).</li> <li>Update: <code>npm install -g @mariozechner/pi-coding-agent@latest</code>.</li> </ul> <p>ECC and Pi</p> <p>The ECC bundle ships no <code>pi</code> adapter, so there are no <code>ecc-*</code> extensions for the Pi harness.</p>"},{"location":"guides/harnesses/#adding-a-harness","title":"Adding a harness","text":"<p>Harnesses are built-in Go types \u2014 adding one is a code change (a small struct implementing the <code>Harness</code> interface plus a registry entry), submitted as a PR. Day-to-day \"add another plugin/MCP\", by contrast, is data-driven via the extensions catalogue and needs no Go code. See Architecture.</p>"},{"location":"guides/harnesses/#related-pages","title":"Related pages","text":"<ul> <li>Authentication \u2014 getting your keys/tokens into the container.</li> <li>LLM providers \u2014 the provider step for Codex/OpenCode/Pi.</li> <li><code>vibrate update</code> \u2014 upgrading the agent CLI.</li> </ul>"},{"location":"guides/integrations/","title":"Integrations","text":"<p>An integration connects your workspace container to a host-side service \u2014 an MCP server, a memory system \u2014 without exposing host credentials to the container or making you hand-configure Docker networking. Vibrator ships two built-ins:</p> Integration What it is Page Serena code-aware MCP server (symbol search, references) Serena claude-mem persistent agent memory claude-mem <p>This guide covers the mechanics common to all integrations. The linked pages cover setup for each.</p>"},{"location":"guides/integrations/#how-integrations-wire-in","title":"How integrations wire in","text":"<p>Each integration is declared as a Go descriptor that self-registers. At image-build time, the descriptors are serialized \u2014 filtered to the harness being built \u2014 into <code>/etc/vibrator/integrations.json</code> inside the image. Then the <code>claude-exec</code> wrapper reads that manifest on every session entry and wires each integration's MCP transport into <code>~/.claude.json</code>, plus any env vars it declares.</p> <pre><code>flowchart LR\n    D[Integration descriptor] --&gt;|build time| M[/etc/vibrator/integrations.json]\n    M --&gt;|every session| CE[claude-exec]\n    CE --&gt;|http or stdio| CJ[~/.claude.json mcpServers]</code></pre> <p>Because the wiring is re-evaluated on every entry (not just first run), starting or stopping a host server between sessions is picked up automatically \u2014 no rebuild.</p>"},{"location":"guides/integrations/#hosting-modes","title":"Hosting modes","text":"<p>You choose a policy per integration, per workspace. It's stored in <code>.vb</code> under <code>[integrations]</code> (keyed by integration ID), and forwarded into the container as <code>VIBRATOR_INTEGRATION_MODE_&lt;ID&gt;</code>:</p> Mode Behavior <code>auto</code> (default) Probe the host server. Reachable \u2192 use HTTP. Unreachable \u2192 fall back to a container-local instance (stdio), with a visible warning. <code>host</code> Use HTTP only. If the host server is unreachable, warn loudly and don't fall back \u2014 the failure stays visible. <code>local</code> Always use the container-local instance (stdio); never probe the host. <code>off</code> Remove the integration's MCP entry entirely. <pre><code># .vb\n[integrations]\nserena     = \"host\"     # require my host Serena server\nclaude-mem = \"auto\"     # probe, fall back if down\n</code></pre> <p>A missing key means <code>auto</code>.</p>"},{"location":"guides/integrations/#transport-switching-in-practice","title":"Transport switching in practice","text":"<p>In <code>auto</code> mode the container probes the integration's HTTP URL with a short timeout on session entry:</p> <ul> <li>Reachable \u2192 writes an <code>{ type: \"http\", url: ... }</code> MCP entry.</li> <li>Unreachable \u2192 writes the stdio fallback (e.g. spawning the server locally) and warns.</li> </ul> <p>Set <code>VIBRATOR_VERBOSE=1</code> to see the decision on each entry (<code>serena: switched to http transport</code> / <code>serena: fell back to stdio</code>).</p>"},{"location":"guides/integrations/#pre-launch-readiness-checks","title":"Pre-launch readiness checks","text":"<p>Before entering the container, <code>vibrate</code> runs each integration's launch checks. A failing check prints a warning with a fix hint and, for fixable gaps, offers an inline prompt:</p> <pre><code>\u26a0  [claude-mem] no workspace key \u2014 all auth'd requests will return 401\n   hint: a project-scoped bearer token must be minted against the host postgres\n   run:  vibrate prereqs bootstrap claude-mem-server-beta\n   Bootstrap now? [y/N]\n</code></pre> <p>Checks never block the launch</p> <p>A failing readiness check is a warning, never a hard stop. A dormant integration is better than blocking you from reaching your container. Answering <code>y</code> to a fixable check runs the fix inline and saves the result to <code>.vb</code> before continuing.</p>"},{"location":"guides/integrations/#setting-up-the-host-side","title":"Setting up the host side","text":"<p>Use <code>vibrate integrations</code> to configure and run the host service, and <code>vibrate prereqs</code> for per-workspace bootstrap (like minting a claude-mem key).</p> <pre><code>vibrate integrations            # interactive picker\nvibrate integrations serena     # manage the Serena host server\nvibrate integrations claude-mem # configure + run claude-mem\n</code></pre>"},{"location":"guides/integrations/#related-pages","title":"Related pages","text":"<ul> <li>Serena \u2014 code intelligence MCP, with <code>auto</code>/<code>host</code>/<code>local</code>   fallback.</li> <li>claude-mem \u2014 persistent memory, with host admin config   and per-workspace keys.</li> <li>Environment variables \u2014   <code>VIBRATOR_INTEGRATION_MODE_*</code>, <code>SERENA_PORT</code>, and more.</li> </ul>"},{"location":"guides/llm-providers/","title":"LLM providers","text":"<p>For provider-agnostic harnesses \u2014 Codex, OpenCode, and Pi \u2014 the wizard shows an LLM provider step, and your choice is stored in <code>.vb</code> under <code>[llm]</code>. Claude Code is Anthropic-only, so it skips this step and uses its auth env vars instead.</p>"},{"location":"guides/llm-providers/#supported-providers","title":"Supported providers","text":"Provider Type Credential Default endpoint <code>anthropic</code> cloud API key <code>https://api.anthropic.com</code> <code>openai</code> cloud API key <code>https://api.openai.com</code> <code>ollama</code> local none <code>http://host.docker.internal:11434</code> <code>lmstudio</code> local none <code>http://host.docker.internal:1234</code> <code>openai-compat</code> custom API key your URL <p>The harness maps your provider/model/key into the environment variables it expects (for Codex/Pi that's an OpenAI-compatible <code>OPENAI_API_KEY</code> + <code>OPENAI_BASE_URL</code> shape; OpenCode uses per-provider keys).</p>"},{"location":"guides/llm-providers/#the-llm-block","title":"The <code>[llm]</code> block","text":"<pre><code>[llm]\nprovider = \"openai\"\nmodel    = \"gpt-4o\"\n\n[llm.auth]\nenv = \"OPENAI_API_KEY\"     # name of a host env var (preferred)\n# value = \"sk-...\"         # OR a pasted literal (plaintext in .vb \u2014 avoid if possible)\n</code></pre> <p>For local providers, <code>[llm.auth]</code> is omitted \u2014 no key is needed:</p> <pre><code>[llm]\nprovider = \"ollama\"\nmodel    = \"qwen3:32b\"\n# base_url defaults to http://host.docker.internal:11434\n</code></pre> <p>See the <code>.vb</code> reference for the full schema.</p>"},{"location":"guides/llm-providers/#cloud-providers","title":"Cloud providers","text":"<p>The wizard's cloud path asks for a model (preset list with a custom fallback) and an auth method:</p> <ul> <li>Env var (recommended) \u2014 store the name of a host environment variable. The key   stays in your shell and is forwarded at run time; it never touches <code>.vb</code>.</li> <li>Paste a value \u2014 stored as a literal in <code>.vb</code> (mode <code>0600</code>, gitignored). Convenient   but less safe \u2014 prefer the env var path.</li> </ul>"},{"location":"guides/llm-providers/#local-providers-ollama-lm-studio","title":"Local providers (Ollama / LM Studio)","text":"<p>Local providers run on your host and are reached from the container via <code>host.docker.internal</code>. Vibrator manages their lifecycle before launch:</p> <ul> <li>Model enumeration \u2014 the wizard lists models already downloaded on your host (or lets   you type a custom name if the server is unreachable).</li> <li>Ensure-running \u2014 at launch, <code>vibrate</code> probes the provider; if it's down it tries to   start it (<code>ollama serve</code>), waits for readiness, and pulls the requested model if missing.   If it can't be reached or started, the launch aborts rather than running a container that   would immediately fail.</li> </ul> Provider Host binary Default URL Ollama <code>ollama</code> <code>http://host.docker.internal:11434</code> LM Studio <code>lms</code> <code>http://host.docker.internal:1234</code> <pre><code># Example: Codex against a local Ollama model.\nvibrate --harness=codex   # then pick \"ollama\" + a model in the wizard\n</code></pre>"},{"location":"guides/llm-providers/#openai-compatible-endpoints","title":"OpenAI-compatible endpoints","text":"<p>Choose <code>openai-compat</code> to point at any OpenAI-compatible HTTP API \u2014 a self-hosted gateway, a proxy, or a third-party service. You supply the base URL and a credential (env var or literal).</p>"},{"location":"guides/llm-providers/#related-pages","title":"Related pages","text":"<ul> <li>Harnesses \u2014 which harnesses support the provider step.</li> <li>Authentication \u2014 how keys are forwarded into the container.</li> <li><code>.vb</code> schema \u2014 the <code>[llm]</code> block in full.</li> </ul>"},{"location":"guides/migrating/","title":"Migrating from bash","text":"<p>Earlier Vibrator workspaces used a shell dotenv file, <code>.vb.env</code>. The current tool uses a TOML pin, <code>.vb</code>. The <code>vibrate migrate-pin</code> command converts one to the other, losslessly.</p>"},{"location":"guides/migrating/#convert-a-workspace","title":"Convert a workspace","text":"<pre><code>cd ~/my-project\n\n# Preview the conversion \u2014 nothing is written.\nvibrate migrate-pin --dry-run\n\n# Convert: writes .vb, archives the old file to .vb.env.bak, updates .gitignore.\nvibrate migrate-pin\n</code></pre> <p>By default the original <code>.vb.env</code> is archived to <code>.vb.env.bak</code>. Pass <code>--keep-old</code> to leave it in place.</p>"},{"location":"guides/migrating/#what-gets-mapped","title":"What gets mapped","text":"Legacy <code>.vb.env</code> New <code>.vb</code> <code>HARNESS</code> <code>harness</code> <code>PROFILE</code> <code>profile</code> <code>SHELL</code> <code>shell</code> <code>WITH</code> <code>with</code> (comma-split) <code>NO</code> <code>no</code> (comma-split) <code>CATALOG</code> <code>extensions</code> (comma-split) <code>CLAUDE_MEM_SERVER_BETA_API_KEY</code> etc. <code>[prereqs.claude-mem-server-beta]</code> <code>USERNAME</code> dropped \u2014 now a build-time flag anything else preserved verbatim under <code>[env]</code> <p>Nothing is lost</p> <p>Unrecognized keys are preserved under <code>[env]</code>, so custom variables you set in the old file survive the conversion. Review the result (or use <code>--dry-run</code> first).</p>"},{"location":"guides/migrating/#example","title":"Example","text":"<p>Before \u2014 <code>.vb.env</code>:</p> <pre><code>HARNESS=claude-code\nPROFILE=full\nWITH=docker-cli\nCATALOG=context7,ecc-developer\nCLAUDE_MEM_SERVER_BETA_API_KEY=cmem_abc...\nOPENAI_API_KEY=sk-...\n</code></pre> <p>After \u2014 <code>.vb</code>:</p> <pre><code>harness = \"claude-code\"\nprofile = \"full\"\nwith = [\"docker-cli\"]\nextensions = [\"context7\", \"ecc-developer\"]\n\n[prereqs.claude-mem-server-beta]\napi_key = \"cmem_abc...\"\n\n[env]\nOPENAI_API_KEY = \"sk-...\"\n</code></pre>"},{"location":"guides/migrating/#after-converting","title":"After converting","text":"<p>The new <code>.vb</code> is everything subsequent runs need:</p> <pre><code>vibrate          # resolves from the converted .vb\n</code></pre> <p>If you want to re-pick anything through the wizard while keeping your credentials, use <code>vibrate reconfigure</code>.</p>"},{"location":"guides/migrating/#related-pages","title":"Related pages","text":"<ul> <li><code>vibrate migrate-pin</code> \u2014 the command reference.</li> <li>The <code>.vb</code> file \u2014 the target format explained.</li> <li><code>.vb</code> schema \u2014 every field.</li> </ul>"},{"location":"guides/profiles-and-features/","title":"Profiles &amp; features","text":"<p>A profile is a named starting bundle of features, and features are the build-time capability layers (language toolchains, browser runtime, CLIs, tool bundles) that make up an image. This guide explains how they combine and how resolution works. For the complete catalogues, see the Profiles reference and the Features reference.</p>"},{"location":"guides/profiles-and-features/#profiles-are-a-starting-point","title":"Profiles are a starting point","text":"<p>Pick one with <code>--profile=&lt;id&gt;</code> (default <code>full</code>). It seeds the feature set; you tune it from there.</p> Profile Best for Features <code>minimal</code> a shell-in-a-box, CI, tight resources (none \u2014 base toolkit only) <code>backend</code> Python/Go services <code>python</code>, <code>go</code>, <code>gh</code>, <code>postgres-client</code>, <code>ralphex</code> <code>frontend</code> web/UI work <code>node</code>, <code>playwright</code>, <code>gh</code>, <code>ralphex</code> <code>full</code> (default) everything except <code>aider</code> <code>python</code>, <code>go</code>, <code>node</code>, <code>playwright</code>, <code>gh</code>, <code>postgres-client</code>, <code>audit-toolkit</code>, <code>codex-cli</code>, <code>ralphex</code> <p>The base toolkit (jq, ripgrep, fd, fzf, vim, git, curl, ssh, \u2026) is always present regardless of profile \u2014 it's baked into Stage 1.</p>"},{"location":"guides/profiles-and-features/#tuning-with-with-and-no","title":"Tuning with <code>--with</code> and <code>--no</code>","text":"<p>Layer deltas on top of the profile:</p> <pre><code># Backend, plus a browser, minus the autonomous loop tool.\nvibrate --profile=backend --with=node,playwright --no=ralphex\n\n# Minimal, but add just Python.\nvibrate --profile=minimal --with=python\n</code></pre> <p>These persist in <code>.vb</code> as <code>with</code> / <code>no</code>.</p>"},{"location":"guides/profiles-and-features/#how-resolution-works","title":"How resolution works","text":"<p>The final feature set is computed like this:</p> <pre><code>flowchart LR\n    P[Profile features] --&gt; M[merge]\n    H[Harness required] --&gt; M\n    X[Extension deps] --&gt; M\n    M --&gt; W[\"+ --with\"] --&gt; N[\"\u2212 --no\"] --&gt; D[Auto-enable&lt;br/&gt;transitive deps] --&gt; R[Resolved set]</code></pre> <ol> <li>Start with the profile's features.</li> <li>Add features required by the harness (e.g. Codex needs <code>node</code>) and by any selected    extensions (their <code>deps.features</code>).</li> <li>Apply <code>--with</code> (add) then <code>--no</code> (remove).</li> <li>Auto-enable transitive dependencies: selecting <code>playwright</code> pulls in <code>node</code>;    <code>audit-toolkit</code> pulls in <code>python</code>.</li> </ol> <p>Features are emitted into the Dockerfile in dependency order (deps before dependents), so the build is deterministic.</p>"},{"location":"guides/profiles-and-features/#a-subtlety-deps-win-over-no","title":"A subtlety: deps win over <code>--no</code>","text":"<p>If you remove a feature that something else still needs, it gets auto-re-enabled. For example <code>--no=node --with=playwright</code> keeps <code>node</code>, because Playwright depends on it. To truly drop it, also drop its dependent (<code>--no=node,playwright</code>). This \"auto-enable deps\" behavior is intentional \u2014 it's friendlier than failing the build.</p>"},{"location":"guides/profiles-and-features/#why-profile-isnt-in-the-fingerprint","title":"Why profile isn't in the fingerprint","text":"<p>The variant fingerprint is computed from the resolved feature set, not the profile label. So <code>--profile=full</code> and the implicit default (which resolves to <code>full</code>) produce the same image \u2014 the profile is purely a UX shorthand. Two specs that resolve to identical features share an image even if you named the profile differently.</p>"},{"location":"guides/profiles-and-features/#image-size","title":"Image size","text":"<p>Profiles carry rough size estimates (informational only): <code>minimal</code> ~150 MB, <code>backend</code> ~600 MB, <code>frontend</code> ~1 GB, <code>full</code> ~2 GB. Reach for <code>minimal</code> + targeted <code>--with</code> when image size or build time matters.</p>"},{"location":"guides/profiles-and-features/#related-pages","title":"Related pages","text":"<ul> <li>Profiles reference \u2014 the canonical bundles.</li> <li>Features reference \u2014 every feature, its deps, and what it   installs.</li> <li>What happens on build \u2014 where features land in the Dockerfile.</li> </ul>"},{"location":"integrations/","title":"Integrations","text":"<p>Vibrator integrations connect your workspace container to host-side services \u2014 MCP servers, memory systems, observability tools \u2014 without exposing host credentials to the container or requiring manual Docker networking.</p> <p>This page covers the two built-in integrations and the mechanics they share. For the conceptual guide (hosting modes, transport switching, readiness checks), see Integrations.</p>"},{"location":"integrations/#built-in-integrations","title":"Built-in integrations","text":"ID Name Category Setup <code>serena</code> Serena MCP code intelligence Serena <code>claude-mem</code> claude-mem memory claude-mem"},{"location":"integrations/#how-an-integration-is-defined","title":"How an integration is defined","text":"<p>Each integration is a Go descriptor (in <code>internal/integration/&lt;name&gt;/</code>) that self-registers at startup. A descriptor specifies:</p> Field Purpose <code>Runtimes</code> Ways to run it on the host \u2014 background process, Docker container, or Compose stack <code>ProbeFn</code> An HTTP/TCP reachability check <code>Wiring</code> How the container consumes it (MCP entry + env vars) <code>AdminConfig</code> Path to a host-only config file (may hold credentials) <code>Workspace</code> Per-workspace credential bootstrap (optional) <code>LaunchChecks</code> Pre-launch readiness checks"},{"location":"integrations/#build-time-manifest","title":"Build-time manifest","text":"<p>At image build, each integration's wiring (filtered to the harness being built) is serialized into <code>/etc/vibrator/integrations.json</code> inside the image. The <code>claude-exec</code> wrapper reads it on every session entry and:</p> <ul> <li>For MCP HTTP entries \u2014 probes the URL, writes the transport to <code>~/.claude.json</code>.</li> <li>For MCP stdio entries \u2014 writes the command (unconditionally, or as the fallback).</li> <li>Respects the per-workspace <code>VIBRATOR_INTEGRATION_MODE_&lt;ID&gt;</code> override.</li> </ul>"},{"location":"integrations/#hosting-modes","title":"Hosting modes","text":"<p>Set per workspace in <code>.vb</code> under <code>[integrations]</code>:</p> Mode Behavior <code>auto</code> (default) Probe the host server \u2192 HTTP if reachable, else stdio fallback. <code>host</code> HTTP only; warn loudly if unreachable (no fallback). <code>local</code> stdio only; never probe the host. <code>off</code> Remove the MCP entry entirely."},{"location":"integrations/#pre-launch-readiness-checks","title":"Pre-launch readiness checks","text":"<p>Before entering the container, Vibrator runs each integration's <code>LaunchChecks</code>. A failing check prints a warning and, for fixable gaps, offers an inline <code>Bootstrap now? [y/N]</code> prompt. Failing checks never block the launch \u2014 a dormant integration beats blocking you from your container.</p>"},{"location":"integrations/#runtime-kinds","title":"Runtime kinds","text":"<p>An integration can run on the host in one of three ways:</p> Kind Used by What it does process Serena A detached background process tracked by a PID file + log under <code>~/.local/share/vibrator/</code>. docker Serena A single managed container with <code>--restart unless-stopped</code>. compose claude-mem A <code>docker compose</code> stack (Postgres + worker + server)."},{"location":"integrations/#adding-a-new-integration","title":"Adding a new integration","text":"<ol> <li>Create <code>internal/integration/&lt;name&gt;/descriptor.go</code>.</li> <li>Call <code>integration.Register(descriptor())</code> from <code>init()</code>.</li> <li>Blank-import the package so it registers.</li> <li>Add a <code>LaunchChecks</code> slice with at least a server-probe check.</li> <li>Add a docs page here.</li> </ol> <p>It then appears in <code>vibrate integrations</code>, gets its wiring baked into every new image, and its checks run on every launch.</p>"},{"location":"integrations/#related-pages","title":"Related pages","text":"<ul> <li>Integrations guide \u2014 the conceptual walkthrough.</li> <li><code>vibrate integrations</code> \u2014 host-side setup command.</li> <li><code>vibrate prereqs</code> \u2014 per-workspace bootstrap.</li> </ul>"},{"location":"integrations/claude-mem/","title":"claude-mem","text":"<p>claude-mem gives the agent persistent memory \u2014 it records observations from every session and injects relevant context into future prompts. Vibrator wires it into the Claude Code harness in server-beta mode: a long-running stack on your host handles storage, and each workspace gets a project-scoped bearer token.</p>"},{"location":"integrations/claude-mem/#architecture","title":"Architecture","text":"<pre><code>Host\n\u251c\u2500\u2500 Postgres (your own instance or bundled)\n\u251c\u2500\u2500 claude-mem-server   (HTTP API: /healthz, /v1/events)\n\u251c\u2500\u2500 claude-mem-worker   (processes observations from the queue)\n\u2514\u2500\u2500 Valkey              (job queue)\n\nWorkspace container\n\u251c\u2500\u2500 CLAUDE_MEM_RUNTIME=server-beta\n\u251c\u2500\u2500 CLAUDE_MEM_SERVER_BETA_URL=http://host.docker.internal:37877\n\u251c\u2500\u2500 CLAUDE_MEM_SERVER_BETA_API_KEY=cmem_...\n\u251c\u2500\u2500 CLAUDE_MEM_SERVER_BETA_TEAM_ID / _PROJECT_ID\n\u2514\u2500\u2500 ~/.claude-mem/settings.json   (written by the entrypoint)\n</code></pre> <p>Credentials flow in three stages:</p> <ol> <li>Admin config (host-only, never forwarded): Postgres DSN, server URL, team name.</li> <li>Workspace bootstrap (per project, stored in <code>.vb</code>): a project-scoped bearer token    minted via a one-shot <code>postgres:16-alpine</code> container.</li> <li>Container launch: Vibrator reads both and forwards <code>CLAUDE_MEM_*</code> env vars; the    entrypoint writes    <code>~/.claude-mem/settings.json</code> and probes auth.</li> </ol>"},{"location":"integrations/claude-mem/#one-time-host-setup","title":"One-time host setup","text":"<p>Run once per machine:</p> <pre><code>vibrate integrations claude-mem\n</code></pre> <p>This creates <code>~/.config/vibrator/claude-mem.toml</code> (path printed in the UI):</p> <pre><code>runtime      = \"server-beta\"\nserver_url   = \"http://host.docker.internal:37877\"\ndatabase_url = \"postgres://user:pass@localhost:5432/claude_mem\"\nteam_name    = \"vibrators\"            # default\nstack_dir    = \"~/dev/claude-mem-stack\"  # default\n</code></pre> <p>The DSN never leaves the host</p> <p><code>database_url</code> is a host-only credential. It is never forwarded to any container \u2014 only the resulting bearer token crosses the boundary.</p>"},{"location":"integrations/claude-mem/#admin-config-path","title":"Admin config path","text":"<p>Resolved in order: <code>$VIBRATOR_CLAUDE_MEM_CONFIG</code> \u2192 <code>$XDG_CONFIG_HOME/vibrator/claude-mem.toml</code> \u2192 <code>~/.config/vibrator/claude-mem.toml</code>.</p>"},{"location":"integrations/claude-mem/#using-your-own-postgres","title":"Using your own Postgres","text":"<p>Set <code>database_url</code> to your existing instance. The bootstrap runs a one-shot <code>postgres:16-alpine</code> container that rewrites <code>localhost</code>/<code>127.0.0.1</code> \u2192 <code>host.docker.internal</code> so it can reach your host Postgres. You can skip <code>stack_dir</code> and run only the server/worker containers, pointing their <code>DATABASE_URL</code> at <code>host.docker.internal</code>.</p>"},{"location":"integrations/claude-mem/#per-workspace-bootstrap","title":"Per-workspace bootstrap","text":"<p>Run once per project (or accept the inline prompt at launch):</p> <pre><code>cd ~/my-project\nvibrate prereqs bootstrap claude-mem-server-beta\n</code></pre> <p>What it does:</p> <ol> <li>Spins up a one-shot <code>postgres:16-alpine</code> container.</li> <li>Upserts a team (<code>vibrators</code> by default, configurable in the admin config).</li> <li>Upserts a project (your workspace basename).</li> <li>Revokes any prior live key for this <code>(team, project, actor)</code> tuple.</li> <li>Inserts a fresh <code>cmem_&lt;48-hex&gt;</code> key, storing only its SHA-256 hash in Postgres.</li> <li>Saves <code>api_key</code>, <code>team_id</code>, <code>project_id</code>, <code>actor_id</code> to <code>.vb</code> under    <code>[prereqs.claude-mem-server-beta]</code>.</li> <li>Adds <code>.vb</code> to <code>.gitignore</code>.</li> </ol> <p>The actor identifier is <code>vibrator:&lt;hostname&gt;:&lt;workspace-path&gt;</code>, used in audit logs.</p> <p>To rotate a key:</p> <pre><code>vibrate prereqs bootstrap claude-mem-server-beta --force\n</code></pre>"},{"location":"integrations/claude-mem/#pre-launch-checks","title":"Pre-launch checks","text":"<p>When <code>claude-mem</code> is in the workspace's extensions, Vibrator runs three checks before entering the container:</p> Check Tests On failure <code>admin-config</code> <code>claude-mem.toml</code> exists with <code>server_url</code> warning + hint <code>server-probe</code> <code>GET &lt;server_url&gt;/healthz</code> returns 200 warning + hint <code>workspace-key</code> <code>.vb</code> has <code>[prereqs.claude-mem-server-beta].api_key</code> offer inline bootstrap <p>For a missing key you'll see:</p> <pre><code>\u26a0  [claude-mem] no workspace key \u2014 all auth'd requests will return 401\n   hint: a project-scoped bearer token must be minted against the host postgres\n   run:  vibrate prereqs bootstrap claude-mem-server-beta\n   Bootstrap now? [y/N]\n</code></pre> <p>Answering <code>y</code> mints the key inline, saves it to <code>.vb</code>, and proceeds with the launch.</p>"},{"location":"integrations/claude-mem/#inside-the-container","title":"Inside the container","text":"<p>The entrypoint's claude-mem step runs when <code>CLAUDE_MEM_RUNTIME=server-beta</code> and <code>CLAUDE_MEM_SERVER_BETA_URL</code> are set:</p> <ol> <li>Writes <code>~/.claude-mem/settings.json</code> from the env vars (idempotent \u2014 preserves existing    fields when a var is empty).</li> <li>Probes <code>GET &lt;server_url&gt;/healthz</code>.</li> <li>Auth-probes <code>POST &lt;server_url&gt;/v1/events</code> with the bearer token:</li> </ol> HTTP status Meaning Logged as 200\u2013202 auth OK, event accepted <code>claude-mem: auth OK</code> 400, 422 server reachable, token not rejected (bad/empty body \u2014 expected) verbose only 401, 403 key rejected \u2014 rotate it <code>claude-mem: WARNING \u2014 auth REJECTED</code> connection refused server not running skipped silently"},{"location":"integrations/claude-mem/#status-check","title":"Status check","text":"<pre><code>cd ~/my-project\nvibrate prereqs status\n</code></pre> <p>Reports the admin config path and fields, the server probe result, and the workspace cache (<code>api_key</code> prefix, <code>team_id</code>, <code>project_id</code>).</p>"},{"location":"integrations/claude-mem/#troubleshooting","title":"Troubleshooting","text":"<p>No <code>CLAUDE_MEM_*</code> vars in the container \u2014 Vibrator forwards them only when both the admin config and the workspace key exist. Check <code>cat ~/.config/vibrator/claude-mem.toml</code> and <code>grep -A5 prereqs .vb</code>, then run <code>vibrate prereqs status</code>.</p> <p><code>auth REJECTED</code> on startup \u2014 the key was revoked. Rotate and rebuild:</p> <pre><code>vibrate prereqs bootstrap claude-mem-server-beta --force\nvibrate --rebuild\n</code></pre> <p>Bootstrap: \"docker: command not found\" \u2014 the bootstrap needs Docker running on the host (it uses a one-shot <code>postgres:16-alpine</code> container).</p> <p>Bootstrap: psql connection error \u2014 if Postgres is on <code>localhost</code>/<code>127.0.0.1</code>, the DSN is rewritten to <code>host.docker.internal</code> automatically; ensure Postgres accepts connections from Docker's bridge network (<code>pg_hba.conf</code>).</p>"},{"location":"integrations/claude-mem/#related-pages","title":"Related pages","text":"<ul> <li>Integrations guide \u2014 hosting modes and readiness checks.</li> <li><code>vibrate prereqs</code> \u2014 status + bootstrap.</li> <li><code>vibrate integrations</code> \u2014 host admin config + stack.</li> <li><code>.vb</code> <code>[prereqs]</code> \u2014 where the key is stored.</li> </ul>"},{"location":"integrations/serena/","title":"Serena","text":"<p>Serena is a code-aware MCP server that gives the agent semantic access to your project \u2014 symbol search, find-references, go-to-definition, and more. Vibrator wires Serena into the Claude Code harness automatically.</p>"},{"location":"integrations/serena/#how-it-works","title":"How it works","text":"<p>Serena has a container-local fallback (stdio via <code>uvx</code>) and an optional host server (HTTP). On every session entry, the <code>claude-exec</code> wrapper decides which to use based on your hosting mode:</p> <pre><code>Container (claude-exec, every session entry)\n  \u2514\u2500 probes http://host.docker.internal:&lt;port&gt;/mcp\n       \u251c\u2500 reachable   \u2192 writes an http MCP entry in ~/.claude.json\n       \u2514\u2500 unreachable \u2192 writes a stdio MCP entry (uvx spawns Serena locally)\n</code></pre> <p>In <code>auto</code> mode (the default) the fallback is transparent \u2014 the agent always has Serena, whether or not a host server is running.</p>"},{"location":"integrations/serena/#setup","title":"Setup","text":""},{"location":"integrations/serena/#auto-mode-no-setup","title":"Auto mode (no setup)","text":"<p>Nothing to do. <code>claude-exec</code> spawns Serena locally via <code>uvx</code> on first use, so the agent gets symbol search out of the box. The trade-off is a brief <code>uvx</code> cold-start on each new session.</p>"},{"location":"integrations/serena/#host-server-mode-faster-persistent","title":"Host server mode (faster, persistent)","text":"<p>Running Serena as a persistent host process avoids the cold-start. Start it via Vibrator:</p> <pre><code>vibrate integrations serena\n# choose the \"process\" runtime  \u2192 Start   (background process, survives terminal close)\n# or the  \"docker\"  runtime     \u2192 Start   (container with --restart unless-stopped)\n</code></pre> <p>Or run it yourself (for systemd / launchd / screen):</p> <pre><code>uvx --from git+https://github.com/oraios/serena \\\n    serena start-mcp-server --transport=streamable-http \\\n    --host=0.0.0.0 --port=8765\n</code></pre> <p>Then pin the workspace to host mode:</p> <pre><code># .vb\n[integrations]\nserena = \"host\"\n</code></pre> <p>With <code>host</code> mode, a pre-launch check warns if the server is unreachable (there's no fallback in host mode).</p>"},{"location":"integrations/serena/#port","title":"Port","text":"<p>Serena listens on <code>8765</code> by default. Override it by setting <code>SERENA_PORT</code> on the host before starting Vibrator (and the host server). The same value is used host-side and in the container's <code>host.docker.internal:&lt;port&gt;</code> URL.</p> <p>The Docker runtime managed by <code>vibrate integrations serena</code> runs the container named <code>vibrate-serena</code> from <code>ghcr.io/astral-sh/uv</code> with the port bound to <code>127.0.0.1</code> and a persistent uv cache volume.</p>"},{"location":"integrations/serena/#pre-launch-check","title":"Pre-launch check","text":"Check Condition Severity <code>server-probe</code> mode = <code>host</code> and the server is unreachable warning <p>The check only runs in <code>host</code> mode. In <code>auto</code> mode an unreachable server simply falls back to stdio, silently.</p>"},{"location":"integrations/serena/#verifying-it-works","title":"Verifying it works","text":"<p>Inside the container:</p> <pre><code>jq '.mcpServers.serena' ~/.claude.json\n# {\"type\":\"http\", ...}  \u2192 host server in use\n# {\"type\":\"stdio\", ...} \u2192 local fallback (uvx)\n</code></pre> <p>With <code>VIBRATOR_VERBOSE=1</code>, <code>claude-exec</code> prints <code>serena: switched to http transport</code> or <code>serena: fell back to stdio</code> on each session entry.</p>"},{"location":"integrations/serena/#troubleshooting","title":"Troubleshooting","text":"<p>\"Serena server not reachable\"</p> <ul> <li>Check status: <code>vibrate integrations serena</code>.</li> <li>Ensure the host server binds <code>0.0.0.0</code>, not just <code>127.0.0.1</code>.</li> <li>Verify <code>host.docker.internal</code> resolves inside the container:   <code>ping host.docker.internal</code>.</li> </ul> <p>\"uvx not found\" inside the container</p> <ul> <li>The local fallback needs <code>uvx</code> on PATH. With <code>profile=minimal</code> (no Python feature),   install uv: <code>curl -LsSf https://astral.sh/uv/install.sh | sh</code>.</li> </ul> <p>Switching transports without rebuilding</p> <ul> <li><code>claude-exec</code> re-probes on every session entry, including <code>docker exec</code> re-entries. Start   the host server, then re-enter: <code>exit</code> the session and run <code>vibrate</code> again.</li> </ul>"},{"location":"integrations/serena/#related-pages","title":"Related pages","text":"<ul> <li>Integrations guide \u2014 hosting modes and transport switching.</li> <li><code>vibrate integrations</code> \u2014 managing the host server.</li> <li>Environment variables \u2014 <code>SERENA_PORT</code>.</li> </ul>"},{"location":"lifecycle/","title":"Lifecycle","text":"<p>This section describes, step by step, exactly what Vibrator does at the two moments that matter most:</p> <ul> <li> <p> What happens on build</p> <p>The deterministic five-stage Dockerfile generator \u2014 every layer, in order, from the Ubuntu base to the final <code>ENTRYPOINT</code>/<code>CMD</code>.</p> </li> <li> <p> What happens on start</p> <p>The <code>docker run</code> invocation, then the entrypoint and the per-session <code>claude-exec</code> wrapper \u2014 config merging, rules, settings, MCP wiring, and credential plumbing.</p> </li> <li> <p> Runtime detection</p> <p>How <code>vibrate</code> finds your Docker socket across Docker Desktop, OrbStack, Colima, Rancher Desktop, Podman, and native Linux.</p> </li> </ul>"},{"location":"lifecycle/#the-big-picture","title":"The big picture","text":"<pre><code>flowchart TD\n    subgraph resolve [Resolve]\n      A[Load .vb + flags] --&gt; B[Wizard if needed]\n      B --&gt; C[Resolve features + extensions]\n      C --&gt; D[Compute fingerprint \u2192 image tag + container name]\n    end\n    subgraph decide [Decide]\n      D --&gt; E{Container status?}\n    end\n    E -- running --&gt; X1[docker exec]\n    E -- exited/created/dead --&gt; X2[docker start \u2192 exec]\n    E -- none, image present --&gt; X3[docker run]\n    E -- none, no image --&gt; BUILD[docker build] --&gt; X3\n    X1 &amp; X2 &amp; X3 --&gt; ENTRY[Entrypoint + claude-exec] --&gt; AGENT([Agent / shell])</code></pre> <p>The Resolve and Decide phases live in the orchestrator (<code>internal/app</code>). The build happens only when no matching image exists (or you pass <code>--rebuild</code>). The startup wiring runs inside the container on every entry.</p>"},{"location":"lifecycle/#where-the-decision-is-made","title":"Where the decision is made","text":"<p><code>vibrate</code> inspects the container by its computed name and branches:</p> Container state What <code>vibrate</code> does Running <code>docker exec</code> straight in Exited / created / dead <code>docker start</code>, then <code>docker exec</code> Missing, image exists <code>docker run</code> (skips build) Missing, no image <code>docker build</code>, then <code>docker run</code> <code>--rebuild</code> passed Remove any container, rebuild <code>--no-cache</code>, run fresh <p>This is the entire reason the first run in a workspace is slow and every run after is instant: the expensive <code>docker build</code> only happens when the resolved variant has no image yet.</p>"},{"location":"lifecycle/#related-pages","title":"Related pages","text":"<ul> <li>What happens on build \u2014 the five-stage Dockerfile generator.</li> <li>What happens on start \u2014 the <code>docker run</code>, entrypoint, and <code>claude-exec</code> flow.</li> <li>Runtime detection \u2014 how the Docker socket is found.</li> <li>Troubleshooting \u2014 fixes when the lifecycle misbehaves.</li> </ul>"},{"location":"lifecycle/build/","title":"What happens on build","text":"<p>When <code>vibrate</code> needs an image \u2014 because no image exists for the resolved variant, or you passed <code>--rebuild</code> / ran <code>vibrate build</code> \u2014 it generates a deterministic Dockerfile and runs <code>docker build</code>. Same spec in, byte-identical Dockerfile out. That determinism is what powers golden-file tests, content-based image fingerprints, and <code>vibrate build-dockerfile</code> for inspection.</p> <p>You can see exactly what would be built, without building it:</p> <pre><code>vibrate build-dockerfile --harness=claude-code --profile=full --shell=zsh\n</code></pre>"},{"location":"lifecycle/build/#the-build-context","title":"The build context","text":"<p><code>vibrate</code> does not send your workspace to the Docker daemon as the build context. Instead it creates a small temporary directory containing only Vibrator-owned files, extracted from templates embedded in the binary:</p> <pre><code>&lt;tempdir&gt;/\n\u251c\u2500\u2500 shells/\n\u2502   \u251c\u2500\u2500 bashrc\n\u2502   \u251c\u2500\u2500 zshrc\n\u2502   \u2514\u2500\u2500 config.fish\n\u251c\u2500\u2500 scripts/\n\u2502   \u251c\u2500\u2500 entrypoint.sh\n\u2502   \u251c\u2500\u2500 claude-exec.sh\n\u2502   \u2514\u2500\u2500 welcome.sh\n\u2514\u2500\u2500 integrations.json     # per-harness MCP wiring (generated at build time)\n</code></pre> <p>Why a dedicated context</p> <p>Using your working directory as the build context would stream the whole project to the daemon on every build \u2014 wasteful \u2014 and a stray <code>Dockerfile</code> in your project could shadow the generated one. The workspace is mounted later, at <code>docker run</code> time, not <code>docker build</code> time.</p> <p>These build args are passed so file permissions on the mounted workspace match you:</p> <pre><code>--build-arg USERNAME=&lt;your sanitized host username&gt;\n--build-arg HOST_UID=&lt;your uid&gt;\n--build-arg HOST_GID=&lt;your gid&gt;\n</code></pre>"},{"location":"lifecycle/build/#the-five-stages","title":"The five stages","text":"<p>The generated Dockerfile is a multi-stage build. Each stage is a named cache boundary, so changing a feature doesn't rebuild the base toolkit, and changing an extension doesn't rebuild the harness.</p> <pre><code>flowchart LR\n    S1[1 \u00b7 base] --&gt; S2[2 \u00b7 features] --&gt; S3[3 \u00b7 harness] --&gt; S4[4 \u00b7 extensions] --&gt; S5[5 \u00b7 runtime]</code></pre> <p>The file begins with a <code># syntax=docker/dockerfile:1.7</code> directive (BuildKit is required \u2014 it enables the heredoc <code>RUN</code> blocks used for extension installs) and a self-documenting header comment recording the harness, profile, shell, features, extensions, and the exact <code>vibrate build-dockerfile</code> command that reproduces it.</p>"},{"location":"lifecycle/build/#stage-1-base","title":"Stage 1 \u2014 <code>base</code>","text":"<p><code>FROM ubuntu:24.04 AS base</code>. The always-on substrate every profile gets:</p> <ol> <li>System packages via <code>apt-get</code> \u2014 <code>ca-certificates curl wget</code>, <code>git gpg    openssh-client</code>, <code>sudo vim less tree</code>, <code>jq sqlite3 dnsutils</code>, <code>unzip xz-utils</code>,    <code>build-essential</code>, <code>locales</code>, zsh autosuggestions/syntax-highlighting,    <code>bash-completion</code>, plus your chosen shell.</li> <li>Shell rc files copied into <code>/etc/skel/</code> (<code>.bashrc</code>, <code>.zshrc</code>,    <code>.config/fish/config.fish</code>) \u2014 all three regardless of your login shell, so invoking    any shell inside the container doesn't trip its first-run wizard. <code>/etc/skel</code> means the    later <code>useradd -m</code> copies them into the user's home automatically.</li> <li>Vibrator scripts \u2014 <code>welcome.sh</code> into <code>/opt/vibrator/</code>, <code>entrypoint.sh</code> into    <code>/opt/vibrator/</code>, and the <code>claude-exec</code> wrapper into <code>/usr/local/bin/</code>.</li> <li>Integrations manifest copied to <code>/etc/vibrator/integrations.json</code>.</li> <li>Build sentinel \u2014 <code>echo \"&lt;build-id&gt;\" &gt; /etc/vibrator/build</code>, so you can identify the    image an old container came from (<code>cat /etc/vibrator/build</code>).</li> <li>Locale + PATH env \u2014 <code>LANG/LC_ALL=en_US.UTF-8</code>, <code>COLORTERM=truecolor</code>, and uv    install-dir hints (<code>UV_TOOL_BIN_DIR=/usr/local/bin</code>,    <code>UV_PYTHON_INSTALL_DIR=/opt/uv-python</code>).</li> <li>ripgrep (multi-arch binary release) and fd + fzf (apt).</li> </ol>"},{"location":"lifecycle/build/#stage-2-features","title":"Stage 2 \u2014 <code>features</code>","text":"<p><code>FROM base AS features</code>. For each resolved feature, in dependency order, the generator emits that feature's Dockerfile fragment with a <code># --- feature: &lt;id&gt; ---</code> banner. Fragments may be any Dockerfile directives \u2014 several use multi-stage <code>COPY --from</code> (Go and Node copy from the official images; Python installs via <code>uv</code>).</p> <p>At the end of this stage the unprivileged user is created \u2014 a deliberate privilege boundary:</p> <pre><code>ARG USERNAME=&lt;you&gt;\nARG HOST_UID=&lt;uid&gt;\nARG HOST_GID=&lt;gid&gt;\n\n# Remove any pre-existing user/group at the target UID/GID\n# (Ubuntu ships \"ubuntu\" at 1000, which usually clashes).\nRUN ... userdel ... groupdel ...\nRUN groupadd -g ${HOST_GID} ${USERNAME} \\\n &amp;&amp; useradd -m -s /bin/&lt;shell&gt; -u ${HOST_UID} -g ${HOST_GID} ${USERNAME} \\\n &amp;&amp; echo \"${USERNAME} ALL=(root) NOPASSWD:ALL\" &gt; /etc/sudoers.d/${USERNAME}\n\nUSER ${USERNAME}\nWORKDIR /home/${USERNAME}\n</code></pre> <p>Why everything before the USER switch runs as root</p> <p>System package installs and multi-stage copies need root. After the switch, the harness and extension installs run as you \u2014 so <code>claude.ai/install.sh</code> lands in <code>~/.local/bin</code>, plugins write to <code>~/.claude/</code>, and there's no unreadable-<code>/root</code> traversal problem at runtime.</p> <p>Immediately after the switch, the install-destination env vars are overridden to user-writable paths (this is critical \u2014 they persist from Stage 1, where they pointed at system dirs the unprivileged user can't write):</p> <pre><code>ENV NPM_CONFIG_PREFIX=/home/${USERNAME}/.npm-global\nENV UV_TOOL_BIN_DIR=/home/${USERNAME}/.local/bin\nENV PATH=/home/${USERNAME}/.npm-global/bin:/home/${USERNAME}/.local/bin:/usr/local/go/bin:...\n</code></pre>"},{"location":"lifecycle/build/#stage-3-harness","title":"Stage 3 \u2014 <code>harness</code>","text":"<p><code>FROM features AS harness</code>. Installs the selected harness binary by emitting its install fragment. Examples:</p> <ul> <li>claude-code \u2014 <code>curl -fsSL https://claude.ai/install.sh | bash</code>, then symlink into   <code>/usr/local/bin/claude</code>.</li> <li>codex \u2014 <code>npm install -g @openai/codex</code> (requires the <code>node</code> feature).</li> <li>opencode \u2014 architecture-aware download of the prebuilt binary from GitHub Releases.</li> <li>pi \u2014 <code>npm install -g @mariozechner/pi-coding-agent</code> (requires <code>node</code>).</li> </ul>"},{"location":"lifecycle/build/#stage-4-extensions","title":"Stage 4 \u2014 <code>extensions</code>","text":"<p><code>FROM harness AS extensions</code>. For each selected extension, sorted by ID for determinism, the generator wraps the extension's <code>install:</code> shell snippet in a BuildKit heredoc <code>RUN</code> block:</p> <pre><code># --- extensions/claude-code/context7 (mcp) ---\n# Source: https://github.com/upstash/context7\nRUN &lt;&lt;'VIBRATE_EXT_INSTALL'\nset -e\nclaude mcp add context7 --scope user --transport http https://mcp.context7.com/mcp\nVIBRATE_EXT_INSTALL\n</code></pre> <p>The unusual delimiter (<code>VIBRATE_EXT_INSTALL</code>) avoids collisions with <code>EOF</code> heredocs inside the snippets themselves; the generator refuses to build if a snippet uses the reserved delimiter on its own line. <code>set -e</code> makes a failed command abort the build.</p>"},{"location":"lifecycle/build/#stage-5-runtime","title":"Stage 5 \u2014 <code>runtime</code>","text":"<p><code>FROM extensions AS runtime</code>. No more installs \u2014 this stage stamps metadata and sets the default command:</p> <ol> <li>Auth env-var declarations \u2014 each of the harness's auth env vars is declared empty    (<code>ENV ANTHROPIC_API_KEY=\"\"</code>). Real values are injected at <code>docker run</code>, never baked in.</li> <li>Variant metadata as env vars, readable inside the container \u2014 <code>VIBRATOR_HARNESS</code>,    <code>VIBRATOR_PROFILE</code>, <code>VIBRATOR_FEATURES_LIST</code>, <code>VIBRATOR_EXTENSIONS_LIST</code>,    <code>VIBRATOR_VERSION</code>, <code>VIBRATOR_BUILD_ID</code>.</li> <li>Labels for image management \u2014 <code>vibrator.version</code>, <code>vibrator.build_id</code>,    <code>vibrator.harness</code>, <code>vibrator.profile</code>, <code>vibrator.shell</code>, <code>vibrator.features</code>,    <code>vibrator.extensions</code>. These drive <code>vibrate variants list</code>.</li> <li><code>ENTRYPOINT [\"/opt/vibrator/entrypoint.sh\"]</code> \u2014 runs the host-config merge on every    container start (see What happens on start).</li> <li><code>CMD [\"/usr/local/bin/claude-exec\", \"/bin/&lt;shell&gt;\"]</code> \u2014 the default command, wrapped    so the per-session integration probes fire even if someone runs the image directly with    plain <code>docker run</code>.</li> </ol>"},{"location":"lifecycle/build/#build-invocation","title":"Build invocation","text":"<p>The orchestrator pipes the generated Dockerfile to <code>docker build</code> via stdin (<code>-f -</code>) with the temp build context, the computed image tag, and the three build args above. <code>--no-cache</code> is added when you pass <code>--rebuild</code> (or <code>vibrate build --no-cache</code>).</p>"},{"location":"lifecycle/build/#result","title":"Result","text":"<p>A tagged image like <code>vb-claude-code-full-you-a1b2c3d4:latest</code>. From here the orchestrator moves on to running the container.</p>"},{"location":"lifecycle/build/#related-pages","title":"Related pages","text":"<ul> <li><code>vibrate build</code> \u2014 build without running.</li> <li><code>vibrate build-dockerfile</code> \u2014   emit the Dockerfile for inspection.</li> <li>Features and Profiles \u2014 what   goes into Stage 2.</li> <li>Naming &amp; labels \u2014 how the image tag is computed.</li> </ul>"},{"location":"lifecycle/runtime-detection/","title":"Runtime detection","text":"<p>Vibrator talks to Docker by shelling out to the <code>docker</code> CLI \u2014 there's no Docker SDK dependency. To do that reliably across the many ways people run Docker on macOS and Linux, it auto-detects the Docker socket. You can inspect what it found with:</p> <pre><code>vibrate runtime detect\n</code></pre> <pre><code>Runtime: orbstack\nSocket:  /Users/you/.orbstack/run/docker.sock\nSource:  socket-scan\n</code></pre>"},{"location":"lifecycle/runtime-detection/#detection-order","title":"Detection order","text":"<p>The socket is resolved by the first of these that succeeds:</p> <ol> <li>Explicit override \u2014 the <code>--docker-socket=&lt;path&gt;</code> flag or the    <code>VIBRATOR_DOCKER_SOCKET</code> environment variable.</li> <li><code>$DOCKER_HOST</code> \u2014 when it's a <code>unix://</code> socket (non-unix schemes like <code>tcp://</code> and    <code>ssh://</code> are rejected).</li> <li>Active <code>docker context</code> \u2014 the endpoint of <code>docker context inspect</code>.</li> <li>Well-known socket paths \u2014 scanned in order (see below).</li> <li>Fallback \u2014 <code>/var/run/docker.sock</code>.</li> </ol>"},{"location":"lifecycle/runtime-detection/#recognized-runtimes","title":"Recognized runtimes","text":"Runtime Socket path probed Docker Desktop <code>~/.docker/run/docker.sock</code> OrbStack <code>~/.orbstack/run/docker.sock</code> Colima <code>~/.colima/&lt;profile&gt;/docker.sock</code> Rancher Desktop <code>~/.rd/docker.sock</code> Podman <code>~/.local/share/containers/podman/machine/podman.sock</code> Native Linux <code>/var/run/docker.sock</code> <p>The runtime name is identified from the matched socket path. A custom path you supply yourself is reported as <code>custom</code>.</p>"},{"location":"lifecycle/runtime-detection/#colima-profiles","title":"Colima profiles","text":"<p>Colima can run multiple VMs (\"profiles\"). Vibrator picks the profile from, in order, the <code>--colima-profile=&lt;name&gt;</code> flag, the <code>COLIMA_PROFILE</code> environment variable, or <code>default</code>:</p> <pre><code>vibrate runtime detect --colima-profile=work\n</code></pre>"},{"location":"lifecycle/runtime-detection/#vm-based-runtimes-and-dind","title":"VM-based runtimes and <code>--dind</code>","text":"<p>For VM-based runtimes (Colima, Rancher Desktop), the macOS-side socket is a proxy that only listens on the host. When you use <code>--dind</code> to mount the socket into a container, Vibrator mounts the daemon's own <code>/var/run/docker.sock</code> (which the daemon, running inside the VM, resolves to its real socket) and the <code>docker-cli</code> feature's sudo wrapper handles group access.</p>"},{"location":"lifecycle/runtime-detection/#overriding-detection","title":"Overriding detection","text":"<p>If detection picks the wrong socket \u2014 unusual multi-runtime setups, remote daemons exposed locally, etc. \u2014 force it:</p> <pre><code># One-off:\nvibrate runtime detect --docker-socket=/custom/path/docker.sock\n\n# Persistent, for the whole session:\nexport VIBRATOR_DOCKER_SOCKET=/custom/path/docker.sock\n</code></pre>"},{"location":"lifecycle/runtime-detection/#related-pages","title":"Related pages","text":"<ul> <li><code>vibrate runtime detect</code> \u2014 the command reference.</li> <li>Docker-in-Docker \u2014 <code>--dind</code> and socket mounting.</li> <li>Environment variables \u2014 <code>VIBRATOR_DOCKER_SOCKET</code>,   <code>DOCKER_HOST</code>, <code>COLIMA_PROFILE</code>.</li> <li>Troubleshooting \u2014 fixes for \"no Docker runtime found\" and DinD on   VM-based runtimes.</li> </ul>"},{"location":"lifecycle/startup/","title":"What happens on start","text":"<p>Once an image exists, <code>vibrate</code> brings the container up and wires it into your host. There are three layers of runtime behavior, in order:</p> <ol> <li>The <code>docker run</code> invocation \u2014 flags, mounts, and forwarded environment.</li> <li>The entrypoint (<code>entrypoint.sh</code>) \u2014 runs once when the container is created.</li> <li>The <code>claude-exec</code> wrapper \u2014 runs on every session entry (<code>docker run</code> and    every <code>docker exec</code> re-entry), then your shell's rc and the welcome banner.</li> </ol> <pre><code>flowchart TD\n    RUN[docker run -it ...] --&gt; EP[ENTRYPOINT: entrypoint.sh&lt;br/&gt;once per container]\n    EP --&gt; CE[CMD: claude-exec&lt;br/&gt;every session]\n    CE --&gt; RC[shell rc + welcome banner]\n    RC --&gt; AGENT([harness CLI or shell])\n    EXEC[docker exec -it ...&lt;br/&gt;re-entry] --&gt; CE</code></pre> <p>Entrypoint vs. exec</p> <p><code>docker exec</code> does not re-run the entrypoint. That's why per-session wiring (integration transport probes, workspace <code>cd</code>, trust acceptance) lives in <code>claude-exec</code> instead \u2014 so re-entering an already-running container still picks up host-side changes.</p>"},{"location":"lifecycle/startup/#1-the-docker-run-invocation","title":"1. The <code>docker run</code> invocation","text":"<p>For a normal launch the orchestrator runs the container interactively (<code>-it</code>) so you drop into a session and control returns when you exit. The key flags:</p> Flag Value Why <code>--name</code> computed container name enables later <code>docker exec</code> re-entry <code>--hostname</code> <code>vibrate-&lt;workspace-basename&gt;</code> makes the shell prompt visibly \"inside a box\" <code>--workdir</code> the workspace path land in the project, not <code>$HOME</code> <code>--init</code> (tini as PID 1) reaps zombie processes, forwards <code>Ctrl-C</code> correctly <code>--shm-size</code> <code>2g</code> Docker's default 64 MB crashes Chromium/Playwright <code>--add-host</code> <code>host.docker.internal:host-gateway</code> container can reach host services <code>--label</code> <code>vibrator.*</code> discoverable by <code>vibrate variants</code> <p>The default command is the harness CLI (or your shell for <code>vibrate shell</code>), always prefixed with <code>/usr/local/bin/claude-exec</code> so the per-session wiring runs first.</p>"},{"location":"lifecycle/startup/#mounts","title":"Mounts","text":"# Host source Container target Mode Condition \u2014 the workspace same absolute path rw always D1 <code>~/.claude.json</code> <code>~/.claude.host.json</code> ro claude-code, file exists D2 <code>~/.claude/settings.json</code> <code>~/.claude/settings.host.json</code> ro claude-code, file exists D3 <code>~/.claude/rules/</code> <code>~/.claude/rules-host/</code> ro claude-code, dir exists D4 <code>~/.claude/hooks/</code> <code>~/.claude/hooks/</code> rw claude-code, dir exists D5a <code>~/.claude/projects/&lt;encoded-cwd&gt;/</code> same (scoped) rw claude-code D5b <code>~/.claude/{file-history,sessions,tasks,paste-cache}</code> same rw claude-code D6 <code>~/.aws</code> <code>~/.aws</code> ro dir exists D7 <code>~/.cache/vibrator/claude-mem/&lt;wsHash&gt;</code> <code>~/.claude-mem/cache</code> rw claude-mem extension selected D8 host Docker socket <code>/var/run/docker.sock</code> rw <code>--dind</code> D9 gpg-agent extra-socket <code>/gpg-agent-extra</code> rw gpg <code>extra-socket</code> configured <p>The workspace mount at the same absolute path is the foundational design choice \u2014 it makes paths in errors, stack traces, and <code>pwd</code> identical inside and out. The Claude state mounts (D1\u2013D5) give you session continuity and let the entrypoint seed the container's <code>~/.claude/</code> from your host config. The <code>projects/</code> mount is scoped to only this workspace's transcript subdirectory so other projects' histories don't leak in. See Authentication and Docker-in-Docker for D8/D9 details.</p>"},{"location":"lifecycle/startup/#forwarded-environment","title":"Forwarded environment","text":"<p><code>WORKSPACE_PATH</code> is always set. On top of it, env vars are layered (later wins on collision):</p> <ol> <li>Harness auth env vars \u2014 host values for e.g. <code>CLAUDE_CODE_OAUTH_TOKEN</code>,    <code>ANTHROPIC_API_KEY</code>, <code>OPENAI_API_KEY</code> (forwarded only when set on the host).</li> <li>LLM-derived vars from <code>[llm]</code> (for provider-agnostic    harnesses) \u2014 the harness maps your provider/model/key into the right shape.</li> <li>Extension <code>auth.env</code> vars \u2014 host values declared by selected extensions.</li> <li><code>[env]</code> overrides from <code>.vb</code> \u2014 literal values, or <code>$NAME</code> indirection resolved    against the host environment at run time.</li> <li><code>VIBRATOR_INTEGRATION_MODE_&lt;ID&gt;</code> \u2014 your per-integration    hosting mode.</li> <li><code>CLAUDE_MEM_*</code> \u2014 when claude-mem is bootstrapped    for this workspace.</li> </ol> <p>See the Environment variables reference for the complete list.</p>"},{"location":"lifecycle/startup/#2-the-entrypoint-entrypointsh","title":"2. The entrypoint (<code>entrypoint.sh</code>)","text":"<p>Runs once, as PID 1, before exec'ing your command. It is POSIX <code>sh</code>, tolerates every input being missing, and never blocks startup on a failure. Set <code>VIBRATOR_VERBOSE=1</code> to see what each step did. In order:</p> <ol> <li>Create the workspace parent dir \u2014 if the workspace's parent doesn't exist, <code>sudo    mkdir -p</code> it so <code>git clone ..</code> / <code>cd ..</code> flows work.</li> <li><code>cd</code> to the workspace \u2014 belt-and-suspenders alongside <code>--workdir</code>.</li> <li>Merge host Claude config \u2014 extract the OAuth/onboarding subset from    <code>~/.claude.host.json</code> (read-only host mount) and deep-merge it into the container's    writable <code>~/.claude.json</code>, so the agent doesn't re-prompt onboarding.</li> <li>Copy host rules \u2014 copy every <code>*.md</code> from <code>~/.claude/rules-host/</code> into    <code>~/.claude/rules/</code>. Done on every start, so editing a rule on the host takes effect    next launch with no rebuild.</li> <li>Generate a user-identity rule \u2014 write <code>~/.claude/rules/user-identity.md</code> telling the    agent to use your real username in generated code instead of <code>your-name</code> placeholders.</li> <li>Merge <code>settings.json</code> \u2014 the trickiest step. Snapshot any plugin hooks baked into the    image, copy host <code>settings.host.json</code>, rewrite macOS-style <code>/Users/.../\u200b.claude/</code> hook    paths to the container's <code>$HOME/.claude/</code>, then merge the baked hooks back per-event so    both host and plugin hooks fire.</li> <li>Ensure baseline files \u2014 create empty <code>~/.claude.json</code> / <code>~/.claude/settings.json</code>    if still missing.</li> <li>claude-mem auth probe \u2014 when <code>CLAUDE_MEM_RUNTIME=server-beta</code>, write    <code>~/.claude-mem/settings.json</code> from the forwarded env, probe <code>/healthz</code>, then    <code>POST /v1/events</code> with the bearer token and surface the result (<code>auth OK</code> /    <code>auth REJECTED</code>). See claude-mem.</li> <li>Re-permission plugin hooks \u2014 <code>chmod +x</code> every <code>*.sh</code> under <code>~/.claude/plugins/</code>    (bind mounts can strip the executable bit).</li> <li>Link the GPG agent socket \u2014 if <code>/gpg-agent-extra</code> is mounted, symlink it where gpg     expects, so <code>git commit -S</code> uses your host key without it ever leaving the host.</li> <li>Prune per-profile MCPs \u2014 drop <code>mcpServers</code> entries (playwright, serena, claude-mem)     whose feature isn't in <code>VIBRATOR_FEATURES_LIST</code>, so a lean profile doesn't spawn them.</li> <li>Re-enable baked plugins \u2014 merge plugins installed at build time back into     <code>settings.json</code>'s <code>enabledPlugins</code> (step 6's host copy would otherwise hide them).</li> <li>De-duplicate integration MCPs \u2014 disable any Claude Code plugin whose name collides     with an integration-managed MCP, so the host-aware integration is the single source.</li> <li>Skip hooks needing a missing tool \u2014 strip any hook whose command shells out to a     tool that isn't on <code>PATH</code> (checked with <code>command -v</code>), logging which tools were skipped.     Stops node/python hooks from erroring on every event under a lean profile. See     Missing-tool hooks.</li> <li>Drop the readiness sentinel \u2014 <code>touch /tmp/.vibrator-entrypoint-done</code> (used by     <code>--login</code>), then <code>exec \"$@\"</code>.</li> </ol>"},{"location":"lifecycle/startup/#3-the-claude-exec-wrapper","title":"3. The <code>claude-exec</code> wrapper","text":"<p><code>claude-exec</code> runs on every session entry \u2014 it's the <code>CMD</code> prefix on <code>docker run</code> and the exec command on every <code>docker exec</code>. It refreshes anything that can change between sessions, then execs your real command. It uses no <code>set -e</code> (a probe failure must never block your shell). In order:</p> <ol> <li>Accept the workspace trust dialog \u2014 set    <code>projects[&lt;workspace&gt;].hasTrustDialogAccepted = true</code> in <code>~/.claude.json</code> (the    workspace is the whole point of the container).</li> <li> <p>Process the integrations manifest \u2014 for each entry in    <code>/etc/vibrator/integrations.json</code> matching the current harness, resolve its    hosting mode and wire the MCP transport into    <code>~/.claude.json</code>:</p> <ul> <li><code>off</code> \u2014 remove the entry.</li> <li><code>local</code> \u2014 write the stdio command unconditionally.</li> <li><code>host</code> \u2014 write the http entry; warn loudly (no fallback) if unreachable.</li> <li><code>auto</code> (default) \u2014 probe the http URL; on success write http, otherwise fall back   to stdio with a visible warning.</li> </ul> <p>Any env vars the entry declares are exported into the session. 3. <code>cd</code> to the workspace \u2014 re-applied here because <code>docker exec</code> skips the entrypoint. 4. <code>exec \"$@\"</code> \u2014 replace the wrapper with the harness CLI (or your shell).</p> </li> </ol>"},{"location":"lifecycle/startup/#shell-rc-and-the-welcome-banner","title":"Shell rc and the welcome banner","text":"<p>After <code>claude-exec</code> execs your shell, its rc file (<code>~/.bashrc</code> / <code>~/.zshrc</code> / <code>config.fish</code>) runs:</p> <ul> <li>History sizing, a color-coded prompt showing <code>you@vibrate-&lt;workspace&gt;</code>, and convenience   aliases \u2014 notably <code>claude</code> aliased to <code>claude --dangerously-skip-permissions</code> (the   container is the sandbox; <code>claude-safe</code> is the escape hatch back to the normal   permission flow).</li> <li><code>GIT_SSH_COMMAND</code> set to skip host-key prompts on first clone (the sandbox is ephemeral).</li> <li>Sourcing <code>/opt/vibrator/welcome.sh</code>, which prints the banner: detected harness CLIs +   versions, auth status, profile, tools, extensions, and the workspace path. Silence it   with <code>VIBRATOR_NO_BANNER=1</code>.</li> </ul>"},{"location":"lifecycle/startup/#missing-tool-hooks","title":"Missing-tool hooks","text":"<p>Claude Code hooks are shell commands in <code>~/.claude/settings.json</code>. If a hook shells out to a tool the image doesn't install \u2014 e.g. a <code>node</code>-based formatter hook under the <code>minimal</code> profile \u2014 it fails on every matching event with <code>node: not found</code>. Vibrator handles this on two independent levels:</p> <p>At launch (host). Before building or running, <code>vibrate</code> scans your host <code>~/.claude/settings.json</code> for hooks that need a tool not in the resolved feature set. On an interactive run it prompts per gap:</p> <pre><code>\u26a0  [hooks] 2 hook(s) call `node`, installed by `node` \u2014 not in this image\n     e.g. node /Users/you/.claude/hooks/format.js\n     The container will skip them. Install `node` and rebuild? [y/N]\n</code></pre> <ul> <li>y \u2192 adds the feature to <code>.vb</code> <code>with</code>, so the next build bakes it and the hooks run.</li> <li>N \u2192 records the choice in <code>.vb</code> <code>[hooks]</code> so you're   not asked again for that tool.</li> <li>Non-interactive runs (CI, pipes) just print a one-line warning and continue.</li> </ul> <p>At runtime (container). The entrypoint strips any hook whose tool isn't actually on <code>PATH</code>, so the agent never sees the per-event error. Because it checks real availability (<code>command -v</code>), a tool installed by some other means is respected. This layer also covers hooks installed by plugins/extensions and non-interactive runs. Set <code>VIBRATOR_VERBOSE=1</code> (or watch for the one-line <code>hooks: skipped \u2026</code> summary) to see what was removed.</p> <p>The two layers are independent: the launch prompt offers the install path; the entrypoint guard is the always-on safety net that silences the noise either way.</p>"},{"location":"lifecycle/startup/#the-login-variant","title":"The <code>--login</code> variant","text":"<p><code>vibrate --login</code> changes the run flow: the container starts detached with <code>sleep infinity</code> so the entrypoint can finish its full setup, <code>vibrate</code> polls for the readiness sentinel, then it execs <code>claude auth login</code>, intercepts the OAuth URL and opens it in your host browser, writes the resulting auth back to your host <code>~/.claude.json</code>, and finally execs the harness. Every other path (<code>running</code>, <code>exited</code>, plain <code>run</code>) gets the login step injected at the right moment.</p>"},{"location":"lifecycle/startup/#related-pages","title":"Related pages","text":"<ul> <li>What happens on build \u2014 how the image being run was produced.</li> <li>Authentication \u2014 auth env vars, <code>--login</code>, GPG, AWS.</li> <li>Integrations \u2014 hosting modes and transport switching.</li> <li>Environment variables \u2014 every forwarded var.</li> </ul>"},{"location":"reference/","title":"Reference","text":"<p>Canonical, exhaustive reference material. For task-oriented walkthroughs, see the Guides; for per-command details, see the CLI reference.</p> <ul> <li> <p> .vb schema</p> <p>Every field of the pin file: type, TOML key, default, and meaning.</p> </li> <li> <p> Profiles</p> <p>The four profiles and their exact feature bundles.</p> </li> <li> <p> Features</p> <p>Every build-time feature, its dependencies, size, and what it installs.</p> </li> <li> <p> Environment variables</p> <p>Vars Vibrator reads on the host, forwards into the container, and bakes into images.</p> </li> <li> <p> Naming &amp; labels</p> <p>How image tags, container names, hostnames, fingerprints, and labels are computed.</p> </li> <li> <p> Architecture</p> <p>The internal module map and the key design decisions.</p> </li> </ul>"},{"location":"reference/#related-pages","title":"Related pages","text":"<ul> <li>Guides \u2014 task-oriented walkthroughs of the same material.</li> <li>CLI reference \u2014 every <code>vibrate</code> subcommand and its flags.</li> <li>Core concepts \u2014 the vocabulary behind these tables.</li> </ul>"},{"location":"reference/architecture/","title":"Architecture","text":"<p>A map of how Vibrator is put together internally, for contributors and the curious. The code is a single Go module (<code>github.com/wlame/vibrator</code>) with a thin <code>main</code> and an <code>internal/</code> tree of focused packages.</p>"},{"location":"reference/architecture/#module-map","title":"Module map","text":"Package Responsibility <code>cmd/vibrate</code> Thin <code>main</code> \u2014 wires the Cobra root command. <code>internal/cli</code> Every subcommand in its own file (run, build, wizard, extensions, \u2026). <code>internal/app</code> The orchestrator. The full decision tree: load pin \u2192 flags \u2192 wizard \u2192 validate \u2192 save \u2192 resolve specs \u2192 readiness checks \u2192 local-LLM startup \u2192 build/run/exec. Topmost package; imports everything below. <code>internal/wizard</code> The charmbracelet/huh form chain with adaptive gating. <code>internal/config</code> The <code>.vb</code> TOML loader/writer and the <code>Pin</code> struct. <code>internal/harness</code> The <code>Harness</code> interface + one implementation per built-in agent. <code>internal/feature</code> Base features and the topological dependency resolver. <code>internal/profile</code> The minimal/backend/frontend/full bundles over features. <code>internal/extensions</code> Markdown + YAML-frontmatter loader for the per-harness catalogue. <code>internal/dockerfile</code> The deterministic five-stage Dockerfile generator + build context. <code>internal/docker</code> A <code>Client</code> interface over the <code>docker</code> CLI (production + mock). <code>internal/runtime</code> Docker socket auto-detection across runtimes. <code>internal/workspace</code> Variant fingerprint, image/container/hostname naming. <code>internal/integration</code> Integration registry + runtimes (process / docker / compose) + serena/claude-mem. <code>internal/hostprobe</code> Scans the host for installed harness plugins. <code>internal/prereq</code> Host-side prereq verifiers/bootstrappers (HTTP/command/file probes; claude-mem). <code>internal/localprovider</code> Ollama / LM Studio lifecycle. <code>internal/migrate</code> Bash <code>.vb.env</code> \u2192 TOML <code>.vb</code> converter. <p>The catalogue (<code>extensions/</code>) and runtime scripts/shells (<code>templates/</code>) are embedded into the binary.</p>"},{"location":"reference/architecture/#the-orchestration-flow","title":"The orchestration flow","text":"<pre><code>flowchart TD\n    A[load .vb + workspace root] --&gt; B[apply CLI flag overrides]\n    B --&gt; C{harness set?}\n    C -- no --&gt; D[run wizard]\n    C -- yes --&gt; E\n    D --&gt; E[validate pin]\n    E --&gt; F[persist .vb + gitignore]\n    F --&gt; G[resolve dockerfile + workspace specs]\n    G --&gt; H[fingerprint \u2192 image tag + container name]\n    H --&gt; I[integration readiness checks]\n    I --&gt; J[ensure local LLM provider running]\n    J --&gt; K[resolveAndLaunch: build / run / exec]</code></pre> <p>Each step is a small function in <code>internal/app</code>. The terminal operations (build, run, exec, login) are indirection seams, so the decision logic can be unit-tested against a mock <code>docker.Client</code> without touching a real daemon.</p>"},{"location":"reference/architecture/#key-design-decisions","title":"Key design decisions","text":"Decision Choice Why Docker integration Shell out to the <code>docker</code> CLI No SDK dependency; works with any Docker-compatible runtime. <code>.vb</code> format TOML (<code>BurntSushi/toml</code>) Human-readable, hand-editable, stable diffs. Extensions format Markdown + YAML frontmatter, one file per item \"Add a plugin\" is a docs change, not Go code. Harness extensibility Built-in Go interface A new harness is a reviewed PR; the surface stays small and typed. Wizard <code>charmbracelet/huh</code>, gated step-by-step Skips anything already supplied via flags/pin. Variant identity SHA-256 prefix over the canonical spec Order-independent, reproducible image/container naming. Container reuse Single-path workspace mount + label-driven discovery Paths match host; objects are discoverable and prunable. Determinism Byte-identical Dockerfile per spec Golden tests, content fingerprints, inspectable builds. Release goreleaser \u2192 GitHub Releases Cross-platform binaries + checksums from tag pushes."},{"location":"reference/architecture/#embedded-assets","title":"Embedded assets","text":"<p>Two <code>embed.FS</code> trees are compiled into the binary:</p> <ul> <li><code>extensions/</code> \u2014 the per-harness catalogue (<code>&lt;harness&gt;/&lt;id&gt;.md</code>), loaded by   <code>internal/extensions</code>.</li> <li><code>templates/</code> \u2014 shell rc files (<code>shells/</code>) and runtime scripts (<code>scripts/</code>:   <code>entrypoint.sh</code>, <code>claude-exec.sh</code>, <code>welcome.sh</code>), extracted into the   build context.</li> </ul> <p>Consistency tests guard these \u2014 e.g. every registered harness must have an <code>extensions/</code> directory, every entry's <code>deps.features</code> must resolve, and the <code>ecc-*</code> commit pins must agree.</p>"},{"location":"reference/architecture/#adding-things","title":"Adding things","text":"To add\u2026 Do\u2026 an extension / MCP / skill drop a <code>.md</code> file under <code>extensions/&lt;harness&gt;/</code> a harness implement the <code>Harness</code> interface + register it (PR) a feature append a <code>Feature</code> (with Dockerfile fragment) to the registry an integration add a descriptor that self-registers, with <code>LaunchChecks</code>"},{"location":"reference/architecture/#related-pages","title":"Related pages","text":"<ul> <li>What happens on build / on start.</li> <li>Harnesses \u00b7 Features \u00b7   Extensions \u00b7 Integrations.</li> </ul>"},{"location":"reference/environment-variables/","title":"Environment variables","text":"<p>A reference for every environment variable Vibrator reads on the host, forwards into the container, or bakes into images.</p>"},{"location":"reference/environment-variables/#read-by-vibrator-on-the-host","title":"Read by Vibrator on the host","text":"<p>These influence how <code>vibrate</code> behaves on your machine.</p> Variable Used for Default <code>VIBRATOR_DOCKER_SOCKET</code> Force the Docker socket path (highest priority). \u2014 <code>DOCKER_HOST</code> Docker socket when it's a <code>unix://</code> URL. \u2014 <code>COLIMA_PROFILE</code> Colima VM profile to probe. <code>default</code> <code>SERENA_PORT</code> Port the Serena integration uses. <code>8765</code> <code>VIBRATOR_CLAUDE_MEM_CONFIG</code> Override the claude-mem admin config path (<code>~/.config/vibrator/claude-mem.toml</code>). \u2014 <code>VIBRATOR_INTEGRATIONS_DIR</code> Override the directory scanned for user-defined integration descriptors. \u2014 <code>XDG_CONFIG_HOME</code> Base for config dirs (e.g. <code>claude-mem.toml</code>). <code>~/.config</code> <code>XDG_DATA_HOME</code> Base for data dirs (Serena PID/log). <code>~/.local/share</code> <p>See Runtime detection for the socket-related vars.</p>"},{"location":"reference/environment-variables/#forwarded-into-the-container","title":"Forwarded into the container","text":"<p>Set at <code>docker run</code> (see What happens on start). Listed by source, in precedence order (later wins on name collision).</p> Variable(s) Source <code>WORKSPACE_PATH</code> always \u2014 the workspace absolute path Harness auth vars (<code>CLAUDE_CODE_OAUTH_TOKEN</code>, <code>ANTHROPIC_API_KEY</code>, <code>OPENAI_API_KEY</code>, \u2026) forwarded from host when set \u2014 see Authentication LLM-derived vars (<code>OPENAI_API_KEY</code>, <code>OPENAI_BASE_URL</code>, provider keys, \u2026) computed from <code>[llm]</code> Extension <code>auth.env</code> vars declared by selected extensions <code>[env]</code> overrides <code>.vb</code> <code>[env]</code> \u2014 literal or <code>$NAME</code> indirection <code>VIBRATOR_INTEGRATION_MODE_&lt;ID&gt;</code> per-integration hosting mode <code>CLAUDE_MEM_RUNTIME</code>, <code>CLAUDE_MEM_SERVER_BETA_URL</code>, <code>CLAUDE_MEM_SERVER_BETA_API_KEY</code>, <code>CLAUDE_MEM_SERVER_BETA_TEAM_ID</code>, <code>CLAUDE_MEM_SERVER_BETA_PROJECT_ID</code> claude-mem when bootstrapped <p>Integration mode var naming</p> <p><code>VIBRATOR_INTEGRATION_MODE_&lt;ID&gt;</code> upper-cases the integration ID and replaces non-alphanumerics with <code>_</code> \u2014 so <code>claude-mem</code> becomes <code>VIBRATOR_INTEGRATION_MODE_CLAUDE_MEM</code>.</p> <p>The claude-mem DSN never crosses the boundary</p> <p>The Postgres <code>database_url</code> (DSN) from the claude-mem admin config is host-only. Only the <code>CLAUDE_MEM_SERVER_BETA_*</code> token/IDs above are forwarded into the container \u2014 the DSN itself is never set as a container env var.</p>"},{"location":"reference/environment-variables/#baked-into-the-image","title":"Baked into the image","text":"<p>Set in the Dockerfile runtime stage; readable inside the container.</p> Variable Value <code>VIBRATOR_HARNESS</code> harness ID <code>VIBRATOR_PROFILE</code> profile ID <code>VIBRATOR_FEATURES_LIST</code> comma-separated resolved features <code>VIBRATOR_EXTENSIONS_LIST</code> comma-separated extensions (or <code>(none)</code>) <code>VIBRATOR_VERSION</code> vibrator version (or <code>dev</code>) <code>VIBRATOR_BUILD_ID</code> build sentinel (also at <code>/etc/vibrator/build</code>) <code>LANG</code>, <code>LC_ALL</code> <code>en_US.UTF-8</code> <code>COLORTERM</code> <code>truecolor</code> <code>PATH</code>, <code>NPM_CONFIG_PREFIX</code>, <code>UV_TOOL_BIN_DIR</code>, <code>UV_PYTHON_INSTALL_DIR</code> install/lookup paths"},{"location":"reference/environment-variables/#read-by-in-container-scripts","title":"Read by in-container scripts","text":"<p>These control the entrypoint and <code>claude-exec</code> wrapper behavior.</p> Variable Effect <code>VIBRATOR_VERBOSE=1</code> Print <code>[vibrator] ...</code> diagnostics for each setup step. <code>VIBRATOR_NO_BANNER=1</code> Suppress the welcome banner. <p>Debugging startup</p> <p>Set <code>VIBRATOR_VERBOSE=1</code> before launching to see exactly which env vars and setup steps the entrypoint runs \u2014 handy when an integration or forwarded credential isn't behaving.</p>"},{"location":"reference/environment-variables/#related-pages","title":"Related pages","text":"<ul> <li>Authentication \u2014 credential forwarding and precedence.</li> <li>What happens on start \u2014 when each var is set.</li> <li>Runtime detection \u2014 the Docker socket vars.</li> </ul>"},{"location":"reference/features/","title":"Features","text":"<p>Features are the build-time capability layers that compose into an image. Each carries its own Dockerfile fragment (emitted in Stage 2) and declares dependencies on other features. Enable/disable them with <code>--with</code> / <code>--no</code>, or via a profile. See Profiles &amp; features for resolution rules.</p>"},{"location":"reference/features/#catalogue","title":"Catalogue","text":"<p>Listed in registry order \u2014 which is also dependency order (dependencies before dependents).</p> ID Name Deps \u2248 Size Installs <code>python</code> Python 3.13 \u2014 100 MB <code>uv</code> + a prebuilt CPython 3.13 (python-build-standalone); symlinked to <code>python3</code>. <code>go</code> Go toolchain \u2014 200 MB Go (v1.26.2 in the image; copied from the official <code>golang</code> image; multi-arch). <code>node</code> Node.js + Bun \u2014 150 MB Node 22 + Bun; npm/npx shims. Needed by most JS-based MCP servers. <code>playwright</code> Playwright + Chromium <code>node</code> 500 MB Chromium system libs + Chromium browser + Playwright MCP. <code>postgres-client</code> Postgres client \u2014 30 MB <code>psql</code>, <code>pg_dump</code>, <code>pg_restore</code>. <code>gh</code> GitHub CLI \u2014 20 MB <code>gh</code> from the official apt repo. <code>docker-cli</code> Docker CLI \u2014 40 MB <code>docker</code> client (no daemon) + a sudo wrapper. Auto-added by <code>--dind</code>. <code>audit-toolkit</code> Production audit toolkit <code>python</code> 400 MB trivy, syft, grype, semgrep, gitleaks, trufflehog, osv-scanner, checkov, dockle, scc, lizard. <code>codex-cli</code> OpenAI Codex CLI <code>node</code> 30 MB <code>@openai/codex</code> (used for cross-model code review). <code>ralphex</code> ralphex \u2014 20 MB Autonomous coding loop \u2014 runs plans task-by-task in fresh sessions. <code>aider</code> aider AI pair programming <code>python</code> 80 MB <code>aider-chat</code> via <code>uv tool install</code>. Opt-in; in no default profile. <p>aider is opt-in</p> <p><code>aider</code> is in no profile. It only enters a build when you explicitly request it with <code>--with=aider</code>.</p>"},{"location":"reference/features/#dependency-behavior","title":"Dependency behavior","text":"<ul> <li>Selecting a feature auto-enables its transitive dependencies (e.g. <code>playwright</code> pulls   in <code>node</code>; <code>audit-toolkit</code> pulls in <code>python</code>).</li> <li>A feature also gets pulled in when a harness requires it (Codex   and Pi require <code>node</code>) or when a selected extension declares it   in <code>deps.features</code>.</li> <li>If you <code>--no</code> a feature that something else still needs, it's auto-re-enabled \u2014 to   truly drop it, also drop its dependent. See   the resolution subtlety.</li> </ul> <p><code>--no</code> can be silently overridden</p> <p>Because dependencies win over <code>--no</code>, disabling a feature that a harness, extension, or another selected feature still requires has no effect \u2014 it stays in the image. Drop the dependent too if you really need it gone.</p>"},{"location":"reference/features/#always-on-base-toolkit","title":"Always-on base toolkit","text":"<p>Independent of features, every image includes a base layer: <code>ca-certificates</code>, <code>curl</code>, <code>wget</code>, <code>git</code>, <code>gpg</code>, <code>openssh-client</code>, <code>sudo</code>, <code>vim</code>, <code>less</code>, <code>tree</code>, <code>jq</code>, <code>sqlite3</code>, <code>dnsutils</code>, <code>unzip</code>, <code>xz-utils</code>, <code>build-essential</code>, <code>locales</code>, your chosen shell, ripgrep (<code>rg</code>), fd, and fzf. See Stage 1.</p>"},{"location":"reference/features/#related-pages","title":"Related pages","text":"<ul> <li>Profiles \u2014 which features each profile bundles.</li> <li>Profiles &amp; features \u2014 using <code>--with</code>/<code>--no</code>.</li> <li>What happens on build \u2014 where fragments are   emitted.</li> </ul>"},{"location":"reference/naming-and-labels/","title":"Naming &amp; labels","text":"<p>How Vibrator names images and containers, computes the variant fingerprint, sets the container hostname, and labels everything for discovery. This is what makes isolation per workspace work.</p>"},{"location":"reference/naming-and-labels/#the-fingerprint","title":"The fingerprint","text":"<p>Every build identity starts from an 8-character hex fingerprint: the first 8 chars of SHA-256 over a canonical form of the spec.</p> <p>The canonical form is:</p> <pre><code>harness=&lt;lower&gt;;shell=&lt;lower&gt;;features=&lt;sorted-csv&gt;;extensions=&lt;sorted-csv&gt;;user=&lt;lower&gt;\n</code></pre> <p>Properties:</p> <ul> <li>Order-independent \u2014 features and extensions   are sorted before hashing, so the same logical selection always yields the same fingerprint   regardless of input order.</li> <li>Case-insensitive for the small enums (harness, shell, user).</li> <li>Profile is excluded \u2014 it's just a label for a feature bundle.   <code>--profile=full</code> and the implicit default resolve to the same features and therefore the   same fingerprint.</li> <li>Username is included \u2014 the image's <code>USER</code> stage hard-codes the host UID/GID, so two   users must get distinct images.</li> <li>A wholly empty spec hashes to the sentinel <code>00000000</code>.</li> </ul>"},{"location":"reference/naming-and-labels/#image-tag","title":"Image tag","text":"<pre><code>vb-&lt;harness&gt;-&lt;profile&gt;-&lt;user&gt;-&lt;fp8&gt;:latest      # with a username\nvb-&lt;harness&gt;-&lt;profile&gt;-&lt;fp8&gt;:latest             # without\n</code></pre> <p>Example: <code>vb-claude-code-full-you-a1b2c3d4:latest</code>.</p> <ul> <li>Harness, profile, and user are sanitized to safe tag characters (lowercase   <code>[a-z0-9._-]</code>, no leading dot/dash).</li> <li><code>:latest</code> is intentional \u2014 image versioning is per-workspace by fingerprint, not a   floating tag.</li> <li>Override the tag for <code>vibrate build</code> with <code>--tag</code>.</li> </ul>"},{"location":"reference/naming-and-labels/#container-name","title":"Container name","text":"<pre><code>vb-&lt;workspace-basename&gt;-&lt;wsHash8&gt;-&lt;fp8&gt;\n</code></pre> <p>Example: <code>vb-my-project-7f8e9a0b-a1b2c3d4</code>.</p> <p><code>wsHash8</code> is a hash of the absolute workspace path plus the host UID. That combination:</p> <ul> <li>disambiguates same-named projects in different paths (<code>~/work/foo</code> vs <code>~/play/foo</code>), and</li> <li>prevents multi-user collisions (alice's and bob's <code>~/dev/foo</code> get distinct containers,   not one reused across UIDs).</li> </ul>"},{"location":"reference/naming-and-labels/#hostname","title":"Hostname","text":"<pre><code>vibrate-&lt;sanitized-workspace-basename&gt;\n</code></pre> <p>Example: <code>vibrate-my-project</code>. Set via <code>docker run --hostname</code>, it's what makes the shell prompt visibly different from a host shell (<code>you@vibrate-my-project</code>). Sanitized per RFC 1123 (letters, digits, hyphens; no leading/trailing hyphen; max 63 chars).</p>"},{"location":"reference/naming-and-labels/#labels","title":"Labels","text":""},{"location":"reference/naming-and-labels/#image-labels-baked-at-build-time","title":"Image labels (baked at build time)","text":"Label Value <code>vibrator.version</code> vibrator version <code>vibrator.build_id</code> build sentinel <code>vibrator.harness</code> harness ID <code>vibrator.profile</code> profile ID <code>vibrator.shell</code> shell <code>vibrator.features</code> comma-separated features <code>vibrator.extensions</code> comma-separated extensions"},{"location":"reference/naming-and-labels/#container-labels-set-at-run-time","title":"Container labels (set at run time)","text":"Label Value <code>vibrator.managed</code> <code>true</code> <code>vibrator.harness</code> harness ID <code>vibrator.workspace</code> the fingerprint <code>vibrator.path</code> the workspace absolute path <p><code>vibrate variants</code> filters on <code>vibrator.managed=true</code> (containers) and <code>vibrator.harness</code> (images) so it only ever lists or prunes Vibrator's own objects.</p>"},{"location":"reference/naming-and-labels/#username-derivation","title":"Username derivation","text":"<p>The container username defaults to your host username, sanitized for Linux <code>useradd</code>: lowercased, non-<code>[a-z0-9_-]</code> replaced with <code>_</code>, prefixed with <code>_</code> if it doesn't start with a letter/underscore, truncated to 32 chars. It falls back to <code>vibrate</code> if detection fails or you're root (UID 0). Override with <code>--username</code>.</p>"},{"location":"reference/naming-and-labels/#related-pages","title":"Related pages","text":"<ul> <li>Core concepts: variant.</li> <li><code>vibrate variants</code> \u2014 list/prune by label.</li> <li>What happens on build \u2014 where labels are written.</li> </ul>"},{"location":"reference/profiles/","title":"Profiles","text":"<p>A profile is a named starting bundle of features. Pick one with <code>--profile=&lt;id&gt;</code> (default <code>full</code>) or in the wizard. See Profiles &amp; features for how resolution works.</p> <p>The four profiles, smallest to largest:</p>"},{"location":"reference/profiles/#minimal","title":"<code>minimal</code>","text":"<p>Just the always-on base toolkit (jq, rg, fd, vim, curl, ssh, git). No language runtimes.</p> <ul> <li>Features: (none)</li> <li>Approx. size: ~150 MB</li> <li>Good for: CI runs, tight resource environments, \"I just want a shell with rg + jq +   vim in a box\".</li> </ul>"},{"location":"reference/profiles/#backend","title":"<code>backend</code>","text":"<p>Backend dev \u2014 adds Python, Go, GitHub CLI, Postgres client, ralphex. No browser, no audit toolkit.</p> <ul> <li>Features: <code>python</code>, <code>go</code>, <code>gh</code>, <code>postgres-client</code>, <code>ralphex</code></li> <li>Approx. size: ~600 MB</li> </ul>"},{"location":"reference/profiles/#frontend","title":"<code>frontend</code>","text":"<p>Frontend dev \u2014 adds Node.js + Bun + Playwright (Chromium). No Python, no Go, no audit toolkit.</p> <ul> <li>Features: <code>node</code>, <code>playwright</code>, <code>gh</code>, <code>ralphex</code></li> <li>Approx. size: ~1 GB</li> </ul>"},{"location":"reference/profiles/#full-default","title":"<code>full</code> (default)","text":"<p>Everything except aider \u2014 backend + frontend + audit toolkit + Codex CLI. Default when the wizard is skipped.</p> <ul> <li>Features: <code>python</code>, <code>go</code>, <code>node</code>, <code>playwright</code>, <code>gh</code>, <code>postgres-client</code>,   <code>audit-toolkit</code>, <code>codex-cli</code>, <code>ralphex</code></li> <li>Approx. size: ~2 GB</li> </ul>"},{"location":"reference/profiles/#notes","title":"Notes","text":"<ul> <li>The base toolkit (jq, ripgrep, fd, fzf, vim, git, curl, ssh, \u2026) is present in every   profile \u2014 it's part of Stage 1, not a feature.</li> <li><code>aider</code> is in no default profile \u2014 it's opt-in via <code>--with=aider</code>.</li> <li>Sizes are informational estimates only.</li> <li>Profile is not part of the variant fingerprint \u2014 only the   resolved feature set is. <code>--profile=full</code> and the implicit default build the same image.</li> </ul>"},{"location":"reference/profiles/#related-pages","title":"Related pages","text":"<ul> <li>Features \u2014 what each listed feature installs.</li> <li>Profiles &amp; features \u2014 resolution and <code>--with</code>/<code>--no</code>.</li> </ul>"},{"location":"reference/vb-file/","title":"<code>.vb</code> schema","text":"<p>The complete schema of the per-workspace pin file. For the practical guide, see The <code>.vb</code> file.</p> <p><code>.vb</code> is TOML, written with mode <code>0600</code>, discovered by walking up to the git or filesystem root. Every field is optional except <code>harness</code> (required to launch). Empty/zero fields are omitted on save.</p>"},{"location":"reference/vb-file/#full-example","title":"Full example","text":"<pre><code># vibrator workspace pin (`.vb`) \u2014 auto-managed by `vibrate`.\n# Plaintext prereq tokens may live in [prereqs.*] subtables \u2014 keep gitignored.\n\nharness = \"claude-code\"\nprofile = \"full\"\nshell   = \"zsh\"\nwith    = [\"docker-cli\"]\nno      = [\"aider\"]\nextensions = [\"context7\", \"ecc-developer\"]\n\n[llm]\nprovider = \"openai\"\nmodel    = \"gpt-4o\"\n\n[llm.auth]\nenv = \"OPENAI_API_KEY\"\n\n[prereqs.claude-mem-server-beta]\napi_key    = \"cmem_...\"\nteam_id    = \"...\"\nproject_id = \"...\"\n\n[env]\nGITHUB_TOKEN = \"$GITHUB_TOKEN\"\nREGION       = \"us-east-1\"\n\n[integrations]\nserena     = \"auto\"\nclaude-mem = \"host\"\n\n[hooks]\nacknowledged_missing = [\"node\"]\n</code></pre>"},{"location":"reference/vb-file/#top-level-fields","title":"Top-level fields","text":"TOML key Type Default Notes <code>harness</code> string \u2014 Required to launch. One of <code>claude-code</code>, <code>codex</code>, <code>opencode</code>, <code>pi</code> (see Harnesses). <code>profile</code> string <code>full</code> One of <code>minimal</code>, <code>backend</code>, <code>frontend</code>, <code>full</code> (see Profiles). <code>shell</code> string <code>zsh</code> One of <code>bash</code>, <code>zsh</code>, <code>fish</code>. <code>with</code> list \u2014 Feature IDs to enable on top of the profile. <code>no</code> list \u2014 Feature IDs to disable. <code>extensions</code> list \u2014 Extension IDs (scoped to the harness)."},{"location":"reference/vb-file/#llm","title":"<code>[llm]</code>","text":"<p>The LLM provider choice, for provider-agnostic harnesses. Omit the whole table for Claude Code (Anthropic-only).</p> TOML key Type Default Notes <code>provider</code> string \u2014 <code>anthropic</code>, <code>openai</code>, <code>ollama</code>, <code>lmstudio</code>, <code>openai-compat</code>. <code>model</code> string \u2014 Model identifier in the provider's namespace (<code>gpt-4o</code>, <code>qwen3:32b</code>, \u2026). <code>base_url</code> string provider default Custom endpoint. Empty = provider's canonical default."},{"location":"reference/vb-file/#llmauth","title":"<code>[llm.auth]</code>","text":"<p>The credential plan. Omit entirely for local providers (<code>ollama</code>, <code>lmstudio</code>). For cloud and <code>openai-compat</code>, set exactly one of:</p> TOML key Type Notes <code>env</code> string Name of a host env var carrying the key (preferred \u2014 secret stays out of <code>.vb</code>). <code>value</code> string Literal API key. Plaintext in <code>.vb</code> (<code>0600</code>, gitignored)."},{"location":"reference/vb-file/#prereqs","title":"<code>[prereqs.&lt;id&gt;]</code>","text":"<p>Cached host-side prerequisite tokens, keyed by prereq ID. The schema inside each subtable is loose \u2014 each prereq's bootstrap decides what it stores. Keys are sorted on save for stable diffs.</p> <p>For <code>claude-mem-server-beta</code> (see claude-mem):</p> Key Notes <code>api_key</code> minted project-scoped bearer token (<code>cmem_...</code>) <code>team_id</code> resolved team UUID <code>project_id</code> resolved project UUID <code>actor_id</code> <code>vibrator:&lt;hostname&gt;:&lt;workspace-path&gt;</code> <p>Plaintext credentials</p> <p><code>[prereqs.*]</code> subtables hold secrets in plaintext. <code>.vb</code> is written <code>0600</code> and added to <code>.gitignore</code> \u2014 keep it out of version control.</p>"},{"location":"reference/vb-file/#env","title":"<code>[env]</code>","text":"<p>Host\u2192container environment forwarding, applied at <code>docker run</code>:</p> Value form Behavior <code>\"$NAME\"</code> Resolved from the host environment at run time (not at pin-load time). <code>\"literal\"</code> Forwarded as-is. <p>Keys are sorted on save. See the precedence rules for how <code>[env]</code> interacts with other forwarded vars.</p>"},{"location":"reference/vb-file/#integrations","title":"<code>[integrations]</code>","text":"<p>Per-integration hosting mode, keyed by integration ID:</p> Value Meaning <code>auto</code> (default when key absent) Probe host \u2192 http, else stdio fallback. <code>host</code> http only; warn if unreachable, no fallback. <code>local</code> stdio only; never probe the host. <code>off</code> Disable the integration's MCP wiring."},{"location":"reference/vb-file/#hooks","title":"<code>[hooks]</code>","text":"<p>Per-workspace hook preferences, written when you respond to a missing-tool hook prompt at launch.</p> TOML key Type Notes <code>acknowledged_missing</code> list Feature IDs you chose not to install for hooks that need them. Vibrator stops re-prompting for these; the container guard keeps skipping the affected hooks. Installing the feature (adding it to <code>with</code>) closes the gap and clears the entry."},{"location":"reference/vb-file/#discovery-and-lifecycle","title":"Discovery and lifecycle","text":"<ul> <li>Found by walking up from <code>$PWD</code> to the git root (or filesystem root); first <code>.vb</code>   wins. Its directory is the workspace root.</li> <li>Written by the wizard (unless <code>--no-save</code>) and by   <code>vibrate prereqs bootstrap</code> / inline launch bootstrap.</li> <li>Defaults applied on save: <code>profile</code> \u2192 <code>full</code>, <code>shell</code> \u2192 <code>zsh</code> when unset.</li> </ul>"},{"location":"reference/vb-file/#related-pages","title":"Related pages","text":"<ul> <li>The <code>.vb</code> file \u2014 practical guide.</li> <li>Migrating from bash \u2014 converting <code>.vb.env</code>.</li> </ul>"},{"location":"reference/commands/","title":"CLI reference","text":"<p><code>vibrate</code> (alias <code>vb</code>) is built with Cobra. Every subcommand lives on its own page below, with all of its flags, defaults, and behavior.</p> <pre><code>vibrate [command] [flags]\n</code></pre> <p>Running <code>vibrate</code> with no subcommand behaves exactly like <code>vibrate run</code>. Global flags <code>--help</code> / <code>-h</code> and <code>--version</code> are available everywhere.</p>"},{"location":"reference/commands/#all-commands","title":"All commands","text":"Command Purpose Reference <code>vibrate</code> (bare) Resolve <code>.vb</code> + flags, run the wizard, build/run/exec launch.md <code>vibrate run</code> Explicit form of the bare invocation launch.md <code>vibrate shell</code> Same as <code>run</code>, but launch your shell instead of the agent launch.md <code>vibrate build</code> Build the image without running a container build.md <code>vibrate build-dockerfile</code> Emit the generated Dockerfile to a file / stdout build.md <code>vibrate wizard</code> Run the setup wizard standalone (no build, no save) wizard.md <code>vibrate reconfigure</code> Re-run the wizard and rebuild, preserving credentials wizard.md <code>vibrate update</code> Update the harness's CLI in place (no full rebuild) update.md <code>vibrate variants list</code> List managed images + containers for this host variants.md <code>vibrate variants prune</code> Remove stopped containers / unused images variants.md <code>vibrate extensions list</code> List extension entries for a harness extensions.md <code>vibrate extensions show</code> Show an extension entry's frontmatter + body extensions.md <code>vibrate integrations</code> Interactive setup for host-side integrations integrations.md <code>vibrate prereqs status</code> Probe host prerequisites (e.g. claude-mem server) prereqs.md <code>vibrate prereqs bootstrap</code> Run host-side setup (e.g. mint a claude-mem key) prereqs.md <code>vibrate hostprobe</code> Show host-detected plugins + extension matches hostprobe.md <code>vibrate runtime detect</code> Show the auto-detected Docker socket runtime.md <code>vibrate migrate-pin</code> Convert a bash-era <code>.vb.env</code> \u2192 TOML <code>.vb</code> migrate-pin.md"},{"location":"reference/commands/#spec-resolution-flags","title":"Spec-resolution flags","text":"<p>These flags shape what gets built. They are shared by <code>vibrate</code>, <code>run</code>, <code>shell</code>, <code>build</code>, and <code>build-dockerfile</code>. The same values can be pinned in <code>.vb</code>; flags always override the pin.</p> Flag Type Default Description <code>--harness=&lt;id&gt;</code> string \u2014 (required) <code>claude-code</code>, <code>codex</code>, <code>opencode</code>, <code>pi</code> <code>--profile=&lt;id&gt;</code> string <code>full</code> <code>minimal</code>, <code>backend</code>, <code>frontend</code>, <code>full</code> <code>--shell=&lt;id&gt;</code> string <code>zsh</code> <code>bash</code>, <code>zsh</code>, <code>fish</code> <code>--with=&lt;feat,...&gt;</code> list \u2014 Features to enable on top of the profile <code>--no=&lt;feat,...&gt;</code> list \u2014 Features to disable <code>--extensions=&lt;id,...&gt;</code> list \u2014 Extension IDs to install <code>--username=&lt;name&gt;</code> string host user Unprivileged user created in the container <code>--host-uid=&lt;n&gt;</code> int host UID UID baked into the image <code>--host-gid=&lt;n&gt;</code> int host GID GID baked into the image <p>Resolution order</p> <p>For every field: CLI flag \u2192 <code>.vb</code> value \u2192 built-in default. The full decision tree is documented in Lifecycle.</p>"},{"location":"reference/commands/#related-pages","title":"Related pages","text":"<ul> <li><code>vibrate</code> / <code>run</code> / <code>shell</code> \u2014 the default launch path.</li> <li>The <code>.vb</code> file \u2014 where these flags get pinned.</li> <li>Lifecycle \u2014 the build/run/exec decision tree.</li> </ul>"},{"location":"reference/commands/build/","title":"<code>build</code> / <code>build-dockerfile</code>","text":"<p>Two commands for producing build artifacts without launching a container. Both share the spec-resolution flags; unlike the launch commands, <code>--profile</code> defaults to <code>full</code> and <code>--shell</code> to <code>zsh</code> at the flag level (rather than being resolved later from <code>.vb</code>).</p>"},{"location":"reference/commands/build/#vibrate-build","title":"<code>vibrate build</code>","text":"<pre><code>vibrate build [flags]\n</code></pre> <p>Resolves the spec and runs <code>docker build</code> with the generated Dockerfile. Does not start a container. Use it to warm an image ahead of time, or in CI.</p>"},{"location":"reference/commands/build/#flags","title":"Flags","text":"<p>In addition to the spec-resolution flags:</p> Flag Default Description <code>--no-cache</code> <code>false</code> Pass <code>--no-cache</code> to <code>docker build</code> (forces a full rebuild). <code>--tag=&lt;tag&gt;</code> \u2014 Override the image tag. Default is the fingerprinted tag <code>vb-&lt;harness&gt;-&lt;profile&gt;-&lt;fingerprint&gt;:latest</code>."},{"location":"reference/commands/build/#examples","title":"Examples","text":"<pre><code># Build the default-spec image for this workspace.\nvibrate build --harness=claude-code\n\n# Clean rebuild of a specific spec.\nvibrate build --harness=codex --profile=backend --no-cache\n\n# Build under a custom tag for pushing to a registry.\nvibrate build --harness=claude-code --tag=myregistry/vibrate-cc:latest\n</code></pre>"},{"location":"reference/commands/build/#vibrate-build-dockerfile","title":"<code>vibrate build-dockerfile</code>","text":"<pre><code>vibrate build-dockerfile [flags]\n</code></pre> <p>Runs the same generation pipeline as <code>build</code>, but writes the resulting Dockerfile to a path (or stdout) instead of invoking <code>docker</code>. The output is byte-deterministic for a given spec \u2014 ideal for diffing changes, golden tests, and CI inspection.</p>"},{"location":"reference/commands/build/#flags_1","title":"Flags","text":"<p>In addition to the spec-resolution flags:</p> Flag Default Description <code>--out=&lt;path&gt;</code> <code>-</code> Output path. Use <code>-</code> for stdout."},{"location":"reference/commands/build/#examples_1","title":"Examples","text":"<pre><code># Print the Dockerfile for a full claude-code image to your terminal.\nvibrate build-dockerfile --harness=claude-code --profile=full --shell=zsh\n\n# Write it to a file for inspection / diffing.\nvibrate build-dockerfile --harness=codex --profile=backend --out=Dockerfile.codex\n\n# See how selecting an extension changes the build.\nvibrate build-dockerfile --harness=claude-code --extensions=context7,ecc-developer\n</code></pre> <p>The generated header records the exact spec and a reproduction command, so a saved Dockerfile documents itself.</p>"},{"location":"reference/commands/build/#related-pages","title":"Related pages","text":"<ul> <li>What happens on build \u2014 the five-stage generator explained.</li> <li>Features and Profiles \u2014 what populates the stages.</li> <li>Naming &amp; labels \u2014 how the default tag is derived.</li> </ul>"},{"location":"reference/commands/extensions/","title":"<code>vibrate extensions</code>","text":"<p>Browse the curated extensions catalogue \u2014 the per-harness inventory of MCP servers, skills, subagents, and bundles you can install into an image.</p> <pre><code>vibrate extensions &lt;list|show&gt; [args]\n</code></pre>"},{"location":"reference/commands/extensions/#vibrate-extensions-list","title":"<code>vibrate extensions list</code>","text":"<pre><code>vibrate extensions list [HARNESS]\n</code></pre> <p>With no argument, lists each harness and how many extension entries it has. With a harness argument, lists that harness's entries in a table:</p> <pre><code>vibrate extensions list claude-code\n</code></pre> Column Meaning <code>ID</code> the extension ID you pass to <code>--extensions</code> <code>KIND</code> <code>plugin</code>, <code>skill</code>, <code>mcp</code>, <code>subagent</code>, or <code>tool</code> <code>DEFAULT</code> \u2713 if pre-selected in the wizard <code>SIZE</code> approximate image-size impact (MB), when known <code>NAME</code> display label"},{"location":"reference/commands/extensions/#vibrate-extensions-show","title":"<code>vibrate extensions show</code>","text":"<pre><code>vibrate extensions show ID\n</code></pre> <p>Prints an entry's full YAML frontmatter and Markdown body \u2014 what it is, why it's useful, its dependencies, auth requirements, and install snippet.</p> <p>The <code>ID</code> can be:</p> <ul> <li>Qualified \u2014 <code>claude-code/context7</code> (unambiguous).</li> <li>Bare \u2014 <code>context7</code> (searched across all harnesses; errors if ambiguous).</li> </ul>"},{"location":"reference/commands/extensions/#examples","title":"Examples","text":"<pre><code># Counts per harness.\nvibrate extensions list\n\n# Everything available for Codex.\nvibrate extensions list codex\n\n# Full docs for one entry.\nvibrate extensions show ecc-developer\nvibrate extensions show claude-code/serena\n</code></pre>"},{"location":"reference/commands/extensions/#related-pages","title":"Related pages","text":"<ul> <li>Extensions guide \u2014 how the catalogue works and how to select   entries.</li> <li>The ECC bundle \u2014 the <code>ecc-*</code> family.</li> <li><code>vibrate hostprobe</code> \u2014 which of your host plugins map to catalogue entries.</li> </ul>"},{"location":"reference/commands/hostprobe/","title":"<code>vibrate hostprobe</code>","text":"<pre><code>vibrate hostprobe\n</code></pre> <p>Scans your host for installed harness configs and plugins, and shows how those raw host-side identifiers map to entries in the extensions catalogue. This is the same scan the wizard uses to pre-check extensions you already use on the host, so it's a useful diagnostic for \"why is the wizard suggesting this?\"</p> <p>Takes no flags.</p>"},{"location":"reference/commands/hostprobe/#what-it-reports","title":"What it reports","text":"<p>For each harness, <code>hostprobe</code> prints:</p> Field Source Home dir the harness's config directory (e.g. <code>~/.claude</code>) Installed whether that directory exists Plugins / skills (raw) plugin IDs found in the harness's config MCP servers (raw) MCP server names found Registered marketplaces marketplace IDs (Claude Code) Extension matches which catalogue entries those raw IDs map to (these get pre-checked in the wizard) Notes best-effort diagnostics (parse warnings, detection method) <p>For Claude Code specifically it reads <code>~/.claude/plugins/installed_plugins.json</code> (modern manifest), <code>~/.claude/settings.json</code> (<code>enabledPlugins</code>), <code>~/.claude.json</code> (<code>mcpServers</code>), and <code>~/.claude/plugins/known_marketplaces.json</code>. Parse errors are reported as notes, never fatal.</p>"},{"location":"reference/commands/hostprobe/#example","title":"Example","text":"<pre><code>vibrate hostprobe\n</code></pre> <p>Use it to confirm an extension you've installed on the host is recognized by Vibrator's catalogue (and will be offered in the wizard), or to debug a mapping that isn't happening.</p>"},{"location":"reference/commands/hostprobe/#related-pages","title":"Related pages","text":"<ul> <li>Extensions guide \u2014 the catalogue and <code>host_aliases</code>   matching.</li> <li><code>vibrate extensions list</code> \u2014 the catalogue itself.</li> <li><code>vibrate wizard</code> \u2014 where the matches get pre-checked.</li> </ul>"},{"location":"reference/commands/integrations/","title":"<code>vibrate integrations</code>","text":"<p>Interactive setup for host-side integrations \u2014 services that run on your machine and get wired into the container (currently Serena and claude-mem).</p> <pre><code>vibrate integrations [list | claude-mem | serena]\n</code></pre>"},{"location":"reference/commands/integrations/#bare-invocation","title":"Bare invocation","text":"<pre><code>vibrate integrations\n</code></pre> <p>Shows an interactive picker listing every registered integration (built-ins plus any user-defined TOML descriptors). Select one to manage it.</p>"},{"location":"reference/commands/integrations/#vibrate-integrations-list","title":"<code>vibrate integrations list</code>","text":"<pre><code>vibrate integrations list\n</code></pre> <p>Non-interactive listing of every registered integration and its status \u2014 the same inventory the bare picker shows, but printed straight to stdout for scripts and quick inspection.</p>"},{"location":"reference/commands/integrations/#vibrate-integrations-claude-mem","title":"<code>vibrate integrations claude-mem</code>","text":"<pre><code>vibrate integrations claude-mem\n</code></pre> <p>The hand-written setup + management flow for claude-mem:</p> <ol> <li>Admin config form \u2014 server URL, team name, stack directory, and the Postgres DSN.    Saved to <code>~/.config/vibrator/claude-mem.toml</code> (mode <code>0600</code>).</li> <li>Runner \u2014 start/stop the Docker Compose stack, probe the server, bootstrap the    workspace key, or tail logs.</li> </ol> <p>The Postgres DSN stays on the host</p> <p>The Postgres DSN (<code>database_url</code>) is a host-only credential \u2014 it never crosses into a container. Only the resulting project-scoped bearer token does.</p>"},{"location":"reference/commands/integrations/#vibrate-integrations-serena","title":"<code>vibrate integrations serena</code>","text":"<pre><code>vibrate integrations serena\n</code></pre> <p>Manage the Serena host server \u2014 show status, start/stop it as a background process or a Docker container, and tail logs.</p> <p>Serena port</p> <p>The Serena server listens on the port given by <code>SERENA_PORT</code> (default <code>8765</code>). Set that var before starting it if the default port is taken.</p>"},{"location":"reference/commands/integrations/#related-pages","title":"Related pages","text":"<ul> <li>Integrations guide \u2014 hosting modes, transport switching,   and how integrations wire into the container.</li> <li>Serena \u00b7 claude-mem \u2014   per-integration setup pages.</li> <li><code>vibrate prereqs</code> \u2014 the per-workspace bootstrap side (e.g. minting a   claude-mem key).</li> </ul>"},{"location":"reference/commands/launch/","title":"<code>vibrate</code> / <code>run</code> / <code>shell</code>","text":"<p>These three forms share one flag set and one orchestration path \u2014 they differ only in what gets exec'd inside the container at the end.</p> Form Final exec target <code>vibrate</code> (bare) the harness's CLI <code>vibrate run</code> the harness's CLI <code>vibrate shell</code> your shell <p>All of them: resolve the workspace config (flags &gt; <code>.vb</code> &gt; defaults), run the wizard for unset fields, build the image if it's missing, create or start the container, and exec the target. See Lifecycle for the full decision tree.</p>"},{"location":"reference/commands/launch/#vibrate-bare","title":"<code>vibrate</code> (bare)","text":"<pre><code>vibrate [flags]\n</code></pre> <p>The default action. Equivalent to <code>vibrate run</code>. Launches the harness's own CLI \u2014 <code>claude</code> for claude-code, <code>codex</code> for codex, <code>opencode</code> for opencode, <code>pi</code> for pi.</p>"},{"location":"reference/commands/launch/#vibrate-run","title":"<code>vibrate run</code>","text":"<pre><code>vibrate run [flags]\n</code></pre> <p>The explicit form of the bare invocation. Identical behavior and flags.</p>"},{"location":"reference/commands/launch/#vibrate-shell","title":"<code>vibrate shell</code>","text":"<pre><code>vibrate shell [flags]\n</code></pre> <p>Like <code>vibrate run</code>, but execs your shell inside the container instead of the harness CLI. Everything else \u2014 build-if-missing, start-if-stopped, workspace mount, credential forwarding \u2014 is identical. Useful for:</p> <ul> <li>Debugging the container (inspect config files, check installed packages).</li> <li>Installing things the extensions catalogue doesn't cover.</li> <li>Driving the harness via subcommands or piped input rather than its interactive TUI.</li> </ul>"},{"location":"reference/commands/launch/#options","title":"Flags","text":""},{"location":"reference/commands/launch/#spec-resolution-flags","title":"Spec-resolution flags","text":"<p>Shared with <code>build</code>. See the shared table: <code>--harness</code>, <code>--profile</code>, <code>--shell</code>, <code>--with</code>, <code>--no</code>, <code>--extensions</code>, <code>--username</code>, <code>--host-uid</code>, <code>--host-gid</code>.</p>"},{"location":"reference/commands/launch/#orchestration-flags","title":"Orchestration flags","text":"Flag Default Description <code>--no-wizard</code> <code>false</code> Skip the interactive wizard; fail if a required field (harness) is unset. <code>--no-save</code> <code>false</code> Don't write the resolved config to <code>.vb</code>. <code>--rebuild</code> <code>false</code> Remove any existing container and rebuild the image from scratch (<code>docker build --no-cache</code>), then run fresh. <code>--dind</code> <code>false</code> Mount the host's Docker socket so <code>docker</code> inside the container drives the host daemon. Auto-installs the <code>docker-cli</code> feature. See Docker-in-Docker. <code>--login</code> <code>false</code> Run <code>claude auth login</code> in the container before launching the harness; opens the auth URL in your host browser and writes auth state back to the host. See Authentication."},{"location":"reference/commands/launch/#examples","title":"Examples","text":"<pre><code># First run \u2014 wizard fills the gaps, then build + launch.\nvibrate\n\n# Fully specified, no wizard.\nvibrate --no-wizard --harness=claude-code --profile=full --shell=zsh\n\n# Backend profile, add a couple of features, drop one, pick extensions.\nvibrate --harness=claude-code --profile=backend \\\n        --with=node,playwright --no=ralphex \\\n        --extensions=context7,ecc-developer\n\n# Try a combo once without saving it to .vb.\nvibrate --harness=codex --profile=minimal --no-save\n\n# Force a clean rebuild (e.g. after changing host rules baked at build time).\nvibrate --rebuild\n\n# Authenticate Claude Code via the browser, then land in the agent.\nvibrate --login\n\n# Drop into a shell instead of the agent.\nvibrate shell\n\n# Let the container drive the host Docker daemon.\nvibrate --dind\n</code></pre>"},{"location":"reference/commands/launch/#exit-code","title":"Exit code","text":"<p><code>vibrate</code> propagates the exit code of the process you ran inside the container \u2014 so exiting the agent with a non-zero status surfaces it to your host shell.</p>"},{"location":"reference/commands/launch/#related-pages","title":"Related pages","text":"<ul> <li>The <code>.vb</code> file \u2014 what gets saved and resolved.</li> <li>What happens on build / on start.</li> <li><code>vibrate reconfigure</code> \u2014 change an existing setup.</li> </ul>"},{"location":"reference/commands/migrate-pin/","title":"<code>vibrate migrate-pin</code>","text":"<pre><code>vibrate migrate-pin [flags]\n</code></pre> <p>Converts a bash-era <code>.vb.env</code> (shell dotenv) into the current <code>.vb</code> (TOML) pin format. See the migration guide for the narrative version.</p>"},{"location":"reference/commands/migrate-pin/#flags","title":"Flags","text":"Flag Default Description <code>--input=&lt;path&gt;</code> <code>./.vb.env</code> Path to the legacy <code>.vb.env</code>. <code>--output=&lt;path&gt;</code> <code>.vb</code> beside the input Path to write the new <code>.vb</code>. <code>--dry-run</code> <code>false</code> Print the conversion without touching disk. <code>--keep-old</code> <code>false</code> Keep <code>.vb.env</code> instead of archiving it to <code>.vb.env.bak</code>."},{"location":"reference/commands/migrate-pin/#what-it-maps","title":"What it maps","text":"Legacy key New location <code>HARNESS</code> / <code>PROFILE</code> / <code>SHELL</code> top-level <code>harness</code> / <code>profile</code> / <code>shell</code> <code>WITH</code> / <code>NO</code> / <code>CATALOG</code> <code>with</code> / <code>no</code> / <code>extensions</code> (comma-split lists) <code>CLAUDE_MEM_SERVER_BETA_*</code> <code>[prereqs.claude-mem-server-beta]</code> <code>USERNAME</code> dropped (now a build-time flag) anything else preserved verbatim under <code>[env]</code> <p>Unknown keys are never lost \u2014 they land under <code>[env]</code>.</p>"},{"location":"reference/commands/migrate-pin/#examples","title":"Examples","text":"<pre><code># Preview the conversion.\nvibrate migrate-pin --dry-run\n\n# Convert: writes .vb, archives the old file to .vb.env.bak, updates .gitignore.\nvibrate migrate-pin\n\n# Convert a file elsewhere, keeping the original.\nvibrate migrate-pin --input=old/.vb.env --output=.vb --keep-old\n</code></pre>"},{"location":"reference/commands/migrate-pin/#related-pages","title":"Related pages","text":"<ul> <li>Migrating from bash \u2014 the full guide.</li> <li>The <code>.vb</code> file \u2014 the target format.</li> </ul>"},{"location":"reference/commands/prereqs/","title":"<code>vibrate prereqs</code>","text":"<p>Inspect and bootstrap host-side prerequisites \u2014 things that must exist on your machine for an integration to work, like a reachable claude-mem server and a per-workspace API key.</p> <pre><code>vibrate prereqs &lt;status|bootstrap&gt; [args] [flags]\n</code></pre> <p>A prerequisite has a verifier (probes whether it's satisfied) and an optional bootstrapper (sets it up). Today the built-in prereq is <code>claude-mem-server-beta</code>.</p>"},{"location":"reference/commands/prereqs/#vibrate-prereqs-status","title":"<code>vibrate prereqs status</code>","text":"<pre><code>vibrate prereqs status [--no-color]\n</code></pre> <p>Probes every registered prerequisite and prints a one-screen report. For claude-mem that covers:</p> <ol> <li>Admin config \u2014 path, and the parsed <code>runtime</code> / <code>server_url</code> / <code>database_url</code>.</li> <li>Server probe \u2014 whether <code>GET &lt;server_url&gt;/healthz</code> is reachable.</li> <li>Workspace cache \u2014 the <code>.vb</code> values for this workspace (<code>api_key</code> prefix, <code>team_id</code>,    <code>project_id</code>).</li> </ol>"},{"location":"reference/commands/prereqs/#flags","title":"Flags","text":"Flag Default Description <code>--no-color</code> <code>false</code> Disable ANSI colors (for logs / non-TTY output)."},{"location":"reference/commands/prereqs/#exit-code","title":"Exit code","text":"<p><code>0</code> when every required step passes; <code>1</code> when at least one required step fails \u2014 so it's usable as a gate in scripts.</p>"},{"location":"reference/commands/prereqs/#vibrate-prereqs-bootstrap","title":"<code>vibrate prereqs bootstrap</code>","text":"<pre><code>vibrate prereqs bootstrap PREREQ_ID [--force] [--no-color]\n</code></pre> <p>Runs the host-side setup for a prerequisite. For <code>claude-mem-server-beta</code> this mints a project-scoped API key against your host's claude-mem Postgres and persists it in <code>.vb</code> under <code>[prereqs.claude-mem-server-beta]</code>.</p> <p>It is idempotent: if <code>.vb</code> already has a cached value it skips re-minting unless you pass <code>--force</code>. It also idempotently adds <code>.vb</code> to <code>.gitignore</code>.</p>"},{"location":"reference/commands/prereqs/#flags_1","title":"Flags","text":"Flag Default Description <code>--force</code> <code>false</code> Re-mint (rotate) even if a cached key exists. <code>--no-color</code> <code>false</code> Disable ANSI colors."},{"location":"reference/commands/prereqs/#example","title":"Example","text":"<pre><code>cd ~/my-project\nvibrate prereqs bootstrap claude-mem-server-beta\n</code></pre> <pre><code>\u2713 team_id    = ...\n\u2713 project_id = ...\n\u2713 api_key    = cmem_\u2026  (length 53)\n\u2713 persisted in .vb [prereqs.claude-mem-server-beta]\n\u2713 added .vb to .gitignore\n</code></pre> <p>To rotate a revoked key:</p> <pre><code>vibrate prereqs bootstrap claude-mem-server-beta --force\n</code></pre> <p>Inline bootstrap at launch</p> <p>You usually don't need to run this by hand. When you launch a workspace whose extensions include claude-mem and the key is missing, <code>vibrate</code> warns and offers to bootstrap inline \u2014 <code>Bootstrap now? [y/N]</code>. See claude-mem.</p>"},{"location":"reference/commands/prereqs/#related-pages","title":"Related pages","text":"<ul> <li>claude-mem integration \u2014 the full credential flow.</li> <li><code>vibrate integrations</code> \u2014 the host-side admin config + stack   management.</li> <li>The <code>.vb</code> file \u2014 where minted keys are stored.</li> </ul>"},{"location":"reference/commands/runtime/","title":"<code>vibrate runtime detect</code>","text":"<pre><code>vibrate runtime detect [flags]\n</code></pre> <p>Inspects and prints the Docker runtime and socket path Vibrator will use. A read-only diagnostic \u2014 it changes nothing. The full detection algorithm is documented in Runtime detection.</p>"},{"location":"reference/commands/runtime/#output","title":"Output","text":"<pre><code>Runtime: orbstack\nSocket:  /Users/you/.orbstack/run/docker.sock\nSource:  socket-scan\n</code></pre> <ul> <li>Runtime \u2014 <code>docker-desktop</code>, <code>orbstack</code>, <code>colima</code>, <code>rancher-desktop</code>, <code>podman</code>,   <code>native</code>, or <code>custom</code>.</li> <li>Socket \u2014 the absolute Unix socket path.</li> <li>Source \u2014 how it was found (<code>env-override</code>, <code>docker-host</code>, <code>docker-context</code>,   <code>socket-scan</code>).</li> </ul>"},{"location":"reference/commands/runtime/#flags","title":"Flags","text":"Flag Default Description <code>--docker-socket=&lt;path&gt;</code> \u2014 Force this socket path (highest priority). Equivalent to <code>VIBRATOR_DOCKER_SOCKET</code>. <code>--colima-profile=&lt;name&gt;</code> <code>$COLIMA_PROFILE</code> or <code>default</code> Colima VM profile to probe."},{"location":"reference/commands/runtime/#examples","title":"Examples","text":"<pre><code># What will vibrate use?\nvibrate runtime detect\n\n# Probe a specific Colima profile.\nvibrate runtime detect --colima-profile=work\n\n# Verify a manual override resolves.\nvibrate runtime detect --docker-socket=/custom/path/docker.sock\n</code></pre>"},{"location":"reference/commands/runtime/#related-pages","title":"Related pages","text":"<ul> <li>Runtime detection \u2014 the detection order and the   list of recognized runtimes.</li> <li>Environment variables \u2014 <code>VIBRATOR_DOCKER_SOCKET</code>,   <code>DOCKER_HOST</code>, <code>COLIMA_PROFILE</code>.</li> </ul>"},{"location":"reference/commands/update/","title":"<code>vibrate update</code>","text":"<pre><code>vibrate update\n</code></pre> <p>Upgrades the harness's own CLI to its latest version, in place \u2014 without regenerating the whole image from the Dockerfile. The harness, profile, and rest of the spec come from the workspace <code>.vb</code>. Takes no flags.</p>"},{"location":"reference/commands/update/#decision-tree","title":"Decision tree","text":"State What happens Persistence Container running Exec the harness's update command inside it (e.g. <code>claude update</code>). Survives restart, lost on container removal. Container stopped (exited/etc.) Start the container, then run the update. Same as above. Container missing, image exists Add a <code>RUN &lt;update-cmd&gt;</code> layer on top of the image and re-tag it with the same name. The old image becomes dangling; the layer cache keeps it fast. Baked into the image. Neither exists Error \u2014 run <code>vibrate</code> first to bootstrap the workspace. \u2014 <p>Promoting a container update into the image</p> <p>A container-level update survives restarts but not container removal. To bake it into the image instead, remove the container first (e.g. <code>vibrate variants prune --containers</code>) and run <code>vibrate update</code> again \u2014 it then takes the image-layer path.</p>"},{"location":"reference/commands/update/#per-harness-update-command","title":"Per-harness update command","text":"Harness Command run <code>claude-code</code> <code>claude update</code> <code>codex</code> <code>npm install -g @openai/codex@latest</code> <code>opencode</code> <code>opencode upgrade</code> <code>pi</code> <code>npm install -g @mariozechner/pi-coding-agent@latest</code>"},{"location":"reference/commands/update/#when-to-use-a-full-rebuild-instead","title":"When to use a full rebuild instead","text":"<p><code>vibrate update</code> only touches the harness binary. To rebuild everything from the Dockerfile \u2014 new base packages, features, extensions, re-fetched bundles \u2014 use <code>vibrate --rebuild</code> instead.</p>"},{"location":"reference/commands/update/#related-pages","title":"Related pages","text":"<ul> <li><code>vibrate variants</code> \u2014 inspect/clean up images and containers.</li> <li>Harnesses \u2014 what each harness installs.</li> </ul>"},{"location":"reference/commands/variants/","title":"<code>vibrate variants</code>","text":"<p>Manage the set of locally-built Vibrator images and their containers. Vibrator finds them by the <code>vibrator.managed=true</code> (containers) and <code>vibrator.harness</code> (images) labels, so it only ever touches its own objects.</p> <pre><code>vibrate variants &lt;list|prune&gt; [flags]\n</code></pre>"},{"location":"reference/commands/variants/#vibrate-variants-list","title":"<code>vibrate variants list</code>","text":"<pre><code>vibrate variants list\n</code></pre> <p>Prints every Vibrator-managed image and container with its metadata, read from the labels baked at build and set at run time.</p> <pre><code>Images (2 managed)\n------------------------------\n  vb-claude-code-full-you-a1b2c3d4:latest\n    harness:  claude-code\n    profile:  full\n    features: python,go,node,playwright,gh,postgres-client,audit-toolkit,codex-cli,ralphex\n    extensions:  context7,ecc-developer\n    size:     2.1GB, built 2 days ago\n\nContainers (1 managed)\n------------------------------\n  vb-my-project-7f8e9a0b-a1b2c3d4  Up 3 minutes\n    image:     vb-claude-code-full-you-a1b2c3d4:latest\n    workspace: /Users/you/my-project\n</code></pre> <p>Running containers are highlighted; the <code>workspace:</code> line is read from the <code>vibrator.path</code> label.</p>"},{"location":"reference/commands/variants/#vibrate-variants-prune","title":"<code>vibrate variants prune</code>","text":"<pre><code>vibrate variants prune [flags]\n</code></pre> <p>Removes Vibrator-managed objects. By default it removes stopped containers and unused (removable) images \u2014 running containers are skipped unless you pass <code>--force</code>.</p>"},{"location":"reference/commands/variants/#flags","title":"Flags","text":"Flag Default Description <code>--containers</code> <code>false</code> Remove containers only (skip images). <code>--images</code> <code>false</code> Remove images only (skip containers). <code>--force</code> <code>false</code> Force-remove running containers (kill + <code>rm</code>). <p>With neither <code>--containers</code> nor <code>--images</code>, both are pruned. In-use images (those a container still references) are skipped automatically.</p>"},{"location":"reference/commands/variants/#examples","title":"Examples","text":"<pre><code># Remove stopped containers and unused images.\nvibrate variants prune\n\n# Only clean up containers, including running ones.\nvibrate variants prune --containers --force\n\n# Only remove images.\nvibrate variants prune --images\n</code></pre>"},{"location":"reference/commands/variants/#related-pages","title":"Related pages","text":"<ul> <li>Naming &amp; labels \u2014 the labels these commands filter on.</li> <li><code>vibrate update</code> \u2014 note that image-layer updates leave a dangling old image   that <code>prune</code> will collect.</li> </ul>"},{"location":"reference/commands/wizard/","title":"<code>wizard</code> / <code>reconfigure</code>","text":"<p>Two ways to drive the interactive setup form. <code>wizard</code> is a preview-only sandbox; <code>reconfigure</code> actually updates an existing workspace and rebuilds it.</p>"},{"location":"reference/commands/wizard/#vibrate-wizard","title":"<code>vibrate wizard</code>","text":"<pre><code>vibrate wizard\n</code></pre> <p>Runs the setup wizard standalone. Nothing is written to disk and no image is built. It prints a summary of your selections and the equivalent <code>vibrate</code> command. Useful for previewing what a configuration looks like, or for verifying the wizard's behavior, without committing to anything.</p> <p>To actually use a configuration, copy the printed equivalent command, or run <code>vibrate</code> (which runs the wizard inline and does save + build).</p>"},{"location":"reference/commands/wizard/#vibrate-reconfigure","title":"<code>vibrate reconfigure</code>","text":"<pre><code>vibrate reconfigure [flags]\n</code></pre> <p>Alias: <code>reconfig</code></p> <p>Re-runs the wizard for an existing workspace and rebuilds the container with the new selections. Requires an existing <code>.vb</code> \u2014 it's a modification flow, not first-time setup.</p> <p>What's preserved (carried through untouched):</p> <ul> <li><code>[prereqs.*]</code> \u2014 minted API keys, team/project IDs.</li> <li><code>[env]</code> \u2014 custom host\u2192container env forwarding.</li> <li><code>with</code> / <code>no</code> \u2014 your per-workspace feature toggles.</li> </ul> <p>What the wizard re-asks (and overwrites): harness, profile, shell, extensions, LLM provider, integration hosting modes.</p> <p>By default the old container \u2014 built for a spec that no longer matches \u2014 is removed, and a fresh one is built (<code>--rebuild</code> semantics).</p>"},{"location":"reference/commands/wizard/#flags","title":"Flags","text":"Flag Default Description <code>--keep-container</code> <code>false</code> Leave the old container running/stopped instead of removing it (so you can switch back without rebuilding). <code>--dry-run</code> <code>false</code> Walk the wizard and preview the new configuration without saving or rebuilding."},{"location":"reference/commands/wizard/#examples","title":"Examples","text":"<pre><code># Change harness/profile/extensions for this workspace and rebuild.\nvibrate reconfigure\n\n# Preview the change without touching anything.\nvibrate reconfigure --dry-run\n\n# Switch setups but keep the old container around to flip back to.\nvibrate reconfigure --keep-container\n</code></pre>"},{"location":"reference/commands/wizard/#the-wizard-flow","title":"The wizard flow","text":"<p>Both commands present the same gated form. Each step is shown only if the corresponding field isn't already set (by a flag, the existing <code>.vb</code>, or \u2014 for <code>reconfigure</code> \u2014 left deliberately blank to be re-asked):</p> Step Shown when Choices Harness harness unset <code>claude-code</code>, <code>codex</code>, <code>opencode</code>, <code>pi</code> Profile profile unset <code>minimal</code>, <code>backend</code>, <code>frontend</code>, <code>full</code> Shell shell unset <code>bash</code>, <code>zsh</code>, <code>fish</code> LLM provider LLM unset and harness supports it <code>openai</code>, <code>anthropic</code>, <code>ollama</code>, <code>lmstudio</code>, <code>openai-compat</code> Extensions always multi-select from the harness's catalogue Serena hosting not pinned and harness is claude-code <code>auto</code>, <code>host</code>, <code>local</code>, <code>off</code> <p>The extensions step pre-checks entries that are either marked default or detected on your host. The LLM step branches into a cloud path (model + auth) or a local path (URL + model picker) \u2014 see LLM providers.</p> <p>On completion the wizard prints a summary and the equivalent command \u2014 the exact flags that reproduce the selection, so you can skip the wizard next time.</p>"},{"location":"reference/commands/wizard/#related-pages","title":"Related pages","text":"<ul> <li>Quick start \u2014 the wizard in context.</li> <li>The <code>.vb</code> file \u2014 what the wizard writes.</li> <li>LLM providers \u2014 the provider selection step in detail.</li> </ul>"}]}